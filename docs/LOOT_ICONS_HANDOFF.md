# 掉落物像素圖示：交給 Claude 接入

2026-09-20。使用者要求整理工程並交付 Claude。Codex 已完成素材，尚未接入遊戲、升版、提交或推送。此次只交付五類掉落物顯示素材，不改拾取、掉落、數量或存檔規則。

## 採用來源（在 Codex 主工作目錄）

- `C:/codex_projects/shooter_roguelike/art/loot-icons-v1/retouched.aseprite`
- `C:/codex_projects/shooter_roguelike/art/loot-icons-v1/retouched-atlas.png`

**以這份使用者親自修改過的工程與匯出圖為準。** 2026-09-20 Codex 再將道具格改為「左上醫療十字＋右下單眼夜視目鏡」。使用者知道放大看稍奇怪，願意以縮小的意象繼續。其他圖格與使用者的框線未改。

工程重新匯出與上述 PNG 的 SHA-256 完全相同：
`51b40f951a2c7b10765b1c725f812acafbea3ac091663984fab41870b2dfd29e`

## 圖集規格

128×32，8 欄×2 列，每格 **16×16**，透明背景，保留角框及其半透明光暈。座標從零開始：`x=col*16, y=row*16`。

| 欄 | 彈種／分類 | 第 0 列 | 第 1 列 | 配色 |
| --- | --- | --- | --- | --- |
| 0 | pistol | 武器（手槍圖） | 彈藥（子彈圖） | #7fa9dd |
| 1 | rifle | 武器 | 彈藥 | #d9b46a |
| 2 | shell | 武器 | 彈藥 | #e08a63 |
| 3 | energy | 武器 | 彈藥 | #62c6c9 |
| 4 | ordnance | 武器 | 彈藥 | #b58ad8 |
| 5 | 所有投擲物 | 手榴彈 | 重複格 | #c3cf7c |
| 6 | 所有一般道具 | 醫療十字＋目鏡 | 重複格 | #b4c3c9 |
| 7 | 廢料 | 板件／螺栓 | 重複格 | #ae9270 |

武器圖全部使用通用手槍輪廓，不按槍型切換。彈藥圖全部使用通用手槍子彈輪廓，不按彈種切換造型；兩類只按選單彈藥色區分。投擲物與道具不再細分造型或顏色。配色已烘入圖集，直接畫即可，避免再次整體染色改掉高光與角框。

近戰武器沒有彈種：本輪尚未製作專用色格。若要覆蓋近戰掉落，建議通用手槍輪廓套現有 `MELEE_TINT`，這是接入建議，不是使用者已確認的額外造型。資料、鑰匙卡、職業遺體等特殊任務物件不在本批五類規格內，不應直接全部歸為道具而覆蓋原本特殊提示。

## Claude 接入範圍

1. 將確認版圖集複製到正式 assets 路徑，新增座標／分類對照。先對照最新 renderer 的地面物品種類，不改資料 ID。
2. 使用 nearest-neighbor，禁止圖片平滑。不要再生成一份更小的內部圖示；16×16 本身已包含角框與光暈。世界縮放可以沿用既有規則；最終螢幕顯示大小由使用者在遊戲裡確認。
3. 五類地面掉落以本圖替換舊字母／符號，避免同時疊原圖示與文字。背包與選單不用改。
4. 不新增光柱、閃爍、額外外框或程式光暈，這些都不是本次素材要求。特殊任務物件原本的光柱另議。
5. 不順便實作先前討論過的隱藏開關、同格合併或相鄰展開；那些只是早期提案，尚未採用。
6. 補離線素材清單，驗證五彈種的武器／彈藥映射、所有投擲物與道具分類、透明邊緣、縮放與暗房可讀性。只改顯示，不需升 SAVE。
7. 依目前 RELEASE 流程處理遊戲整合與發布；這份交付本身不代表已部署。

## 保護手工修改

- **不要執行 `retouch.lua`、`framed.lua` 或 `process.lua` 覆寫採用檔。** 它們只記錄先前製作流程，無法重現使用者最新手工修改。
- `replace-item.lua` 是此次局部改圖紀錄，也不用重跑。
- 舊 `retouched-preview-5x.png`、`retouched-native.png` 仍是手工修改前的候選，不是交付依據。
- `before-medical-optic.aseprite` 備份使用者修改後、道具替換前的工程。
- 未追蹤資料夾含許多未採用圖；不要整批加入正式素材。保留採用工程、匯出圖、必要來源提示／製程與本交接即可。

## 已做驗證

Codex 以 Aseprite 批次重新匯出，PNG 雜湊與交付圖一致。上一輪逐像素確認道具修改僅位於兩個道具格的內部 12×12（共 177 個像素）；其他格與使用者框線未變。未進行遊戲整合測試，本輪未執行 npm test／build。

## Claude 接入紀錄（3.154.0，2026-09-20）

- **採用來源**：Codex 主目錄的 `retouched.aseprite` 與 `retouched-atlas.png`，SHA-256 `51b40f95…2dfd29e`，已確認與文件相符（128×32、8 欄×2 列、每格 16×16、五個彈種色與三個分類色都對得上）。兩個檔都複製進 `art/loot-icons-v1/`。
- **使用者調色**（`qa/loot-icon-lab.html`，2026-09-20）：明度 0.8、彩度 1.4、對比 1.09。數值**烘進圖集**後存成 `assets/pixel/loot-icons-v1/atlas.png`（SHA-256 `074fc983…f914777e`），執行時不套任何濾鏡；未調色的原圖保留在 `art/loot-icons-v1/retouched-atlas.png`，調色後的副本是 `retouched-atlas-tuned.png`。
- **圖示大小**：`LOOT_ICON.size` 21px（滿格 38px 時），沿用 `itemScale` 隨格子縮小。
- **分類依背包**（使用者決定 2026-09-20）：`PREPARED_CATALOG.grenade` 的四項是投擲物；照明彈、誘餌、地雷在背包屬於道具，所以歸道具；護甲板也歸道具；廢料自成一類。測試 `tests/loot-icons.test.mjs` 直接讀背包目錄，改背包分類而沒改這裡會被擋下來。
- **近戰武器**（使用者確認「維持使用無妨」）：圖集沒有近戰色格，改用通用手槍輪廓套 `MELEE_TINT` 染色，這是唯一一處程式染色，結果會快取。
- **特殊物件**：鑰匙卡、資料碎片、學習資料維持原本的光柱與標記，不歸進道具（文件接入範圍第 4 點）。地上因此會同時存在新像素圖與舊標記，這是預期的過渡狀態。
- **拿掉的疊加**：武器上方的代號文字、補給快取的名稱文字、武器外框與光暈都不再畫（文件接入範圍第 3、4 點）。
- **縮小門檻**（Claude 判斷，使用者授權）：`ITEM_SCALE.hideBelow` 25 → 20。窄版面最縮的格子是 24.7px，舊門檻剛好把最縮的一階整個關掉；改成 20 之後整個縮放範圍都看得到，最小的圖示仍有 13.7px。
- **未做**：交接文件第 5 點提到的隱藏開關、同格合併、相鄰展開，維持未採用。

## 3.177.6 撿不了的半透明（使用者）

- 地上的掉落物如果現在走上去一點都拿不到，就用 `LOOT_ICON.dim`（45%）的不透明度畫，包括還記得位置但看不到的。
- 判斷是 `Game.canTake(item)`，只讀不改，逐項照 `pickup()` 的規則：
  - 武器欄已滿；
  - 彈藥、投擲物（共用上限）、醫療包與道具已經封頂；
  - 護甲板已滿；
  - 外骨骼已經有或是大型職業；
  - 夜視鏡、熱像鏡已經有。
- 廢料、資料、繩索、鑰匙卡永遠拿得到。
- 用掉一些、有空間了，下一次畫面就恢復。
- 繪圖在 `Renderer.groundItem`。`tests/loot-dim.test.mjs` 讓每種掉落物一路撿到滿，每一步都比對 `canTake` 和 `pickup()` 實際做了什麼。
