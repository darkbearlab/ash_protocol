# 3.88.2 模擬訓練結束一律「篩選合格」QA（Claude，2026-09-15）

- **使用者決定**：
  - 模擬訓練結束時，狀態一律顯示「篩選合格，後續績效尚待評估」。
  - 分數權重交由 Claude 與 Codex 決定。

## 修改

- **`src/killhouse-ui.js`**：
  - 新增 `TUTORIAL_VERDICT`。
  - 教學結算不再套用肅清分級，改列三行：單位（編號 模擬結束）、狀態「篩選合格」、備註「後續績效尚待評估」。
  - 上方文字改成「訓練紀錄已提交。」。
- **`expansion.css`**：新增 `data-verdict="screened"` 樣式：橘色邊線，狀態字橘色。
- **不變**：街機結算照舊顯示分數、肅清率、回合數與肅清評估；戰役結算不變；教學的肅清率照常計算。

## 自動測試

- `npm test` 852/852。
- **既有測試修改**（理由：使用者決定）：tests/killhouse-ui.test.mjs 的教學結算斷言改寫：
  - 教學沒殺任何人時，肅清分級是「不足」，但結算畫面只出現「篩選合格」與「後續績效尚待評估」。
  - 畫面不出現任何分級文字。

## 瀏覽器

- 本機 dev server 用實際樣式畫出教學結算（肅清分級為不足的局面）：單位 C-0567 模擬結束、狀態「篩選合格」為橘色、備註「後續績效尚待評估」，左側橘色邊線；不含任何分級文字。這是畫面與樣式檢查，不是從頭打完教學。

## 未驗

- 從頭打完教學後的實際結算流程，目前由測試保證文字與按鈕。
- 真手機。
