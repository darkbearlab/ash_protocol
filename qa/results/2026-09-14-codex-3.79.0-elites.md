# 3.79.0 精英規則（派系框架之後的獨立提交）

基底 df81f153bbda5db5d6f24d5a50e3fea4c181b67a（3.78.0）。範圍僅 ELITE_ENEMIES 第 2 節；頭目／雜兵排除已獲使用者明確確認。介面由 Claude 接。

## 前一批發布補記

3.78.0 由 Claude 代為推送 df81f15。本輪已直接查詢 GitHub API：該 SHA 的 [Pages 流程 34824003992](https://github.com/darkbearlab/ash_protocol/actions/runs/34824003992) completed / success。前批報告的「發布受阻」是當時狀態，目前已解除。

## 差異檢查與基準

先執行 `node qa/enemy-data-identity.mjs`，退出 1（預期），完整保留 [154 筆差異](2026-09-14-codex-3.79.0-identity-diff.txt)。檢查器只移除最多印 30 筆的截斷，擷取、正規化、雜湊算法不變。

| 範圍 | 差異數 |
| --- | ---: |
| 現行第 9／10／11／12 層 | 21／27／36／39 |
| 偏移 6，第 3／4／5／6 層 | 2／6／9／9 |
| 無盡第 12 層任務局面 | 5 |
| 其餘範圍（包含機器人） | 0 |

共 149 張生成地圖、5 份局面改變。偏移 0 第 1–8 層 320 張、舊生成器 240 張、偏移 6 第 1–2 層 20 張、其他任務局面 105 份、24 次機器人均完全一致；另有 51 張符合深度但未抽中精英的地圖也一致。

以完整 154 行清單逐行驗證 group/key 白名單，確認沒有越界、清單未截斷，才執行 `node qa/enemy-data-identity.mjs --write`。重錄後再執行相同檢查，結果 identical to baseline：780 張地圖、110 份局面、24 次機器人全數一致。

## 實作與驗證

- 七項新增測試：深度曲線、原詞條／非精英逐一不變、補抽適用性與頭目／雜兵排除、兩種出生入口、深層地形／物品／基礎數值不變、XP 與非 XP 獎勵、save41／舊檔／封存／完整備份、召喚不繼承（部分合併在同一 test）。
- 七項已通過；完整 npm test 766/766 通過。既有 runtime-enemies 的基礎敵人比較，與已排除的 affixes 一樣，新增排除 elite 欄位；數值、物資、地圖斷言不變，精英由新增測試獨立驗證。其他既有測試未改。
- `npm run build` 通過；場景產生器可產生第 8／12／60 層 QA 存檔。未做瀏覽器或真手機驗證，不宣稱自然平衡已通過。
- 數值採規格預設：9 層起每層 +2%、上限 15%、至少 3 詞條（不足適用池則用盡）、經驗 ×2。其他資源不加成。
- save41；profile5、backup1。只在實際生成精英時包世代11，未抽中保持原世代10，以同時保留變更標記與淺層原基準。執行期精英出生不重寫既有地圖描述。
- 召喚只借用基本兵種，不繼承精英或詞條；敵人屍體保留旗標供未來染色。API 與介面交接見 ELITE_ENEMIES 第 6、7 節。

## 本批發布

本批測試、build 與重錄後比對均已通過，作為 df81f15 之後的獨立規則提交推往 main。推送後確認同一 SHA 的 Pages 流程，最終發布結果附於交付回覆。無外觀或介面變更。
