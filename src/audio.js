export class AudioFX{
 constructor(){this.enabled=true;this.ctx=null;}
 play(type){if(!this.enabled)return;try{this.ctx??=new(window.AudioContext||window.webkitAudioContext)();if(this.ctx.state==='suspended')this.ctx.resume();const ctx=this.ctx,t=ctx.currentTime,gain=ctx.createGain();gain.connect(ctx.destination);gain.gain.setValueAtTime(.065,t);gain.gain.exponentialRampToValueAtTime(.001,t+.16);if(type==='fire'){const buffer=ctx.createBuffer(1,ctx.sampleRate*.15,ctx.sampleRate);const d=buffer.getChannelData(0);for(let i=0;i<d.length;i++)d[i]=(Math.random()*2-1)*Math.exp(-i/d.length*5);const n=ctx.createBufferSource();n.buffer=buffer;const filter=ctx.createBiquadFilter();filter.type='lowpass';filter.frequency.value=1600;n.connect(filter);filter.connect(gain);n.start(t);}else{const o=ctx.createOscillator();o.type=type==='heal'?'sine':'triangle';const f=type==='reload'?430:type==='heal'?650:type==='move'?100:240;o.frequency.setValueAtTime(f,t);o.frequency.exponentialRampToValueAtTime(type==='heal'?900:60,t+.13);o.connect(gain);o.start(t);o.stop(t+.17);}}catch{}}
}
