# 3.177.0 — 勝利結算的管制員撤離確認；英文目標列

2026-09-24，Claude。規格：docs/STORY.md 第 8 節（撤離確認）、docs/PURGE_REVIEW.md 第 3 節（英文目標列）。

## 需求

- 使用者：「關於勝利畫面，英文的表格 objective status 後馬上接 status 感覺不大對，如果 objective 那一欄不寫 status，留給下一欄呢？另外這裡應該也可以增加管制員的對話框。」附英文 PURGE COMPLETE 結算截圖。

## 做法

- 語言表：`purge-review-ui.objectiveStatus` 英文改成 `Objective`（中文「目標狀態」不變）；新增 `comms.egret.extracted.1–3`、`comms.wren.extracted.1–3`、`comms.overseer.extracted.1`（「……」）。
- `src/comms.js`：`COMMS_LINES` 三人各加 `extracted`，`COMMS_EXPRESSIONS` 白鷺預設放心、鷦鷯預設咧嘴笑，另兩句各自指定表情。
- `src/controller.js`：`showResult` 依結局選事件（陣亡 `lossReport`、成功 `extracted`，放棄沒有），仍只在剛結束（`fresh`）且非重播時說，停留時間沿用 `kiaSeconds`。
- 台詞是 Claude 草稿，審稿頁 <https://claude.ai/artifact/9VpCrux8idZj4iiALRcEPY>（資料庫 `decisions`）。

## 驗證（Claude 自己跑的）

- `tests/kia.test.mjs`：三人都有 `extracted`、表情存在於臉譜、監視官是「……」、白鷺鷦鷯各三句；結算依結局選事件的那一行。
- 完整 `npm test`：1294 通過、0 失敗；`npm run build` 成功（`/ash_protocol/` 相容）；英文掃描乾淨。
- 瀏覽器（本機 `?test=1`、英文、280 寬）：新開一局後把局面設為成功、按等待觸發結算：上方出現白鷺「Recovery complete. ...Welcome back.」，表格列為 Unit／Objective／Status／Reason。

## 未驗

- 鷦鷯當班時的撤離確認（同一條路徑、只換說話的人）；實機手機上的版面。

## 待決

- 淨化審查可能在同一畫面判「已處決」，這時「歡迎回來」「活著回來了」會變成反諷；是否依審查結果分兩組，等使用者決定。
- 中文「目標狀態」後接「狀態」也有同樣的重複，這一版沒動，等使用者決定。
