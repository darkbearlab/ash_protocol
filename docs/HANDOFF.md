# 快速接手：ASH PROTOCOL 3.39.0

## 3.39.0：工程師無人機改版（Claude 開發，使用者決定）

- 規格：[ALLIES.md](ALLIES.md)「工程師」；報告：[qa/results/2026-09-11-claude-3.39.0-allies-iteration.md](../qa/results/2026-09-11-claude-3.39.0-allies-iteration.md)。使用者決定已註記在 [ALLY_ITERATION_SCOPE.md](ALLY_ITERATION_SCOPE.md) 工程師段。
- `src/allies.js`：`DRONE_HP`／`DRONE_BUILD_COST`／`SENTRY_ARMOR`、`fitDrone`（依模式設定裝甲與 `no_cover`）。`droneLost` 時技能改為生產；`allyAct` 內的 `topUp` 負責自動換彈。移除 `canRecoverWreck`。
- `src/game.js`：save v27 遷移（在 `restore` 結尾：退還追隨機體彈匣內的手槍彈、45→90 HP 上限、`fitDrone`）；無人機被毀提示改為生產。
- 美術待辦：哨兵要改成地面砲台外觀，見 [PIXEL_ART.md](PIXEL_ART.md) 開頭。
- 模擬腳本：`qa/drone-waves.mjs`（三波連戰，可並列新舊 src）。

## 3.38.0：換位解決窄口卡點（Claude 開發，使用者決定）

- 規格：[ALLIES.md](ALLIES.md)「3.38 換位」；報告：[qa/results/2026-09-11-claude-3.38.0-allies-iteration.md](../qa/results/2026-09-11-claude-3.38.0-allies-iteration.md)。
- `src/allies.js`：移除 `pushCell`／`pushReason`／`pushAlly`，改為 `swapReason`／`swapWithPlayer`；`stepToward` 無進展時可 `swapPast`（只在追擊、留守、返回時），條件在 `canTrade`／`attackFrom`；`slotTurn`（WeakMap，不存檔）決定被換者放棄本回合或下一回合。
- `src/game.js`：移動驗證與執行兩處改呼叫換位。存檔不變。
- 推動已移除，使用者表示要觀察是否需要以其他形式回來。

## 3.37.0：德魯伊寵物可回收（Claude 開發，使用者決定）

- 使用者決定取代授權文件的「醫療包救援」與「不增免費復活」，已註記在 [ALLY_ITERATION_SCOPE.md](ALLY_ITERATION_SCOPE.md)。規格：[ALLIES.md](ALLIES.md)「德魯伊」；報告：[qa/results/2026-09-11-claude-3.37.0-allies-iteration.md](../qa/results/2026-09-11-claude-3.37.0-allies-iteration.md)。
- `src/allies.js`：`PET_TETHER`／`PET_REGEN`／`PET_MEDKIT_FRACTION`、`leash(a)` 取代寵物的 `TETHER`；`canRecoverPet`、`tickPackedPet`、`petSkillReason`；移除舊的 `rescueCell`。
- `src/game.js`：寵物倒地／收納時技能耗 1 回合；每個付費回合結束呼叫 `tickPackedPet`（在環境結算之後）；save v26。
- **save v26**：只放寬驗證（寵物可 packed），v25 不轉換；`storage.js` 首讀備份清單含 25。
- 未改：其他友軍、敵人 AI、共用公式、無人機修復流程。

## 3.36.0：友軍第一輪 —— 卡點與跟隨（Claude 開發）

- 依 [ALLY_ITERATION_SCOPE.md](ALLY_ITERATION_SCOPE.md) 第一批。報告：[qa/results/2026-09-11-claude-3.36.0-allies-iteration.md](../qa/results/2026-09-11-claude-3.36.0-allies-iteration.md)；現行規格：[ALLIES.md](ALLIES.md)「3.36 跟隨、交戰與讓路」。
- `src/allies.js`：`allyAct` 重排優先順序（繩索→留守→攻擊→追擊到攻擊位置→閒置跟隨 `FOLLOW_RANGE`）；新 `stepToward` 在精確目的格不可達時以步行距離逼近；新 `pushCell`／`pushReason`／`pushAlly`。`validAllies` 驗證選填 `restTurn`。
- `src/game.js`：只改移動的 `validateAction` 與 `executePlayer` 兩處接線，玩家撞友軍改為推動；沒有動 `initiativeQueue` 或其他行動。
- 數值全未動。下一輪假設：無人機射程 5 與友軍索敵半徑 8（從友軍算）是交戰距離的主要瓶頸；拉長時要一併評估友軍會驚動敵人的代價。

## 文件：開放 Claude 三職業友軍迭代

依使用者要求新增 [ALLY_ITERATION_SCOPE.md](ALLY_ITERATION_SCOPE.md)，開放工程師／德魯伊／死靈法師局部 AI、尋路讓路、指揮與戰力多輪迭代；列出可調範圍、身分與保存保障、測試和回報方式。覆蓋舊交接的一律禁止友軍規則變更條款；建議先卡點再控制再戰力。候選方案未實作，本次只有文件，遊戲仍 3.35.8、save v25／profile v4／backup v1；未重跑本機遊戲測試。已同步 Claude 3.35.4–3.35.8，保留其選單更新。

## 3.35.8：任務說明改為固定位置（Claude 開發）

- 選擇任務畫面：清單只列標題，選中任務的說明顯示在清單下方、「進階」上方的 `.mission-brief`。所有說明疊在同一格以保留最大高度，切換時版面不跳動。
- 切換由 `document` 上的 `change` 監聽處理（限 `.mission-list` 內的 `name="mission"`）。取代 3.35.6 的列內展開。

## 3.35.7：任務共用說明拆進各任務（Claude 開發）

- 部署清單下方的共用段落移除，依任務拆進 `controller.js` 的 `MISSION_NOTES`，接在 `MISSIONS[].text` 後面顯示。
- **新增任務時**：若它的 `text` 沒寫到第 3 層封鎖官、青色菱形或回收耗時，請在 `MISSION_NOTES` 補一句；沒補也能正常顯示，只是少了提醒。不要把這些寫進 `MISSIONS[].text`，它同時是抵達任務樓層的提示。

## 3.35.6：選擇任務畫面精簡（Claude 開發）

- 任務說明只在選中的列展開（`.reveal-notes`；**3.35.8 已改為清單下方固定區塊，此類別已移除**）；種子欄位收進「進階」摺疊（`.seed-advanced`），驗證失敗會自動展開。
- 修正任務列名稱被排進頭像欄、層數看不見的問題（`.term-caret+.term-body` 跨欄）。
- 只動 `showDeployMission()`、`deployOperator` 的錯誤處理與 `expansion.css`；`newGame()` 與部署草稿流程不變。未改規則或存檔。

## 3.35.5：開機文字改為靜態（Claude 開發）

- 使用者要求 `INITIALIZING` 不閃爍，只保留平平的文字；刪除 `boot-blink` 與其 reduced-motion 規則。12 秒重新整理提示不變。未改規則或存檔。

## 3.35.4：開機遮罩（Claude 開發）

- 開啟網址時不再先閃戰鬥介面。靜態 HTML 的戰鬥介面在 41 個模組以瀑布方式載完、`showIntro()` 執行前會先被畫出，冷載入實測空窗約 4.9 秒。
- `<body class="booting">` 期間以 `visibility:hidden!important` 隱藏整個 `.app` 與其所有子元素，顯示置中 `INITIALIZING`；`showIntro()` 開好主選單後同步移除。底色與主選單相同。用 visibility 不影響版面量測。超過 12 秒以純 CSS 浮出重新整理提示。
- **若之後新增會在開機前就需要顯示的元素，請放在 `.app` 之外**，否則會被遮罩隱藏。
- 未改規則或存檔；432 項測試與 build 通過。

## 2026-09-10 文件：升級強化與角色參數對照（Claude）

- 新增 [PERKS.md](PERKS.md)，供使用者調整升級選項時參考：八個強化的精確效果與作用點、六名角色的完整參數表、以及目前沒有任何強化觸及的旋鈕清單。
- 重點發現：選項不是隨機的。`(seed + level×3) mod 8` 配偏移 `[0,1,3]`，**全遊戲只有 8 組三選一**，種子只決定起點；有 4 對強化永遠不會同時出現；沒有追蹤已拿過什麼，可無上限重複疊加。
- 八個強化裡沒有一個觸及四個戰鬥欄位、近戰、護甲板上限、武器攜行上限、技能持續／冷卻、被動特質或友軍參數 —— 這些資料結構在 3.25 與 3.30 之後都已就位。
- 另附 3.35.0–3.35.3 的外部驗證報告 [qa/results/2026-09-10-claude-3.35-anchor-doors.md](../qa/results/2026-09-10-claude-3.35-anchor-doors.md)：432 原測＋20 項獨立引擎測試通過，**未發現缺陷**；下錨的「普通與緩速各一次」實作、失能只扣一次、著彈修正不重複累積、斜角門的四斜角與阻隔判定都已逐條確認。未驗項為下錨後的 UI 即時更新與雙段演出順序（工具窗格 rAF 凍結）。
- 純文件，遊戲維持 3.35.3，未改程式或存檔。

## 3.35.3：下錨按鈕簡化

下錨改用既有單色菱形技能圖示，避免彩色錨 emoji 與介面不一致；狀態只顯示「啟動／解除」，按鈕不再附「1 回合」。耗時仍為各一回合，技能詳細說明保留，存檔與遊戲規則不變。33 項下錨／技能測試與 build 通過；未跑瀏覽器，視覺驗收見根目錄紙條。

## 3.35.2：斜角互動開關門

門兩側格與四個側邊斜角均可透過互動鈕開關；斜角必須能直接通往同側門前格，不能隔著實牆、實心物件或未破壞的阻路邊操作。每次仍花 1 回合、人留原地，方向標籤支援東北等斜角。只擴大玩家操作範圍，不改移動開門、掩體範圍或敵人開門 AI；快速單位行動後會重新檢查是否可操作。save v25／profile v4／backup v1 不變。新增四項規則測試；全部 432 項 Node 測試與 build 通過，手機驗收見根目錄紙條。

## 3.35.1：下錨不影響投擲物

使用者更正：下錨雙攻擊只限武器射擊與近戰（含撞敵動力拳），所有投擲物仍在原本速度投一次、扣一顆。移除 3.35 的投擲雙次排程與跳過視線複查例外，煙霧／自震暈恢復原時序；武器與回合／保存規則保持。save v25／profile v4／backup v1 不變，既有下錨存檔直接續玩。升級選單不接邏輯。相關測試覆蓋四種投擲只扣一次及原緩速時序；428 項回歸與 build 通過，沿用更新後八份 3.35 fixture（single-smoke 取代 double-smoke）。

## 3.35.0：重裝兵「下錨」

- 已快轉同步 Claude 的 3.34.1（220ad76）：終端主選單／設定、升級卡片與攜行重置、NORMAL／DAILY／QUICK、部署清單及折疊資料保留。本輪依使用者要求只接下錨，不接任何升級／解鎖邏輯；技能佔位清單若自動列出下錨，仍為純展示。
- Bulwark 起始學會並預備 anchor。開啟／解除各花 1 回合，在原本行動時點執行；無冷卻、持續至手動解除。下錨不能移動／翻越／換層；原地開關門、互動、撞敵動力拳仍可用。取消預備不解除效果，須重新預備後付一回合解除。
- 啟動添加 clumsy／source=skill:anchor，解除只刪此來源。原 character:bulwark 的笨拙保持，同名不疊；笨拙本身是移動閃避規則，下錨沒有再追加命中減益。
- 下錨時自己的 fire／bumpMelee 在普通 0 與緩速 1 各執行一次，玩家同速優先；通常是快速敵人→玩家第一輪攻擊→普通友軍／敵人→玩家第二輪攻擊→緩速友軍／敵人。此兩攻擊時點固定；其他行動依原 initiative。世界只 +1 回合，友軍不加倍，環境／技能／traits 倒數一次。
- 兩次獨立判定、各付彈藥，輕機槍最多 6 發、動力拳兩擊；資源不夠只執行付得出的部分，不補彈。第二次射擊／近戰仍原目標 ID，逃離／死亡不轉移目標，射擊依原承諾規則向最後位置落空仍扣彈；原 burst 遇死亡提前中止規則保持。投擲物不受下錨影響，依原本速度投一次、扣一顆。
- 失能不解除下錨；原本失能該輪跳過兩次、計一次，兩段間才被震暈則取消第二次並計一次。失能免疫每輪只扣一次。玩家死亡即停止。著彈修正按連續回合累積一次，兩段使用同一輪開始的修正；等待瞄準在第一段用掉。
- save v25／profile v4／backup v1。v24 重裝兵只補未啟動技能、空預備欄才填入，不改 HP／彈藥／RNG／友軍。保留 v24 首讀原字串。toggle 使用 remaining 0/1、cooldown 0，不倒數；驗證技能狀態與笨拙來源一致。
- 428 項 Node 測試、build 通過；新增 17 項 anchor 專項及八份 qa/create-3.35-fixtures.mjs。Claude 3.31 報告已讀：411＋23 項通過，修復／部署畫面已驗、殘骸回收與自然局等仍未測。新技能手機／演出／自然難度依最新驗證紙條，Codex 未跑瀏覽器。

## 2026-09-10 開發分工交接（遊戲仍為 3.31.0）

使用者為節省 Codex 週額度，將數值、全域經濟、選單等非規則層工作交 Claude 接手；Codex 於規則需求再介入。[給 Claude 的交接](../給Claude的交接.md) 列出授權邊界、原始碼／規格位置、存檔保障、工作樹整合與 main 發布、回報格式。舊「只提交 QA 報告」限制僅適用純驗證任務；完成的開發仍須測試、更新文件、發布 main。本次只改文件，不升遊戲版號、不重跑本機遊戲測試。

## 3.34.1：部署選單終端化與可折疊幹員資料（Claude 開發）

- 任務／行動員選擇改成終端機清單（`SELECT CONTRACT`／`SELECT OPERATOR`、`>` 游標、細分隔線、等寬說明）。原生 radio 只是視覺隱藏，無障礙不受影響。
- 行動員列有 26px 頭像與簡短代號，**詳細資料每名各自 `<details>` 折疊**。任務列標示 `6F`／`3F`。
- **三種模式在有存檔時都會撞到放棄保護**（NORMAL／DAILY 的結束鈕為「確認放棄並部署」，QUICK 有獨立確認頁），實測瀏覽各層不動存檔。警告改為橘色左邊條。
- 願望清單第 9 條：**每日種子之後必須再串雜湊文字**，目前 `YYYYMMDD` 可被提前推算；已註明客戶端鹽值只提高門檻、真正防堵需伺服器。**尚未實作。**
- 未改規則或存檔；save v24／profile v4／backup v1 不變。411 項測試與 build 通過。

## 3.34.0：三種開局模式（Claude 開發）

- NEW GAME 分層：模式選擇 → （NORMAL 才有）任務與種子 → 行動員 → 開始。
- **NORMAL** 等同原部署畫面拆成兩步。**DAILY** 以本地日期 `YYYYMMDD` 為種子、任務由同種子雜湊推導（同日同局），跳過任務與種子畫面。**QUICK** 全隨機，無進行中的局時直接開始，有局則先確認放棄。
- 選角順序與升級頁共用 `OPERATOR_ORDER`：士兵、偵察兵、工程師、死靈法師、德魯伊、重裝兵。
- **`newGame(seed,character,mission)` 未改**，仍是唯一入口；任務與種子改由模組層 `deployDraft` 在畫面間傳遞，`abandonRun()` 結算與放棄警告保留。QUICK 直接開始只發生在沒有進行中的局時，不會靜默毀掉存檔。
- 未改規則或存檔；save v24／profile v4／backup v1 不變。411 項測試與 build 通過。Claude 已實測三條路徑（含 QUICK 的有局／無局兩種分支）與返回導覽。

## 3.33.1：升級分類可折疊與排序（Claude 開發）

- 五個分類改為原生 `<details>` 折疊，標題列有卡片數與 `+`／`−`；預設只展開「攜行容量」。重置區固定在最下方不收合。
- 幹員順序：士兵、偵察兵、工程師、死靈法師、德魯伊、重裝兵（使用者指定）。**技能順序由 `OPERATOR_ORDER` 自動推導**，跟著擁有該技能的幹員排，重裝兵無固有技能故不貢獻項目；新增角色／技能會自動跟上。
- 願望清單記錄重裝兵技能提案「下錨／站穩腳跟」（開啟與解除各 1 回合，開啟期間攻擊兩次）——**未授權實作**，屬規則層，附六項待釐清定義。
- 未改規則或存檔；save v24／profile v4／backup v1 不變。411 項測試與 build 通過。

## 3.33.0：永久升級卡片頁與重置（Claude 開發）

- UPGRADES 改為方形卡片格狀（攜行容量／幹員／武器／投擲物／技能／重置，共 30 張），每張一行短提示＋等級＋按鈕。320px 兩欄。
- **只有攜行容量是實際功能**，沿用 `purchaseCarrying()`。**其餘四區是靜態佔位，一律顯示「已解鎖」，不讀也不寫 profile** —— 之後接真實 gating 不必先清假資料。
- 新增 `resetCarrying(game)`（storage.js）：`carryingSpent()` 精確退還已投入點數、等級歸零、單次 atomic write，再走既有 `setCarryLevel()` 讓超額備彈落地。`protocol.earned` 不動。免費，有確認頁。
- **給 Codex**：解鎖鉤子盤點與待接需求全部寫在 [PROGRESSION.md](PROGRESSION.md)「3.33.0」——武器鉤子完整但九把都沒 `unlockId`；`unlocks.characters` 只有欄位沒有閘門；道具／技能／成就完全不存在。實際 gating 屬規則層，且**既有玩家的六角色與九把武器不可倒退**。使用者另要求部分幹員以遊玩成就解鎖，需新設計。
- save v24／profile v4／backup v1 不變。411 項測試與 build 通過。Claude 已實測購買、退款守恆與版面；真機手感交使用者。

## 3.32.1：設定選單依情境切分（Claude 開發）

- `settings()` 依 `entered` 切分。**主選單進入＝「系統設定」**：音效、移動邊界白線與不透明度、完整備份／還原／下載還原前備份、匯出／匯入任務、任務紀錄與敵人圖鑑、重置遊戲進度、← 返回主選單。
- **局內（☰／Escape）＝「作戰設定」**：維持原本 15 項，額外分出「本局」區塊（永久攜行升級、任務簡介、放棄本局、重新部署新任務）與並列的作戰指南，關閉鈕仍是「繼續任務 →」。
- 主選單版本拿掉的是重複入口（作戰指南／永久攜行升級已在主選單 MANUAL／UPGRADES）與局內專屬功能（任務簡介／放棄本局／重新部署）。
- **修正一個 3.32.0 引入的斷點**：局內「任務簡介」指向 `data-modal="intro"`，而 `showIntro()` 現在是主選單，會從局內跳出標題畫面。已改指 `showMission()`，並在 `data-modal` switch 追加 `case 'mission'`（原僅在 `data-action` switch）。
- 新增「顯示／存檔／紀錄／本局／危險」小標，沿用 `.eyebrow`，只加 `.settings-section` 上邊距。
- 未動規則、`Game.action` 或存檔欄位；save v24／profile v4／backup v1 不變。411 項測試與 build 通過。Claude 已實測兩種情境的按鈕清單、關閉鈕文案、音效與滑桿重繪、返回路徑，以及 375×812／320×568 版面。真機手感交使用者。

## 3.32.0：終端機風主選單（Claude 開發）

- 標題畫面改為暗底滿版：置中放大的 A 標誌（沿用 assets/icon.svg 造型）、ASH PROTOCOL 英文字標，下方五列等寬大寫的終端機風選單。原本的故事文案、四句規則短句與幹員頭像已移除。
- 選單為 **CONTINUE／NEW GAME／UPGRADES／MANUAL／SETTING**；作戰指南由次要按鈕扶正為一般入口 MANUAL。每列右側只留一行極短狀態（角色與樓層、協定點數等）。
- **導覽邏輯完全未變**：五個入口沿用既有 `data-modal`（`enter`／`deploy`／`carrying`／`help`／`settings`），沒有新增或修改任何 handler、`Game.action` 或存檔欄位。CONTINUE 在沒有可續任務時停用並顯示「無進行中的任務」。
- 實作：`modal()` 增加第三個 `title` 參數（預設 false，其他對話框不受影響）、`#modal.title` 修飾類與 `.title-*` 樣式。只動 `src/controller.js`、`expansion.css`、版號與 `sw.js` 快取名。
- save **v24**／profile v4／backup v1 **不變**，未升存檔版本。411 項既有測試與 build 通過，80 項預快取無缺件。
- Claude 已驗：375×812 與 320×568 無橫向溢出、註記不換行不裁切、內容在視窗內；五個入口與返回路徑逐一實測，`title` 類不外洩到其他對話框。**真機手感與觀感交使用者。**

## 3.31.0：無人機殘骸回收與收納維修

- 追隨／放置僚機共用原機體。損毀後在同格或四方向相鄰、無阻隔且殘骸未被其他活單位占據時，以任一已預備僚機技能回收；花 1 回合，不花廢料、不補 HP／彈藥、不更換機型。地上殘骸仍留在原樓層，收納殘骸可隨玩家換層。
- 背包→技能：列收納／待修復、HP／彈藥，收納機體才有修復按鈕。暫定 10 廢料＋1 回合，每次 +ceil(最大 HP×50%)，45 HP 機體為 +23、最多補滿；零 HP 可修復但不能部署，修好仍收納，部署另花一回合。滿血／資源不足／失能停用，維修不用預備該技能。
- src/allies.js 的 DRONE_REPAIR_COST／DRONE_REPAIR_FRACTION 集中暫定價格與比例，UI 同源；修復只改 HP／廢料，原彈匣、備彈、身分、機型、失能保持。殘骸回收不裝填；活機體回收與再次部署仍從玩家備彈裝填。快速敵人先殺／失能玩家不扣修復費，先占殘骸則取消回收，但已承諾回合照付。
- save v24：packed 僅無人機允許 HP 0，active 仍要求正 HP。v23 保留全部友軍與資源／RNG，不免費復活；首讀備份原字串至 ash-save-v23-backup（QA 前綴）。profile v4／backup v1 不變。
- 411 項 Node 測試與 npm run build 通過，80 項預快取無缺件；新增九項引擎測試與七份 qa/create-3.31-fixtures.mjs；手機按鈕／回收修復演出、10 廢料價格的自然局平衡交最新驗證紙條，不自行做瀏覽器 QA。

### 外部驗證結果（Claude，2026-09-10）

報告：[qa/results/2026-09-10-claude-3.31-repair.md](../qa/results/2026-09-10-claude-3.31-repair.md)

- 線上確認 BUILD 3.31.0，建置雜湊 `7152c8875f9b` 與本機 build 一致；411 項測試、build、80 個預快取入口 0 缺件皆與宣稱相符。**未發現缺陷。** 另有 23 項獨立引擎測試，第一次執行即全數通過。
- **本輪影格迴圈可用，修復與部署的完整耗回合流程已實機跑完**：修復（回合 2→3、廢料 40→30、HP 0→23、彈匣 3 不變、仍收納、備彈不動、記錄正確、自動退出背包播放）；第二次修復顯示 +22 並補到 45；滿血按鈕「狀態完好」停用；9 廢料時「廢料不足」停用且副標「需要 10 廢料 · 1 回合」；四個分頁瀏覽回合不變。
- 部署實機：修好後鈕變「部署 · 1 回合」，免費改預備哨兵後部署 → 回合 4→5、手槍備彈 24→27（退回原 3 發）、步槍備彈 48→40（扣 8 發）、彈匣 8、機型切換、HP 45 保持、同一台 id、bornTurn＝當前回合。
- 引擎層確認：殘骸回收距離／同格／關門／敵人占位、0 HP 收納不可部署、回收不補血不補彈；停用原因六種逐項正確；活機體不可遠端維修、失能不靠修復洗掉；無效修復不扣廢料不耗回合；save v24 四種 status/HP 組合；v23 遷移不免費復活；UI 顯示 +N 與實際回復四種情況完全一致。
- `qa-ash-save-v23-backup` 經真實本機首讀產生，與遷移前存檔逐位元組相同（23953 bytes）。320×568 橫向溢出 0、技能列表可捲、控制區在視窗內。
- 未驗證：地圖上殘骸回收的實機觸控路徑（規則已由引擎測試覆蓋）、承諾時序的實機重現、`legacy-v23-wreck` fixture 本身的匯入、背景暫停行為。**10 廢料佔位價格的自然局平衡仍交人類**；報告附了與武器改裝 25 廢料的比價供參考。3.26.x–3.30 既有未驗收項目原樣保留。
- 本輪為純 QA：Claude 未修改程式或美術，未提交、未推送；`qa/node/` 依慣例不進版控，只提交報告。
- 已只讀核對並原樣收錄 Claude 3.30 報告：402 原測＋30 獨立測試通過，沒有規則缺陷；免費寵物指揮／友軍標記有受測畫面確認，耗回合播放、換層與三職業完整自然局仍待驗，不視為全數完成。

## 3.30.0：共用友軍與三個新職業

- 新免費 Engineer／Druid／Necromancer；基礎生命 100、裝甲 0、標準配給。工程師與死靈法師 SMG＋霰彈槍，德魯伊步槍＋霰彈槍；原三角色保持。
- src/allies.js 共用友軍：獨立身分、來源、任務 ID、生命／狀態／指令／彈藥／所在樓層。敵人能選擇較近可見友軍，蓄勢追蹤原身分、狙擊仍鎖落點；同速玩家→友軍→敵人，快慢被動照常。當輪新部署／復活／召喚不追加行動，回收者退出佇列。
- 工程師起始一台收納機體，追隨／放置兩技能共用。追隨型 45 HP、12 發手槍彈、射程 5、基礎命中 55%；哨兵 8 發步槍彈、射程 7、命中 60%。部署／三步可通路內回收各 1 回合，以玩家備彈補彈；換型先回收、舊彈退回再裝新彈，不補生命。機體毀損永久失去，首版沒有替代機體掉落／購買。
- 德魯伊一隻 90 HP 獵獸，近戰 24–28／命中 90%、裝甲 1、無掩體。技能點已探索六格內位置，再右下確認，指令免費，下次自身行動執行；點玩家召回。倒地後相鄰按技能消耗醫療包 1／回合 1，救回 50% HP，站屍體格則在鄰近空格復活。生命連結暫不加入。
- 死靈法師「死者徵召」：消耗本層未使用非頭目敵人屍體，隨機補至兩隻；花 1 回合／冷卻 4（含施放輪）。保留種類被動／射程，採召喚數值，不移植頭目技能或爆炸自殺；召喚物死亡不給素材／掉落／獎勵，存活者不補血。
- 所有友軍繩索 6 格；連線中分享自身 8 格視線，實際射擊仍要求射手自己的 LOS／射線。追隨僚機不追擊，哨兵不移動、超距停火；寵物／召喚物只在繩索內接敵，超距優先返回。爆炸／EMP／震撼／掩體／移動／翻越同樣作用，己方子彈不誤鎖友軍，爆炸可友傷。青框標友軍，屍體救援標記與概覽同步，物理內容仍在牆下。
- 換層三步可通路內活友軍自動同行，兩類僚機皆可帶走；保留 HP／彈藥／失能，清移動與留守指令。太遠召喚物消失放名額，不需解除；寵物／機體留在原層（倒地寵物也留下），可回程重新會合。電梯簡示帶／留數，背包技能頁列全部位置／狀態。
- save v23，舊檔補 allies=[]／allySerial=0，不贈送新職業或夥伴，不改原資源；v22 首讀保留原件。友軍為全局帶 floor 的紀錄，不複製進 floorStates，往返避免重複身分；現在／留置／收納／倒地隨完整備份保持。
- 倖存友軍士兵鉤子：addAlly(game,'survivor','rifleman',{point,sourceId,missionId})；共用行動／視線／傷害／同行／保存，獨立於職業技能與召喚上限。本版不生成救援事件、不發任務獎勵。詳見 docs/ALLIES.md。
- 402 項 Node 測試與 build 通過，80 項預快取；新增 31 項專項、12 份 qa/create-3.30-fixtures.mjs。沒有跑瀏覽器，真機指揮／青框、三職業完整自然局留最新驗證紙條。機制通過不等於平衡驗收。
- 已原樣收錄 Claude 3.29 報告：371＋31 獨立檢查通過，沒有規則缺陷；報告列出的動畫、視覺及自然局未驗項保留。外部頭像與私人紙條未納入。

## 3.29.0：戰利品、近射、預警與矮隔板

- 斷訊槍兵步槍掉落 12%→20%，其他機率不變；掉落記錄明確列武器名稱，屍體與其他物資之上顯示武器代碼／金框。撤退增援仍不掉補給。
- 霰彈槍 1–2 格命中 +15 個百分點、基礎傷害 60–72；更遠 42–54、射程 4／彈匣 4／45% 鄰格濺射不變。詞條先調整兩段基傷，再加人物／改裝；背包、比較與實際攻擊共用數值。
- 通道繼承連接其中一房的照明，暗暗保持暗、亮亮保持亮、混合由獨立雜湊決定；交會依連線順序先到者保留，房內照明不被覆寫。維持三暗房／入口亮，不耗戰鬥 RNG。新樓層才採用，舊當層／往返存層不重繪照明。
- 移除明暗界短線，換成格內三段柔化；視野邊緣也向已看見一側漸暗。僅地板遮罩，不使用 blur，不揭露未知地板／物資／敵人，各格明暗與 LOS 仍離散判定。
- Soldier 起始「預警」：免費、曼哈頓距離 8、冷卻 5 次耗回合行動。敵人當下座標光點穿牆顯示，至下一次耗回合行動結束消失，不追蹤、不附名稱／HP、不提供射線或探索。被掃描敵人立即 alert 並記住玩家當下位置。換層清光點、保留冷卻。
- 新 low_partition 格間矮隔板：HP 60；擋普通通行，可朝它移動翻越一格、花 1 回合。不擋視線／射線／爆炸，完整掩體 −35 命中／45% 減傷，角度沿用三段制。每層至多兩段，不替換封閉隔間。點邊可鎖定破壞，無傷不顯示血條；低細軌＋三鉚點，白線不封可翻越邊。
- 敵我翻越後 vaultExposed=true，被射擊命中 +20（近戰不受影響），到下一次自身行動機會前清除；先後手各只涵蓋一次敵方機會，免費／無效操作不清除。落點有人不翻、不隔板撞擊近戰；提交後被快速敵人占位仍耗已承諾回合。近戰敵人面對玩家占住隔板另一側可破壞隔板，不重疊。
- save v22；舊 Soldier 補預警與空欄預備，不補 HP／彈藥；Recon 計時、資源、樓層／往返狀態保持。v21 原件備份、既有存層敵人補 false。當前取消預備不復原。profile v4／backup v1 不變。
- 371 項 Node 測試與 build 通過；79 個預快取入口。新增 16 項專項、八份 qa/create-3.29-fixtures.mjs；手機視覺與自然平衡交最新紙條，未自行做瀏覽器驗收。
- 已原樣收錄 Claude 3.28 報告 qa/results/2026-09-10-claude-3.28-retreat.md：355＋27 獨立檢查通過、無規則缺陷；完整連續往返、部分結果 UI／背景播放仍有限制。使用者已回報 Recon 自然通關、風格滿意；不是所有手機與往返檢查皆完成。僚機保留討論，不實作；外部頭像／私人紙條保持。

## 3.28.0：五回合煙霧與三層往返回收

- 煙霧由 3 輪改 5 輪（含投擲當輪，之後可再行動 4 次），長於失能彈的 2 次敵方行動；SMOKE_DURATION 集中數值。免費裝填／預備不耗時間，紅外線／爆炸規則不變；舊已展開煙霧保留原倒數，新投擲才延長。
- 新合約 roundtrip「原路回收」：1→2→3 取件並擊敗封鎖官，再由原入口 3→2→1 撤離。回程不回血／補彈或重建物資；門箱、補給站、敵人傷勢、地面武器與痕跡保留。出口指向入口、互動鈕改上樓／撤離，最深紀錄仍為 3。
- 一波撤退增援分布三層，每層最多兩名且只排一次；取件／返回該層時預告兩次行動後科技傳送，進場當輪不攻擊，占位就延後。按正常感知取得玩家位置、增援不掉裝備／彈藥／醫療，仍有有限 XP／廢料。鑽地／異界只保留規劃。
- src/retreat.js 明列樓層所有權，去程最多存兩層；角色、武器 slot、任務、點數與 RNG 全局唯一。非目前樓層煙霧／轟炸暫停，回程接續；不重複獎勵。詳見 [RETREAT.md](RETREAT.md)。
- save v21，profile v4／backup v1 不變；舊局任務／資源／Recon 技能計時保持，v20 首讀保留原件。修正放棄任務的無 prototype 快照也能正確記最深樓層。
- 355 項測試、build 通過，79 項預快取無缺件；七份 qa/create-3.28-fixtures.mjs。自然往返難度與手機交最新紙條，腳本清敵只測流程、不作通關平衡證明；使用者正在人工審核前版，尚未回報完成。外部頭像／私人紙條保持。

## 3.27.1：資訊卡優先靠近目標

- 仍限右上／右下／左下，目標精靈／血條／蓄勢為硬性避讓。先比較遮住其他敵人的數量與面積，接著比較目標到卡片最近邊緣的實際連線長度，再比較玩家／HUD 遮擋，最後才用原角落及固定順序打破平手。取代 3.26.3 同樣清空時先保留舊角落的排序，避免跨地圖長線。
- 卡片尺寸、底部 44px 保留、目標框／連線、存檔及技能規則不變。10 項 target-card 測試與 build 通過；手機連線觀感交最新驗證紙條。沿用 node qa/create-3.26.3-fixtures.mjs 的 single／crowded 場景。
- 已只讀檢查 Claude 隔離 worktree 的 HANDOFF 和 qa/results 清單，最新兵種驗證仍為 3.25，尚無 3.27 報告；不可把舊報告當技能驗收。下一批候選為短程往返任務，已詢問使用者方向，尚未加入回程／增援。

## 3.27.0：Recon 訊號斷層

使用者確認持續 3 回合／冷卻 6 回合，已取代先前 2／8 待定提案。免費啟動，3 次耗回合行動內敵人無法更新玩家位置；保留最後目擊搜索、已鎖定狙擊及轟炸。冷卻從啟動同時起算 6 次；免費裝填／換裝／預備不倒數，第三輪效果結束後還有 3 次冷卻。下樓清效果、留冷卻並扣下樓那一回合。

新 Recon 預設學會且預備；舊 Recon 遷移補技能，保留 HP／資源／地圖／RNG。save v20、profile v4／backup v1。技能按鈕顯示就緒／生效／冷卻，背包、部署與說明同步；HUD 不把暫時無法感知玩家的敵人算成直接射擊威脅，仍保留站在狙擊落點的警示。規格 [SKILLS.md](SKILLS.md)，實作 src/skills.js + Game.action/sight/loadFloor/restore。

16 項技能專項及原 323 項回歸通過，npm run build 通過；78 項預快取無缺件。七份 qa/create-3.27-fixtures.mjs 場景，手機／自然可玩性依最新驗證紙條交 Claude／使用者。balance 代理尚不會用技能，不以代理勝率宣稱 Recon 已平衡；本批沒有拓展技能以外的敵人尋路規則。外部頭像與私人紙條未處理。

## 3.26.3：瞄準資訊卡固定三角落

- 使用者回覆上一版已驗證，要求卡片只放右上／右下／左下，保留連線和方框；不遮目標，其他敵人改成盡量不遮。本節取代舊「搜尋全畫面空隙／所有敵人皆硬性排除」規則。
- targetCardPlacement 只產生三個角落候選。目標的精靈、血條、蓄勢標記範圍為硬性排除；可行候選依「遮住其他敵人數量、總遮擋面積、玩家遮擋、HUD 遮擋」排序，再優先原角落防止無謂跳動，初次平手為右上→右下→左下。無任何合格角落或卡片大於畫面則隱藏卡片／連線，戰場目標方框仍保留。
- 底部候選保留 44px 給縮放／復位／地圖按鈕。既有卡片寬度／內容、半透明外觀、連線接卡片最近邊緣、目標方框、瞄準關閉行為不變；連線／卡片不攔截觸控。卡片可以在密集敵群中遮住少量非目標敵人，不能沿用舊測試要求全部避開。
- 323 項測試、npm run build 通過，77 項預快取無缺件。node qa/create-3.26.3-fixtures.mjs 提供 single／crowded 兩份視覺場景；手機角落、縮放、連線與長卡片由使用者／Claude 驗收，未自行跑瀏覽器。save v19／profile v4／backup v1 不變。
- 下一項 Recon「訊號斷層」仍待使用者回覆本輪具體提案：免費啟動、維持 2 次耗回合行動、冷卻 8 次；阻止敵人更新玩家位置但保留最後目擊搜索與已鎖落點攻擊。已發出選項詢問，尚未取得回覆，這批沒有新增技能。使用者先前要求保留技能設計，因此不把本輪籠統「接著做」當作已確定所有技能數值。

## 3.26.2：白線原位斷點、可調透明度與綠色門提示

- 使用者再次調整提示外觀：刪除 3.26.1 的 0.35 格延伸，所有白線端點停在原格邊，仍不跨門口／迷霧封口。
- 設定新增「白線不透明度」0–100%、每次 5%，預設 80%；0 完全透明，100 不透明。即時套用並以 ash-boundary-opacity 本機保存（QA 加 qa-），不耗回合；白線與深色描邊共同淡化，關白線時仍可預設透明度。輸入缺漏／非法回退 80，超界夾至 0–100。任務／profile 備份不含此顯示偏好。
- 可見且未毀的門新增獨立綠色 UI：關閉為沿門身的綠線；開啟為門框兩端各一個綠色像素點。橫直方向一致，在牆美術上方描畫；不受白線開關或不透明度影響。被摧毀／不可見的門不畫綠標，隔板不當成門；原門圖與開關、阻擋、耐久規則不變。
- 321 項測試、npm run build 通過，77 個預快取入口無缺件。沿用五份 3.26 fixture，boundary-gallery 補一扇已開的橫向門，方便比對兩種門標記。手機滑桿與觀感交最新驗證紙條，未自行跑瀏覽器。
- save v19／profile v4／backup v1、撞敵近戰等遊戲規則不變，舊局直接續玩。

## 3.26.1：白線在門口／未知通道保持開口

使用者回饋門與未探索邊界不要折回閉環。本版取代 3.26 的關門橫向白線：不論開關，門都不加白線封口；沿可見牆／障礙物的輪廓末端，遇門口或視野外通道時沿原方向直伸 0.35 格，不橫接或回折。延伸只是方向提示，不讀取後方隱藏地形，也不表示關門可直接通過。可見實牆、存活障礙物與關閉隔板仍畫真實邊界；已知完整障礙物輪廓可正常閉合。

設定仍預設關閉、保留偏好，白線仍為牆美術之上的 UI。戰鬥、門開啟成本及 save v19／profile v4／backup v1 不變。318 項測試、build 通過，含四方向延伸／門開關不封口／迷霧不封口／不改存檔；77 個預快取入口無缺件。手機觀感交最新驗證紙條。`node qa/create-3.26-fixtures.mjs` 現產生五份，新增 open-contour-corridor。

## 3.26.0：撞敵近戰與移動邊界提示

- 使用者要求接續開發並插隊設定中的白線提示。本批完成先前討論的撞敵自動近戰；Recon 主動技能仍待定案，未加入。
- 四方向移動撞到可見、相鄰敵人時，依 player.owned 順序取第一把近戰、且相對目前武器切入／切出均為 0 回合的武器。攻擊耗 1 回合、留原地、保留目前槍械與彈匣；沒有合格武器時沿用阻擋提示，不耗回合。關閉門先開門，隔板／牆／障礙物不能隔空近戰；不支援斜向撞擊。
- 撞擊在提交時保存原敵人 ID／位置與近戰 slot，照既有快速／正常／緩速排序執行。原敵人仍相鄰則追蹤它；離開範圍、失去有效射線或死亡則對原位置揮空，仍耗回合、不換目標或補走一步。先被殺／失能則不出拳。近戰打斷連射／等待／移動狀態，不能藉撞擊取得側身。
- strike 接受明確近戰 slot，hitTarget 接受本次武器；傷害、升級、命中、穿透使用近戰武器，不暫時切換玩家裝備，也不誤用原槍的能量加傷／掩體規則。既有揮擊→中彈→死亡→升級播放順序保留。
- 設定「移動邊界白線」預設關閉，以 ash-movement-boundaries 本機偏好保存（QA 加 qa-）。白線為地形通行邊界：牆、存活掩體箱／油桶／家具、關門／隔板；開門或摧毀後消失。只畫當前可見可通行地板的阻擋邊，封閉門以可見邊判定；不標敵人、物資、煙霧／明暗分界或迷霧邊緣，不表示安全區或掩體效率。
- 白線屬戰術 UI，按地面座標在牆美術之上畫細白線與深色描邊，避免被牆頂蓋掉；人物／痕跡／其他戰場效果仍在牆下。只讀渲染、不改地圖、RNG、回合或存檔；不填滿房間、不畫整片網格。
- 選角補列三兵種起始護甲板，Bulwark 顯示 30。Claude 3.25 報告已原樣收至 qa/results/2026-09-10-claude-3.25-class-stats.md：規則與受測 UI 通過，自然 Recon 難度、部分 fixture 觸控、背景／投擲播放未全面驗證，不能當作全部人工驗收完成。
- 315 項自動測試、npm run build 通過；77 個預快取入口無缺件。四份 QA 場景 node qa/create-3.26-fixtures.mjs，瀏覽器／手機驗收交最新紙條。save v19／profile v4／backup v1 不變，舊局可直接使用；白線偏好不隨任務備份跨裝置。外部頭像、私人紙條不納入更新。

## 3.25.0 兵種數值與起始配給

- Soldier 天生射擊命中 +8；Recon 天生射擊迴避 +10、投擲總容量 +2，新局煙霧／EMP 各 2、破片 0，預備煙霧；其他角色破片 2。HP／裝甲沿用 100／0、100／0、Bulwark 200／6（板 30）。醫療各 2、武器與備彈不變。
- 四個命中／迴避欄獨立區分射擊／近戰，近戰初始皆 0；actor.combatModifiers 可為敵我另加有正負的隱藏調整鉤子。預估與實際攻擊一致，最終 10–99%。詳見 [CHARACTERS.md](CHARACTERS.md)。角色資料集中裝備、配給、容量、HP／裝甲與被動。
- 部署／背包／武器比較／永久容量顯示同步，Recon 共用投擲容量 6–9，不增加同時預備欄。主動技能仍無、撞敵近戰留後續，天生數值和條件被動分開。
- save v19／profile v4／backup v1；相容舊局、不重置。舊局只套兵種修正及容量，保存的 HP／資源／預備／RNG 不補發或覆寫；v18 首讀備份原件。SW 新增 actor-stats.js。
- 305 項自動測試與 npm run build 通過，76 個預快取入口無缺件。新增十項數值／實際擲骰／配給／容量／遷移／原件備份測試，以及八份 qa/create-3.25-fixtures.mjs 場景。原命中測試跟隨 +8／+10 更新；隔離照明／體型測試明確抵銷 Soldier +8，沒有取消正式角色加成。代理在需要投破片前免費預備破片，避免 Recon 預設煙霧誤用；不當作人類難度驗收。
- 瀏覽器／手機驗證交使用者或 Claude，Recon 可玩性與舊明暗自然平衡風險仍未結案。外部頭像及私人紙條未處理。往後純討論不編輯或推送，只有授權完成的更新才發布。

## 最新修正：3.24.1 家具正面與散亂彈殼

置物櫃、門禁櫃檯與值勤桌固定使用正面圖，不再隨板塊旋轉圖片；板塊內位置、占格與掩體規則保留。彈殼改成四組散落排列，每組混合水平、垂直與兩種斜向，霰彈殼略粗；沿用既有 variant／rotation，舊痕跡直接套用，不重抽 RNG 或改存檔。 295 項既有測試與 build 通過；手機觀感交外部驗收。

## 最新接手：3.24.0 明暗板塊與雙感知 Recon

- 使用者同意照明／感知方案，指定 Recon 同時取得夜視及紅外線。已實作；僚機仍只在待辦。規格 [LIGHTING.md](LIGHTING.md)。
- 新層三暗房、入口房／房外走廊亮；lighting[y][x] 決定目標暗區 −40 射擊命中，敵我同式、不減傷。地板冷色遮罩在人物／物件下，概覽、HUD、卡片與說明同步。
- night_vision 免暗區懲罰；infrared 穿煙不穿牆門。Recon 兩條、新狙擊手夜視、新封鎖官紅外線；機械本身沒有感知特權。震撼對生物 OR 夜視 OR 紅外線，共用原失能免疫，不重複計算。
- reveal 改各敵人自行觀察與更新 lastKnown；不能因玩家紅外線看到敵人便洩露位置。失去視線按最後目擊搜索，掩體移動視線預估帶 actor 被動。既有承諾扣彈與狙擊落點保留。
- save v18；v1–17 當層全亮保留內容與 RNG、下層才明暗；舊 Recon 補兩條且容量增到 68。profile v4／backup v1，v17 本機原件備份，SW 增 lighting.js。
- 自動測試 295 項通過。十份 QA 場景：node qa/create-3.24-fixtures.mjs。瀏覽器與手機不自行測，交最新驗證紙條。
- 平衡風險必須保留：12 種子代理 Recon 3 勝、Soldier 0 勝。playthrough 測試已明確改為兩角色合法結束／Recon 至少兩勝，不是原 Soldier 勝率保持。tools/balance 支援角色、暗區等待瞄準、投擲容量滿時不往返撿彈。真實難度／耗彈交使用者驗證，不用人工 HP 999 場景當平衡證據。
- 外部頭像與私人紙條未碰；既有未驗收項目仍保留。


## 最新討論：僚機待辦（2026-09-10）

使用者要求先記錄追隨／放置兩型僚機，未授權實作。已同步使用者願望清單與 DESIGN.md「僚機待辦」：追隨有套索、不主動追擊；放置不隨探索／換層自動帶走，要互動回收；共通提供視線、低命中、一次一個。道具或技能、角色歸屬、成本／耐久／結算、部署名額與跨層狀態未定，Codex 建議另標未確認。遊戲仍為 3.23.0，未變動程式或存檔；本次為純文件更新。


## 最新接手：3.23.0 三段角度掩體

驗證：281 項自動測試與 npm run build 通過；瀏覽器／手機交外部驗收。

- 使用者批准以 45°／騎士方向 atan(2) 為分界：<45° 完整、45° 起半效、≥63.435° 無效。這節取代下方舊半平面、平行端點與固定地形優先序。詳見 [COVER_RULES.md](COVER_RULES.md)。
- 新增純函式 cover.js，以 dot／cross 比例精確比較，不使用近似角度；所有掩體共用，命中與傷害、敵我一致。多掩體取實際命中懲罰最高者，不疊加且回傳原物件。
- 完整箱／牆命中懲罰 35／42 百分點；半效 18／21。減傷 45%／22.5%；穿透乘在減傷上。掩體損耗仍 ceil(raw*.35)，只扣選中者；半效仍可架槍 +12。no_cover、近戰／爆炸例外、探身視線不改。
- 瞄準卡標「半效」，HUD 可顯示半效掩護，遊戲說明同步。SW 預快取新增 cover.js；save v17／profile v4／backup v1 不變，舊局即時套用角度規則，無需重置。
- 新增 8 項角度／選擇／傷害／保存整合測試與 qa/create-3.23-fixtures.mjs 的 7 份場景。瀏覽器、手機和自然難度交最新驗證紙條；外部頭像與私人紙條保留不提交。


## 最新接手：3.22.3 受傷物件血條／掩體規則清點

- 使用者要求可破壞物件除非受傷不顯示血條。renderer.objectHealth 共用條件 0 < hp < maxHp：掩體箱、六家具、油桶、門／隔板，生成圖／舊圖／程式回退一致；受傷的開門也顯示。角色血條和目標卡耐久文字未改。
- 依 game.protectingCover、combat.wallCover／shotChance、barriers.edgeCover 清點到 [COVER_RULES.md](COVER_RULES.md)，沒有改戰鬥公式或角色被動。
- 要注意：掩體為相對攻擊者的方向判定；門／隔板優先於牆再箱子，只取一個。候選掩體線沒有指定敵人，不保證對所有敵人有效。只有 drone 預設 no_cover，獸類／大型／Bulwark 仍可有掩體。
- 近戰目前不對稱：玩家動力拳固定 99% 且無視掩體；敵近戰固定 97% 不吃射擊命中懲罰，但 damagePlayer 仍可能吃掩體減傷。爆炸免掩體減傷但仍受牆／封閉門的幾何阻隔。本批只記錄，不順便改規則。
- save v17／profile v4／backup v1、原素材與 0.5＋1 格牆／遮擋不變；全套 273 項與 build 通過，真機顯示交驗證紙條。外部頭像與私人紙條保留未追蹤。

## 最新接手：3.22.2 牆遮擋戰場內容

使用者指出人物、戰鬥痕跡及特效浮到牆面上，要求它們被牆壓住。本節取代此前「人物／效果最後畫」的約定。

- renderer 將牆移到戰場最後一層，地板、痕跡、屍體、物件、門、人物、煙／彈道／爆炸、戰場文字與標記均在牆下。HTML HUD／瞄準資訊卡仍是介面。
- drawWall 先畫覆蓋整塊 1.5 格輪廓的不透明底，再依探索霧亮度畫材質，避免低透明度讓人物／痕跡透出。素材審核頁同步牆後畫。
- 每塊立面 0.5＋實心頂板 1 格、色調、人工選材和存檔／戰鬥規則不變。
- 原全畫面測試改驗證痕跡／物件／人物／特效先於牆，另驗證霧中不透明底與 alpha 還原。全套 273 項與 build 通過；真機遮擋和點選觀感交最新紙條。
- 外部頭像與私人紙條仍未納入本批。

## 最新接手：3.22.1 每塊牆固定 0.5＋1 格

使用者確認 3.22 色彩改善，但糾正牆的尺寸：**每塊磚下方立面 0.5 格，上方實心頂板 1 格，總高 1.5 格，統一沒有例外**。此節取代下方「同一格」規格。

- walls.js 統一幾何：牆腳仍是原格底緣；立面 0.5 格，頂板 1 格，向上突出原格 0.5 格。刪除南鄰改高度／省略立面的分支，每塊均畫兩張；相鄰只影響外緣線。
- 按 y 排序的正常遮擋仍可蓋住後方磚局部，不能把它誤認為某些磚改成一格高。探索不觸發頂板透明；原霧亮度保留。地面內容和人物最後畫，點選／掩體／視線不變。
- 色調、原 PNG、人工選材清單、角色與地圖規則全部沿用，save v17 不變。審核頁共用 drawWall，比例同步。
- 更新原測試：16 種鄰接都確實畫 16px 立面＋32px 頂板（32px 地磚），透明度不隨探索變；全套 273 項與 build 通過，手機觀感交最新紙條。
- 文件／願望清單更新。外部頭像和私人紙條不納入提交。

## 最新接手：3.22.0 地圖可讀性、半格牆與手工選材

使用者肉眼否定 3.21 高牆；大致採納 Claude 可讀性 F 組。原報告已從 Claude worktree 逐位元複製到 qa/results/2026-09-10-claude-map-readability.md，未改其工作目錄。詳見 [MATERIALS.md](MATERIALS.md)、[WALLS.md](WALLS.md)。

- 牆收回同一地格：南向暴露面下半格立面、上半格實心頂板；其他牆完整頂板。取消 .12 透明剖面與跨格抬升，四鄰接、霧亮度、地板／物件／人物順序保留。格間門／隔板規則與高度不變。
- art-tone.js 首次使用生成 32px 快取 Canvas，按 floor／prop／wall／unit 色調處理，不依賴逐幀 ctx.filter、不覆寫 PNG。採 F 組，地板另先平均 luma 正規到 52.4 消除衛浴突亮；活體單位 1.3 亮度／1.5 彩度＋8px 暗光暈，屍體不增亮。
- material-review.html 獨立審核頁，20 張 T01–T04／W01–W16，三用途可交換／停用／多用，原圖與用途調色、共用 drawWall 組合預覽、匯入下載 selection.json。只記憶體草稿，不碰任務儲存、不自動寫檔或推送；刷新前要下載。
- art/materials/selection.json 為人工收件位置；npm run materials 檢查並生成 src/material-selection.js，build 也先編譯。兩檔一起提交。批准是白名單，原主題偏好停用時取清單第一張；不是勾選後保證每局隨機出現所有圖。目前沿用原選材待使用者批准。
- floor resolveSprite 與 walls drawWall 接同一批准清單；跨兩圖集依用途處理，舊 room.wallStyle 名稱仍相容。圖片路徑白名單，空清單／未知 ID／重複會擋建置；不增加存檔欄位，save v17／profile v4／backup v1 不變。
- 本機 server 補上審核頁及三種既有像素子目錄路徑，避免本機預覽載不到圖；不開放任意工作區。審核頁也打包、共同雜湊與預快取。
- 更新原牆測試＋新增 6 項，全套 273 項與 build 通過；73 個預快取資源齊全，共同模組雜湊 b9492dd9c35a。沿用 3.20／3.21 fixture；沒有自行做瀏覽器／真機 QA，頁面操作、材質階層和手機效能交最新紙條。不能把 Claude 3.21 的 session 實驗當成本批驗收。
- 外部頭像與私人紙條仍保留未追蹤，未納入提交。遊戲規則／地圖生成／RNG／進度不改。

## 最新接手：3.21.0 高牆與組合材質

使用者要求生成牆面、至少一格立面高度加頂面、兩組可混搭。詳見 [WALLS.md](WALLS.md)。

- 內建 GPT Image 生成 art/walls-v1/source-atlas.png；提示、32px 預覽、64 搭配預覽皆保留。tools/pixelize_walls.py 最近鄰／共用 32 色 RGB555，輸出 assets/pixel/walls-v1 的 8 立面＋8 頂面及一張 128px atlas；可重建，不覆寫舊圖。
- walls.js 純外觀：立面一格＋抬升一格的頂面，南向暴露面、四鄰接頂面；同房間穩定選材，room.wallStyle 的 face/cap 可獨立覆寫，未知值回預設，不耗 RNG。
- renderer 改三段：地板、高牆、地面內容；人物／效果最後。頂面投影在已探索可走格時乘 .12 透明度，未知格不剖面，不額外顯示敵人。底部多取兩格避免高牆出畫面閃現。四方向／unproject／LOS／掩體／門隔板規則不變。
- 存檔仍 v17、profile v4、backup v1；不需要遷移或重置。格間門隔板不升高、實體牆不可破壞，未加彈孔。
- 新增五項測試，全套 267 項與 build 通過；68 個預快取檔案齊全、模組雜湊一致（a2a788e9a6b1），像素重建逐位元相同；四份 QA fixture 有效，手機與瀏覽器美術驗收交最新紙條，不宣稱實機已通過。3.19／3.20 外部驗收仍待原清單。
- 外部頭像 art/portraits-custom/ 與 custom/*.png/json、私人給Codex紙條繼續保留未追蹤，不混入提交。

## 最新接手：3.20.0 場景像素素材與戰鬥痕跡

使用者授權趁離開時做痕跡與場景 asset 管線；異界關鍵字和增援只記錄。詳見 [TERRAIN.md](TERRAIN.md)、[OTHERWORLD.md](OTHERWORLD.md)。

- 內建 GPT Image 一次生成 4×4 圖集；art/terrain-v1 有來源、提示、預覽。tools/pixelize_terrain.py 重建十六張 32px／共用 32 色／RGB555／二值透明 PNG 與 128px 圖集，結果逐位元一致。正式 assets/pixel/terrain-v1/；不覆寫舊人物圖。
- themes.js 語意角色查圖，生活模組先於房間 visualTheme，未知主題／缺角色回 industrial，載入失敗用舊繪圖。四種地板＋六家具＋掩體箱／桶／終端／補給箱／門板／隔板；家具跟模組旋轉，門板跟邊界軸旋轉。空箱／殘骸／開門框仍用舊繪圖；幾何、HP、命中與掉落未改。
- traces.js 保存七種地面痕跡，每格 3 種／全層 192 筆上限，雜湊樣式不耗 RNG。同格同類更新；受擊血／機械油、實彈殼、爆炸／能源焦痕、碎屑／破壞碎片。只在地板顯示，未新增實體牆壁彈孔。
- 規則事件記錄到 Game.traces，演出快照延到子彈抵達才顯示；不得在 renderer/rAF 重生。爆炸先取原門板阻隔範圍，痕跡不漏；免費準備／無效操作不增痕跡。地面痕跡在提示、物件、屍體與人物下方，按格分組降低繪圖掃描量。
- save v17 接受 v1–v16，舊局 traces=[]，v16 本機首讀保原件；profile v4／backup v1 不變。當層痕跡存檔，下樓清空，未做跨層回程。
- 新增 13 項規則／主題／PNG 測試，全套 262 項測試與 build 通過，66 個離線預快取檔案齊全、模組雜湊一致（bdade215d4dd）。七份 fixture 已建立；圖集已檢視，手機／瀏覽器／長局密度／真實離線交最新驗證紙條。
- 使用者只要求記錄地獄／亞空間／異空間候選詞與傳送／鑽地增援；地獄視覺為特殊傳送，名稱及數值未定。未加正式關鍵字、增援波次或戰鬥規則。
- 外部頭像素材仍延期，工作區 art/portraits-custom/ 和 custom/*.png/json 為外部未提交素材，不要刪除或混入本批。3.19 外部驗收尚待報告，不能把本批規則測試說成其真機／自然難度已過。

## 最新接手：3.19.0 離線入口與任務合約

本節取代下方歷史「離線待修／任務未開工」狀態。使用者要求延後頭像接入，先完成離線與任務多樣化。詳細規則見 [MISSIONS.md](MISSIONS.md)。

- sw.js：只對同源、SW 目錄入口或 index.html 的 GET 導覽，在斷線／HTTP 失敗時從當前版本快取取精確入口，再回退 index.html。保留 JS／CSS 雜湊精確比對，不全域 ignoreSearch、不搜尋舊快取、不把未知路徑回成 HTML。快取停用／寫滿不攔住線上成功回應。實際手機離線仍交 Claude／使用者。
- 新 missions.js：五份可選合約，核心撤離、定點清除 1、獵殺名單 3、機密回收 1、分散檔案 3。全部六層／第 3 層封鎖官不變；後四類第六層依任務解鎖，核心仍在但可不殺。完成目標仍需電梯互動；獎勵沿用，不增加任務點數。
- 目標只在新局最後樓層配置，獨立種子房序、每房一個，檢查可達且不改敵人／掉落／RNG。指定敵人固定 ID，任何死亡原因均計入；回收是獨立 mission.targets，不占容量、不受爆炸、相鄰或腳下互動 1 回合且不能穿關門，沿用速度／失能／承諾順序。
- 頂部原標題換任務按鈕，不新增一列；簡報、地圖、背包、繼續、結果與歷史顯示。青色 D 資料匣／青色菱形指定敵人；遵守探索／可見性，不洩露未知座標。殲滅進度使用演出快照，先子彈、再死亡與進度、最後升級。
- save v16 相容 v1–v15，舊局補 extraction 保留原地圖；v15 本機首讀保留原件。profile v4／backup v1 不變，歷史追加可選 mission。不可在 restore 重抽任務或目標。
- 16 項新增測試，涵蓋 100 個種子 × 五合約配置、回合／保存／備份／遷移／演出與 SW 模擬；全套 249 項自動測試與 build 通過，63 個預快取檔案與全部模組雜湊一致。八份 QA 場景由 node qa/create-3.19-fixtures.mjs 生成；紙條交真機、實際斷網、正常流程與難度。未自行跑瀏覽器 QA。
- 工作期間外部應用已在 assets/pixel/portraits/custom/ 交付 16 組 PNG／JSON，另有 art/portraits-custom/；本批未檢查、未接入、未提交，保留原檔供後續頭像工作，勿刪除。根目錄私人「給Codex的紙條.md」也維持未追蹤。
- 新任務初版先維持原六層資源與敵人量，沒有宣稱 Recon 可玩性已修好。自訂頭像接入、回程、不同長度、主題素材與交戰痕跡未實作。

## 最新接手：Claude 3.18 報告已收到／外部頭像規格交付

- 已讀取 Claude worktree 的 qa/results/2026-09-09-claude-3.18-map-group.md，並逐位元相同歸檔到主工作區 [報告](../qa/results/2026-09-09-claude-3.18-map-group.md)。未修改 Claude worktree、未複製其測試腳本。
- 受測 a9c572d／BUILD 3.18.0／內容雜湊 a5200d5636e5。報告摘要八項通過、一項離線問題，但必須保留範圍：Chromium 合成事件／尺寸模擬，背景與方向值被覆寫；自然平衡未測，哨站家具浮卡未逐一完成。其 233 項既有測試＋18 項額外規則測試通過。
- 【中，待修】sw.js 僅 caches.match(event.request) 精確回退，未曾在新快取中載入的查詢字串入口（?test=1／?v=...）離線可能無回應。已對照現行原始碼確認缺少導覽回退；Claude 證據為快取缺項＋原始碼，未真正斷網。下一次程式修正優先處理，建議只對遊戲入口導覽回退 index.html，保留 JS／CSS 雜湊精確匹配，不全域 ignoreSearch。
- 未驗證項目仍包括真機觸控／長按／網址列、背景暫停、真機方向守衛、真正離線、v14 本機首讀原件、自然六層資源與 Recon 平衡。不能寫成整組全面驗收通過。
- 使用者要求先把頭像規格放目錄，供外部應用製作。已建立 [assets/pixel/portraits/custom/README.md](../assets/pixel/portraits/custom/README.md)：64×64 靜態 PNG、最多 32 色建議 16、穩定小寫檔名、可選同名 JSON、不要烘入 KIA。此目錄就是成品收件位置。
- 自動掃描、圖片驗證、隨機池／SW 接入、自訂缺圖回退都尚未實作；目前仍是 16 張內建白名單。不能告訴使用者放圖後已可被 3.18 選中。本次只文件／報告，遊戲版本與 save v15 不變。
- 最近討論方向：低成本先做指定殲滅／回收任務，原路撤回需保存跨層狀態；房間主題素材介面、戰鬥痕跡與自訂頭像管線皆尚未開工。Recon 仍待配套，不自動擴充到此文件任務。

## 最新接手：3.18.0 地圖整組完成，統一驗收

使用者要求繼續把整組做完再一起驗收。本批完成生活模組與補給站分布，門（3.16）、補給箱（3.17）併入同一份最新紙條。以下歷史的「下一步做生活模組」已由本節取代；不要求先分別驗收兩個舊版本。詳見 [LIVING_MODULES.md](LIVING_MODULES.md)。

- 每層一至兩處 2×2 模板：衛浴 WC（隔離門／隔板＋馬桶／洗手台）、門禁 ACCESS（櫃檯／設備）、哨站 POST（置物櫃／桌）。不同非起始房，支援四向旋轉，逐候選檢查所有可走地板與必要目的地連通，不覆蓋物資／敵人／門邊／通道。
- 六種家具是可破壞 cover，35–75 耐久、方向性命中 −35、原有減傷；破後可通行，無額外獎勵。地面標線與名稱只作辨識，不增加互動。整數矩形繪圖保留六種輪廓，地圖只畫已探索區域。
- 新樓層終端從九座縮為兩座，主路中段一座、最短主路外的探索支路一座；價格／品項／單次使用不變。地圖標出已發現站、已用變暗。箱內／地面資源量與 RNG 不改；可購買機會減少的自然平衡待整組驗收。
- modules.js 無 world 依賴，放置器傳入 reachable／corridors；先完成地形與箱子再篩站／放模組。props 的 module 標記＋cover.style/moduleId 保存模板關聯，barriers 繼續共用門狀態。
- save v15 接受 v1–v14；舊局當前樓層不放模組、不刪原站，下樓才更新；v14 本機首讀原件保留。profile v4／backup v1 不變。家具損壞、空箱、門狀態與站點使用保存。
- 新增九項測試（包含 600 張生成、旋轉／物資／RNG、家具、站點與保存），全套 233 項自動測試與 npm run build 通過。七份整組場景由 qa/create-3.18-fixtures.mjs 產生；根目錄紙條整合門／箱／家具／站／保存／手機與自然資源節奏。
- 本組完成測試、build、main 與 Pages 後停在整組驗收。沒有瀏覽器 QA；不要把之前 Claude 3.15 的通過範圍延伸到本組。下一個功能方向是 Recon 主動技能／可玩性，仍未實作。

## 最新接手：3.17.0 補給容器

使用者回覆「好 做吧」授權補給容器，本節取代下方歷史的未實作狀態。完整規則見 [CONTAINERS.md](CONTAINERS.md)。

- src/containers.js：低矮可走過的補給箱，無耐久／掩體、不可射擊或破壞。六種類別以符號與顏色辨識；既有可破壞掩體箱不同。獎勵房分類補給集中裝箱，其餘同格物資一起包裝，不改 RNG／物資量；軍械、資料片與敵人掉落維持地面。
- props 中 type:container、kind、opened、contents、indestructible:true。右下統一互動開箱花一回合，原地、無等待或移動加成；只落地不自動拾取。優先箱格（站箱時先面向鄰格），再同側可用鄰格、最後玩家腳下；不穿關門、不刪除物資。開後 contents 清空、空箱留下，禁止重開。
- 新圖生成最後 packSupplies；allSupplies 只供守恆測試／開發側資料讀取。兩項舊測試從地面改查裝箱後的完整物資，原數量預期不變。通關 bot 用合法開箱行動取所需補給。
- save v14 接受 v1–v13。舊局當前地面物品不裝箱，下樓才套新生成；v13 本機首讀原件保留。profile v4／backup v1 不變，backup 地圖驗證新增 container，箱內容嚴格驗證。放棄／重置／備份復原回歸通過。
- 224 項自動測試與 npm run build 通過（新增 14 項），包括物資守恆、144 張生成可達、門／敵人占位／危險地格、容量溢出、失能／快速順序、演出快照與保存。建置後提交 main 並確認 Pages。沒有自行做瀏覽器 QA。
- 八份場景由 qa/create-3.17-fixtures.mjs 生成；最新紙條交手機辨識、空箱疊圖、混合互動、離線與自然節奏。3.16 門外部驗證仍待報告。Claude 3.15 的範圍與未測保留歷史紀錄，不擅自補勾。
- 本批結束後建議生活模組／補給站分布，再回到 Recon 主動技能。Recon 可玩性、煙霧自遮蔽文字提示、堅毅／重生等仍未實作／結案。

## 最新接手：3.16.0 格間障礙物、門與隔板

使用者已回覆「好 那接著開發嗎」授權本批；以下歷史節「尚未實作／尚未授權門」均已由此節取代。完整規則見 [BARRIERS.md](BARRIERS.md)。

- 邊界模型 src/barriers.js，隔離門 60／固定隔板 90；兩側仍可站立，同一邊共享 open／hp。關閉時擋通行、視線、射線與爆炸，提供 42 點牆級掩體；開啟／破壞不再提供掩體，門框只是圖像。
- 朝關門移動耗一回合開門並留原地；右下互動亦可開／關，多選共用原選單。動作先轉成指定門的開啟承諾，快速敵人先改門也不會導致意外跨門或反向切換。隔板不能開，可點邊界鎖定／射擊／動力拳破壞。
- LOS／探身／近戰／NPC 尋路／相鄰終端與武器、電梯都接上邊界。封閉隔間不漏視線；暴露的單片邊緣允許探身。煙霧與爆炸預覽共用 areaCells；炸破門的同一次爆炸仍被原門阻隔，後續爆炸才穿缺口。
- 人形與一般機械敵人開門，crawler／brute／bomber／boss 破門，每次占自身一個行動。失去視線按 lastKnown 搜索；已標記狙擊可被關門攔住並損傷門，子彈先到再扣耐久。
- 自然新樓層最多三扇通道門＋嘗試一個 2×2 中性隔間；144 張樣本均連通，139 張有隔間、571 扇門。不改補給量／終端密度，還沒有生活模組／補給容器。
- save v13 接受 v1–v12；舊局當前地圖不插入門，下樓後新生成才有。本機 v12 首讀原件備份；profile v4／backup v1 不變，開關／受損狀態完整保存。邊界位置／ID／種類／耐久皆驗證。
- 210 項自動測試與 npm run build 全部通過；本批新增 19 項邊界測試。通關 bot 修正為可先接近敵人占住的通道，仍只使用合法操作；不能拿自動通關當自然平衡已驗證。自行重建 grid 的歷史測試／QA helpers 同步清 barriers。
- 十份場景由 qa/create-3.16-fixtures.mjs 重建。手機點選細邊、低縮放、HUD、備份／實際離線與自然玩法交根目錄驗證紙條；沒有跑瀏覽器 QA。原有 Claude 3.15 已收到且無阻擋缺陷，報告保留以下紀錄。
- 下一步：先收重要回饋，然後補給容器→生活模組／補給站分布→Recon 技能與可玩性。Recon 回饋未結案，煙霧罩住自己的非彈窗預覽提示仍待做；不擴到此批。

## 最新接手：格間障礙物方向與 Claude 3.15 審核

- 使用者明確底層應為「格子之間的障礙物」，可圍出小隔間；門是可開關的種類，固定隔板共用位置與方向基礎。下一步排程改為格間障礙物基礎＋固定隔板／門，詳見 [BARRIERS.md](BARRIERS.md)。尚未寫遊戲程式；開門回合成本、門框掩體、耐久、敵人開關／破門能力仍待定。
- 已讀取使用者提供的 Claude 3.15 報告，受測 commit 31820aa，線上／本機資源雜湊一致。報告指出九項清單於其測試範圍通過、無缺陷，另有 19 項 Claude 規則測試通過；不可推論真機與自然平衡已驗證。
- 低優先觀察：近距離煙霧會罩住自己，建議在投擲預覽內說明。規則符合設計，不是阻擋問題；尚未修改 UI，不加回使用者已取消的警告彈窗。
- 未測：真機觸控／煙層清晰度、背景暫停、真機方向守衛、3.15 實際離線、本機 v11 首讀原件路徑、自然平衡。使用者先前的 iOS／離線確認保留，但不能代替新版本完整回歸。
- 原件：C:/codex_projects/shooter_roguelike/.claude/worktrees/codex-project-handoff-c04775/qa/results/2026-09-09-claude-3.15.md。逐位元相同副本已存主工作區 [qa/results/2026-09-09-claude-3.15.md](../qa/results/2026-09-09-claude-3.15.md)，便於未來接手；未改 Claude worktree，未複製其測試腳本或覆蓋主 HANDOFF。
- 本輪只整理報告與設計，遊戲仍 3.15.0／save v12。Recon 技能仍保留；目前沒有必須插隊修正的已知缺陷。

## 文件整理：目前順序與提案狀態（2026-09-09）

使用者詢問開發順序並指出願望清單過期。本次將根目錄 `使用者願望清單.txt` 全面整理為目前狀態、建議順序、待討論及已完成，移除相互矛盾的舊狀態與側身 −15 等過時數值。

建議順序：3.15.0 Claude／手機重要問題 → 門 → 補給容器 → 每層 1–2 生活模組與補給站分布 → Recon 主動技能／平衡 → 任務與解鎖擴充。Claude 3.15 報告已收到，詳見上方最新接手節。遊戲仍 3.15.0／save v12，這次只有文件更新。

使用者提出門可移動開啟／互動開關、掩體與破壞；箱內補給互動後落地；廁所、門禁櫃檯、哨站等模組與降低補給站密度。使用者後續已明確格間障礙物方向；開門一回合不移動、箱子一回合、每層兩座補給站仍為 Codex 提案，未定案、未開工。Recon 技能仍按先前要求保留。不要將此建議排程直接當成實作授權。

## 3.15.0：生物／機械、EMP、震撼與煙霧

- 本批使用者只授權三種投擲物與關鍵字；Recon 主動技能延後。詳見 [THROWABLES.md](THROWABLES.md)，下方各歷史節的「尚無煙霧／EMP」已被本節取代。
- biological／mechanical 為可並存的獨立被動。所有玩家含 Bulwark 為生物；drone／warden／boss 機械，其餘生物。初始分類由 traits.bodyKeyword 讀內容 metadata，實際相剋查 activeTrait。現有玩家電漿直擊 +20% 已改用機械關鍵字。
- src/throwables.js：四種 catalog 共用背包預備與容量；frag→grenades，另 smoke／emp／stun。range 5／radius 2，牆阻隔，預備免費、投擲 1 回合。EMP／震撼不扣 HP，不炸物件；震撼會自暈。
- control.disabled 跳過自身行動兩次，boss／warden 一次；中斷蓄勢、等待、移動與連射，恢復後 control.immune 保護兩次自身行動，不刷新／不交替無限控。依速度隊列扣機會，不能用全局回合末 tickTraits 代替；自身暈後按等待恢復，跳過時不扣彈藥或道具。免費預備不解暈，失能時免費換裝／快填被拒絕。
- smoke 保存 {cells,expires}，投擲當輪＋後兩輪有效，回合末到期清除。Game.sight 共用煙霧 LOS，煙中僅相鄰可見；不減傷，已標記狙擊／轟炸仍可打。普通射手失去視線時按 lastKnown 搜索，煙區不是完整感知系統。
- 共用投擲容量 4→5→6→7，沿用 grenade 永久升級；grenadeTotal／receiveGrenade／trimGrenades 必須一起用，不能只檢查 p.grenades。超額按種類留地。各層獎勵房固定一顆 EMP／震撼／煙霧，不新增 RNG，終端可買；起始裝備不變。
- v12 保存控制、目擊、煙區、新庫存，兼容 v1–v11，v11 首讀留原件；profile v4／backup v1 不變。validTraits 上限 66，讓舊 64 個來源能保留後補分類。未知／缺失新版控制值、煙區、庫存拒絕載入。
- 已有 191 項自動測試及 build 通過；新增 16 項包含九種速度組合、自己失能、混合分類、共用容量、煙霧搜索、狙擊與保存。最終小修後全套與建置亦通過。七份場景由 qa/create-3.15-fixtures.mjs 產生；手機與平衡交根目錄紙條，不自行跑瀏覽器 QA。
- Recon「幾乎不可玩」回饋仍待自然遊玩複驗；本批不聲稱解決。未實作堅毅／重生、敵人丟道具或訊號斷層技能。完成 main／Pages 後停手。

最後更新：2026-09-09。先讀本檔，再讀 DESIGN.md 和 RELEASE.md。

## 3.14.0：Bulwark 重裝兵、輕機槍與固定近戰

- 新免費角色 bulwark，生命 200／固定裝甲 6／護甲板 30，四條獨立被動 large、clumsy、slow、heavy_armor。使用者再次確認緩速；不是減少每輪移動格數。普通敵人先結算。
- heavy_armor 直接傷害在固定裝甲後 ×.75 向上取整，再套等待與護甲板；環境／毒繞過。敵我共用，未附帶 no_cover。詳細初版數值和設計見 BULWARK.md。
- WEAPONS 追加 base 6 lmg（18–22×3、射程 7、30 發步槍彈）與 base 7 powerfist（70–90、射程 1、命中 99%、無限、不需裝填、穿甲 .5、無視掩體）。本批起始專用，未加入隨機掉落。
- 動力拳 locked＋integrated，常駐一格，不可拆解／交換／詞條，可 +3。使用者明確要求切進與切出皆免費；weaponSwitchTurns(destination,current)，含經拳換到另一把槍也免費，無次數限制。免費操作不進時間或清狀態；真正揮拳才耗回合。
- strike 保留原目標 ID 承諾；敵人先逃開則揮空且耗回合，不打替代目標、不生出彈藥、不增加 stats.shots，揮拳中斷連射。近戰固定 99%，不套射擊專用加減成；曼哈頓 1。LMG 彈道／拳擊／音效與子彈→倒下順序已接入。
- 追加拾取限定 thunder 雷鳴爆彈槍 base 8：24–30×3、射程 6、9 發榴彈，每次命中各自半徑 1 爆炸。第 3 層起軍械額外 20%、鐵殼破壞者 10%／封鎖官 15% 額外掉落，不在起始套裝或商店；原保證軍械／電漿保留。射手被首發炸死即停止餘發。
- 自動通關 bot 補給策略改為 min(兩匣,攜帶容量)，避免雷鳴彈匣 9 而備彈上限 12 時追逐不存在的 18 發目標；遊戲容量不變。
- 新單局 v11，相容 v1–v10，v10 首次本機讀取留原件。舊實例 ID 不重排；新 Bulwark 檔必須有一把拳，近戰彈匣 0／無詞條，固定武器不得出現在地上。profile v4／backup v1 不變。
- 採用使用者認可的 16 張新像素頭像，保留四舊 ID，另加 portrait-05–16。舊 v1–v9 雜湊固定原四 ID。python tools/pixelize_portrait_atlas.py --install 重建正式池；v1 腳本改輸出 art/portraits/cells-v1，對照圖從原始 v1 來源重建，避免被新版污染。
- 重要未解問題：使用者實測 Recon 幾乎不可玩，需要主動技能或道具配合，已記願望清單；本批不加配套，不可聲稱 Recon 平衡已修好。頭像喜好已獲肯定，不代表機制驗證通過。
- 175 項自動測試與 npm run build 通過（新增 16 項），五份場景由 qa/create-3.14-fixtures.mjs 重建。手機／選角／近戰比較／自然難度交根目錄驗證紙條；完成 build、main、Pages 後停手。

## 3.13.0 後續美術預覽：頭像第二輪

使用者要求以 4×4 圖集方法重產，改善高解析畫作硬縮造成的碎細節。已由內建 GPT Image 單次產 16 張像素構圖，tools/pixelize_portrait_atlas.py 切成真正 64×64／16 色／RGB555／4-bit 索引 PNG。原圖、提示、編號預覽、舊新對照、候選成品與 manifest 均在 art/portraits-v2。已檢查圖片與重建一致性；只是待評估美術，未替換遊戲四張頭像，BUILD／存檔仍 3.13.0／v10。發布 main 後等使用者意見，不自行採用全部。

## 3.13.0：暴露側身與隨局像素頭像

使用者測試期間追加此批。下方 3.12 的側身 −15 是歷史數值，已由此節與 CHARACTERS.md 取代。

- 側身基礎額外 −20；暴露時以 max(20,42−movePenalty) 補足，移動＋側身至少 −42，等同牆角命中懲罰。敏捷可更強，追獵／笨拙不突破暴露側身下限。敵我共用；一般暴露側身被命中 55%，敏捷 42%，沒有額外減傷。方向／交叉火力／回合期限不變。
- src/portraits.js 頭像池四張，選角畫面每次獨立隨機、同次兩角色不重複，確認後將預覽 ID 傳入 Game 第五參數。player.portrait 隨局保存，不消耗遊戲 RNG。背包／繼續任務／結算顯示，只有 dead 蓋 KIA；撤離／放棄不蓋。
- 單局 v10 接受 v1–v9；舊局由 runId/seed 雜湊補固定頭像，v9 留原件。profile v4／backup v1 不變，新歷史可存 portrait。新未知 ID 拒絕載入，原角色／裝備／回合／RNG 保留。
- 內建 GPT Image 四張日系原圖和提示位於 art/portraits；tools/pixelize_portraits.py 轉 64×64、最多 32 色、RGB555、無抖色。成品 assets/pixel/portraits，共約 9 KB，SW 預快取。來源／manifest／預覽保留，詳見 PORTRAITS.md。
- 使用者建議未來一次產 4×4 頭像圖集再切割，此批四張已生成而沿用。未改地圖精靈，未新增煙霧／EMP／技能。
- 159 項自動測試與 npm run build 通過；新增頭像持久化／舊原件／備份／RNG、KIA 狀態、全部六槍與詞條／大小／敏捷笨拙的側身和牆體對照。手機／瀏覽器不自測，交 qa/create-3.13-fixtures.mjs 四場景與根目錄驗證紙條；發布 main／Pages 後停手。

## 3.12.0：Soldier／Recon 與四個角色被動

使用者已授權實作架槍、著彈修正、側身、快速裝填，並確認 +12／每輪 +8 上限 +24／側身 −15／手槍彈免費裝填數值。歷史「僅記錄」與尚無角色的描述已被本節取代；堅毅／重生仍延期。完整約定見 [CHARACTERS.md](CHARACTERS.md)。

- src/characters.js：Soldier 士兵（步槍＋霰彈，braced／correction）和 Recon 偵察兵（SMG＋霰彈，sidestep／quick_reload），免費，其他起始數值相同。均沿用現有玩家圖像。constructor 第四參數為角色 ID；使用者必須在新局選擇，不在進行中換角。
- 首頁新任務、結果重新部署、設定重新部署皆先進選角；確認才放棄現有任務並建立新局，取消無影響。初始裝備名稱用 WEAPONS 基礎定義，不能從當前任務 slot 猜。角色在背包、設定、結算及歷史顯示，單局匯入會直接採用該任務角色並可繼續，不回到新局選擇。
- 架槍查 protectingCover(attacker,target)，自己方向正確的掩體才 +12，可破壞物也適用；no_cover 取消自身掩體條件。原目標掩體懲罰仍保留。
- fireChain 保存敵人 ID／回合／count 1–3。每次整個射擊完成後記一筆，未命中／失去目標的承諾射擊也算，SMG 同輪兩發不分開累積。免費與無效操作不打斷；耗回合非射擊、射擊不同目標／物件及換層會重置。敵方遠程可獨立掛此規則，executeEnemy 回報實際射擊，enemyAct 統一記錄。
- moveDelta 保存實際移動向量；側身以當前射線的橫向分量 > 縱向分量判定（45 度不算），每個攻擊者分開 −15，與移動／敏捷疊加。免費操作不清除；狀態列「側身 N」僅表示對 N 名目前射程內射手生效。
- actionCost('reload') 按 quick_reload 和 ammoType:pistol 回 0；共用 Game.reload() 轉移備彈，免費分支不走 executePlayer 的清狀態程序。SMG 換裝仍 1 回合。戰場快填標籤、浮卡架槍／修正／敵方側身提示與指南已更新。
- 單局 v9 保存角色／移動方向／連射；v1–v8 補 Soldier 與兩個角色來源，保留原武器、其他被動、預備和資源；不知道舊方向設 [0,0]。v8 首次讀檔留原件。profile v4／備份 v1 不變，新歷史可含 character；舊歷史顯示 Soldier。免費基礎角色不依賴歷史 unlocks.characters 的 operator ID。
- 152 項自動測試與 npm run build 通過，新增 15 項角色測試；原等待基礎測試明確排除 Soldier 連射效果、舊遷移測試改驗士兵來源。四份場景由 node qa/create-3.12-fixtures.mjs 重建，手機／選角／平衡仍交根目錄驗證紙條。
- 完成必要測試、build、main 與 Pages 部署後停手；煙霧、EMP、主動技能和新角色美術不在此批。

## 3.11.0：背包四分頁與獨立預備欄

使用者決定先做共用操作基礎，再接煙霧、EMP、主動技能與角色。這批只實作已授權的預備系統與免費手槍換裝接口；剛記錄的四個被動和堅毅／重生仍延期。完整約定見 [PREPARED.md](PREPARED.md)。

- 背包武器／手榴彈／道具／技能四頁；武器保留比較交換、彈藥、拆解改裝，地面互動直開武器頁。三個預備頁免費選取或取消，保持背包開啟。角色被動列在技能頁作說明，不占主動技能欄。
- src/prepared.js：分類與品項 catalog、選擇／保存驗證、weaponSwitchTurns。player.prepared={grenade:'frag',item:'medkit',skill:null}，player.skills=[]。數量沿用 grenades／meds；用完保留選擇，補給後恢復按鈕。煙霧／EMP／技能 catalog 內容尚未新增。
- Game.action('prepare', {category,id}) 與免費武器換裝在驗證後直接更新指定欄位，不建立速度隊列、不清除等待、不進回合／RNG／環境／被動期限。Game.action('usePrepared', {category,target}) 固定已選指令後沿用原速度結算；heal／grenade 相容入口也要求預備，不能繞過。
- Game.actionCost 查目的武器 weaponClass，pistol 為 0，其他為 1；現有武器均非 pistol，SMG 雖用手槍彈仍耗回合。拾取／交換／裝填不變；新免費動作必須另外接執行，不可只回傳 0。這不是實作快速裝填被動。
- 戰場三鈕顯示預備品項／庫存，無選擇或用盡停用；H 使用道具、G 預備投擲物選點，右下確認，原鈕取消。四分頁與預備鈕至少 44px，分頁支援鍵盤左右／Home／End。技能目前為空並停用。
- 單局 v8 支援 v1–v7；舊檔預設破片／醫療包、空技能，其他資料保留；首次載入 v7 留原件備份。v8 的空選擇保持，不回填。profile v4／備份外層 v1 不變，prepared.js 已納入 SW。
- 137 項自動測試及 npm run build 通過；新增 10 項引擎測試，涵蓋免費操作完整狀態守恆、消耗／無預備／空庫存、先手致死、手槍與 SMG 區分、保存遷移／完整備份／QA 原件。測試內暫時註冊技能和模擬手槍類別，不在正式內容發布測試技能／槍。
- node qa/create-3.11-fixtures.mjs 產生三份單局驗證場景；手機 UI／分頁／H 鍵／投擲物取消確認交根目錄驗證紙條，未宣稱外部 QA 完成。發布 main／Pages 後停手。

## 先前方向：先推整體內容，被動構想只記錄

2026-09-09 使用者追加架槍、著彈修正、側身、快速裝填，可能部分玩家專屬；明確要求先不用做。連同堅毅、重生保留在 TRAITS.md「保留構想」與使用者願望清單，優先推遊戲整體進度；後續已授權的預備系統見上方 3.11.0。不要因看到清單就直接實作。此次僅文件更新，遊戲維持 3.10.1，不重跑本機遊戲 QA。

## 3.10.1：射擊承諾必須耗彈

使用者更正：敵人先逃出視線也要扣彈，玩家決策是承諾。現在有效射擊保存目標 ID 與提交時位置；輪到玩家時若原目標已死亡／不可見／超射程，仍向最後確認位置演出落空並按武器扣彈、增加 stats.shots。SMG 兩發（剩一發則一發），不切換目標、不對原位置的其他單位造成傷害；榴彈落空沿用不爆炸規則。仍有效的原目標照常追蹤新位置。玩家先被擊殺則取消尚未執行的射擊，不扣彈。

存檔格式不變。更新原測試並補六槍種／SMG 殘彈／快照／重載一致性測試，總計 127 項；沿用六份 QA fixture，flee-range 改驗霰彈 −1、回合 +1、射擊落空。下方 3.10.0 描述已同步最新規則，避免誤接。手機演出交外部驗證，發布後停手。

## 3.10.0：獨立被動、反向抵銷與速度結算

使用者同意本批實作，射擊必須追蹤原目標身分。規則全文和擴充約定見 [TRAITS.md](TRAITS.md)；不要將大型、無掩體、敏捷或快速重新綁成一個複合效果。

- 新增 src/traits.js：大型／小型、敏捷／笨拙、快速／緩速，以及無法利用掩體。敵我共用，多來源不疊數值；相反同時存在即抵銷但保留來源，有限期效果每輪末扣一次。
- Game.action 改為唯讀驗證 → 承諾回合與原目標 → 固定速度隊列 → executePlayer／enemyAct → 轟炸／環境 → 期限結算。快速 → 普通 → 緩速，同速玩家先行，每單位最多一次；新召喚者下輪加入，換層停止舊隊列。
- 無效指令不讓快速敵人免費行動。承諾後原目標先死亡／逃出視線或射程，回合與彈藥仍消耗，向最後確認位置開火落空，不自動換打其他敵人；目的格被占用也消耗回合。先死亡則取消玩家及後續行動。手榴彈與狙擊手既有蓄勢狙擊仍固定落點。
- 移動／等待防護保留至自己的下次行動，上一輪等待可擋本輪先手；當輪新選等待不能提前生效。全體行動後才處理轟炸與環境，避免緩速多扣狀態。速度隊列本輪固定，途中變速下輪生效。
- 自然敵人先少量套用：無人機 no_cover、鐵殼破壞者 large、第 4 層起裂隙獵犬 fast。玩家共享規則但尚無選角／取得介面；可用 QA fixture。重生、堅毅、狡猾 AI 與角色技能留待後續。
- 浮卡追加獨立被動和相對行動順序，空間不足仍隱藏且不蓋敵人。圖鑑／背包／指南解釋規則。presentation 可記錄先手移動／蓄勢短片段，仍先彈道再扣血倒下、最後開選單，不重跑 RNG。
- 單局 v7，支援 v1–v6 遷移；舊玩家 traits=[]，舊敵人依當前樓層補自然規則，地圖、武器、RNG 與進度保留。本機首次載入 v6 留原件備份。profile v4／完整備份外層 v1 不變，SW 納入 traits.js。
- 126 項自動測試及 npm run build 通過（新增 18 項），六份 fixture 可由 node qa/create-3.10-fixtures.mjs 重建；實際 AI 逃出霰彈射程場景亦已用引擎驗證。手機 UI／演出和自然難度尚未驗證，交根目錄紙條。發布 main／Pages 後停手，勿把外部 QA 當成已通過。

## 3.9.1：瞄準卡微調

距離與武器射程改為獨立兩行，各附格數。卡寬 120 → 104px，左右內距 3 → 2px，極小畫面 2 → 1px；Renderer 使用相同寬度並重新量測完整高度，沿用所有敵人的硬性避讓規則。只改呈現，不改射程、命中或存檔。沿用既有測試與 build，手機可讀性交由使用者確認，發布 main／Pages 後停手。

## 3.9.0：個別彈種升級、放棄與重置

使用者手測指出希望各彈種分開升級，並要求重置方法與放棄 run 按鈕。本批只改這些內容；3.8 的手機／詞條平衡尚未宣稱全部驗證完成。

- 五種備彈與手榴彈各自三階，每種費用 20／40／70 點。選單六個購買鈕，只更新指定彈種，不補彈、不耗回合。
- profile v4：upgrades.carrying 六鍵物件。v3 整組舊升級退費 20／60／130（依階數），六種歸零；保留任務、earned／去重、解鎖與歷史。先存 ash-profile-v3-backup，再一次寫新餘額與版本，重載不再退款。詳細規則見 AMMUNITION.md。
- 單局 v6：carryLevel 也改六鍵物件；支援 v1–v5，本機載入 v5 另留原件。loadGame／單局匯入採 profile 個別等級，縮減容量時原備彈超額留在腳下。武器實例／彈匣／改裝不變。完整備份外層仍 v1，支援 profile v2–v4，預覽列六種等級及舊升級退款後餘額。
- 設定 → 放棄本局，有確認：保留點數與永久進度，history.outcome=abandoned，不算勝場、不推進回合，移除可繼續任務；結果可查看最後戰場或開始新局。重新部署也先記為放棄。
- 設定 → 重置遊戲進度，有確認與下載備份：清空任務、點數、升級、解鎖與歷史，首頁準備新局。兩個操作重用 restoreBackup 的復原 journal；寫入失敗保留原資料，成功才換場景。設定的「還原前備份」可下載並完整還原；再次放棄／重置／還原會覆寫，選單已提醒。偏好與遷移原件不刪，正式與 QA 分離。
- 108 項自動測試與 npm run build 通過。以 tests/individual-carrying.test.mjs 補上九項測試，涵蓋六種獨立扣款、重複退款防止、舊局超額守恆、備份驗證、放棄去重與重置失敗回退。舊 3.8 fixture／測試已明確使用 v4 的數字 carryLevel。
- `node qa/create-3.9-fixtures.mjs` 產生 800 測試點數和舊整組三階退款兩份 QA 完整備份。只用 ?test=1 → 還原備份，詳見根目錄紙條。完成測試／build、main／Pages 發布後停手，手機 UI 交給使用者或 Claude。

## 3.8.0：武器詞條、實例與比較交換

使用者回報 Claude 的 3.7 清單全過，只有廢料／霰彈同符號及方向偵測防禦性建議；接續已選定的武器詞條優先項。已讀外部報告 `.claude/worktrees/codex-project-handoff-c04775/qa/results/2026-09-09-claude-3.7.md`，未動其 worktree。本批完成 main／Pages 發布後停止，瀏覽器／手機驗證仍交給 Claude／使用者。

- 六種單一詞條：穩定、穿甲、擴容、強擊、長管、追獵，各有數值取捨。新地圖軍械與敵人掉槍約 65% 帶詞條，發射器不抽穿甲；開始武器與遷移既有武器維持無詞條。完整數值與程式約定見 [WEAPONS.md](WEAPONS.md)。
- 使用實例 slot 代替槍種索引：新增 player.weaponBases／affixes，與既有 ammo／upgrades 陣列逐 slot 對應。owned／weapon 改指實例，地面 item.slot 指同一表；item.weapon 仍為槍種。用 game.weaponAt(slot)，不要 WEAPONS[slot]。槽位追加、永不重用，地面與持有不可重複參照。
- 同槍種可占不同背包格，不自動拆解。附近軍械可以比較、選任一持有槍交換；交換只耗一回合，原槍連彈匣與改裝留在腳下。換出裝備槍會直接裝備新槍，換出備用不動當前裝備。原 +1～+3 增傷改裝仍保留，詞條沒有重抽功能。
- 詞條在生成時固定，用獨立 hash，不改掉落率或額外抽戰鬥 RNG。命中公式、物件射擊與浮卡均套用同一武器數值；追獵降低目標移動懲罰。霰彈散射套用穿透，視覺彈道仍由原武器類別決定。
- 單局 v5 支援 v1–v4 原地遷移；v4 五彈種不重新分配。既有彈匣、改裝、地圖、回合、RNG、點數保留。首次 loadGame 留 ash-save-v4-backup 原件，完整備份亦支援實例。profile 與備份外層版本不變。
- 廢料地圖符號 S → ◇；霰彈仍 S。方向守衛過時值退路記入願望清單低優先，本批不改方向規則。
- 99 項自動測試與 npm run build 全數通過。新增 11 項實例／詞條規則測試，更新原來以槍種當實例的測試與回歸 bot；舊 3.2 fixture 產生器也補上實例註冊。`node qa/create-3.8-fixtures.mjs` 產生滿包比較、追獵戰鬥、v4 遷移三個隔離場景。手機 UI 與長期平衡尚未測，步驟見根目錄驗證紙條。

## 3.7.0：五彈種、攜帶上限與永久攜行升級

本批只做使用者選定的第一優先項：彈藥分類與全域攜行升級。武器詞條／實例、角色技能、支線仍留待下一批。完整數值、資料欄位、遷移演算法與補給規則見 [AMMUNITION.md](AMMUNITION.md)。

- 手槍彈供 SMG、步槍彈供突擊／精準步槍、霰彈供霰彈槍，另有能量電池與發射器榴彈。五種備彈與手榴彈各自有上限，彈匣另外計算。背包列出各自數量／上限與武器彈種。
- 基礎容量：手槍 120、步槍 72、霰彈 24、電池 36、榴彈 8、手榴彈 4。每階分別 +30／18／6／9／2／1，三階上限。進場簡介／設定的永久攜行升級依序花 20／40／70 協定點數，立即套用本局與未來新局，不耗回合、不補彈。
- 所有拾取／拆解／終端／換層／補給模組遵守容量。拾取只取部分、剩餘留地；其他來源溢出 dropAmmo 到腳下，換層在新地圖生成後加入補給。滿額終端交易不扣費不耗回合；部分容量時未收下的已購補給落地。
- 地圖彈藥庫含五種備彈，普通房間輪替實彈類別；槍兵／狙擊手掉步槍彈、突擊兵掉手槍彈、哨兵掉霰彈。終端 UI 提供指定彈種，舊 terminal('ammo') 混合補給僅作引擎相容入口。回歸 bot 已改為找適合持有武器的彈藥和終端選項。
- 單局 v4：player.reserve 仍存步槍彈，新增 pistol／shell；carryLevel 為本局套用的容量等級。支援 v1–v3，舊通用彈依持有實彈武器彈匣比例分類，總量保留；超額落地，舊彈匣／地圖／回合／RNG 不重設。loadGame 先保存本機 v3 原件，重載 v4 不重複分類。
- profile v3 新增 upgrades.carrying；purchaseCarrying 以一次 profile 寫入保存費用與等級，拒絕舊等級重複請求、餘額不足與寫入失敗。購買中關頁可由 profile 恢復容量；protocolRuns 不清除，匯入舊局不退還已花點數。
- 完整備份外層仍 v1，支援 profile v2／v3，預覽顯示攜行等級前後值；整份還原含等級。單局匯入以當前 profile 等級為準，不會免費取得永久升級，容量降低時彈藥落地保留。
- 88 項自動測試通過。新增涵蓋全部彈種裝填、部分拾取／溢出、終端拒絕、舊檔守恆、扣款原子性、去重、profile 還原失敗回退與 QA 隔離。最後補強的遷移備份／已花點數測試也通過；未執行瀏覽器或真機驗證。
- `node qa/create-3.7-fixtures.mjs` 可建立容量、200 測試點數完整備份及 v3 遷移場景。只匯入 ?test=1，詳見根目錄驗證紙條。ammunition.js 已加入 SW 快取。完成 main 與 Pages 狀態確認後停手，等資源平衡／手機回饋。

## 3.6.1：點擊敵人重新開啟瞄準

使用者確認關閉瞄準時可保留隱藏鎖定，視野回到與沒有敵人時相同；這已由 3.6.0 的鏡頭忽略隱藏目標實作。本批補上地圖點擊活敵人時自動開啟瞄準，儲存顯示偏好並更新浮卡、鎖定框及取景，不耗回合、不開火。點選油桶／掩體和使用目標輪換鈕維持原行為，手榴彈落點選擇仍優先處理。浮卡空間不足時依舊不遮敵人。

本批執行既有 78 項測試與 build；瀏覽器／手機點選回饋交給使用者或 Claude，見驗證紙條。完成 main／Pages 發布後停手。

## 3.6.0：瞄準開關與短促武器演出

使用者確認上一批其餘手動驗證完成，提出本批三項修改；完成 main／Pages 發布後停手，再討論下一步。

- 浮卡寬度 136 → 120px，水平內距 3px（極小畫面 2px）。`targetCardPlacement` 用玩家、目標及所有可見敵人的圖像／血條／蓄勢標記作硬性禁區，從障礙邊界組合搜尋空位。回傳 null 代表沒有可容納完整卡片的空位，Renderer 暫時隱藏卡與連線；絕不以部分重疊換取顯示。縮放、其他敵人移動或圖集載入後重新計算，資產未載入時也涵蓋程序圖形邊界。
- 快速輪換武器鈕改為「瞄準 開／關」，Q 同步改為開關。此為顯示偏好：關閉浮卡、鎖定框與目標取景，保留目標及直接開火能力；目標輪換／點選仍正常。開關不進引擎行動、不耗回合、不清除等待加成，以 storage 的 ash-targeting 儲存（QA 會加前綴），與音效相同不屬於 profile 備份。
- 關閉瞄準而視野內有活敵人時按鈕每秒柔和閃爍；沒有敵人、重新開啟或任務結束就停止。減少動態偏好改為常亮提示。換槍請從背包指定武器，一次有效換裝仍花 1 回合。
- 每次規則射擊的視覺彈道：步槍 3 條（125ms）、霰彈 6 粒（95ms）、SMG 每個真實射擊 3 條（90ms，因此正常雙發共 6 條）、精準步槍 1 條（110ms）、電漿 1 發（130ms）、榴彈 1 顆（170ms）、手投 1 顆（180ms）。敵方依武器類型套用；近戰維持刀／爪。分散角度及錯開發射只用於演出，不產生額外命中擲骰、傷害或耗彈。
- 一般受擊停留 130ms、SMG 兩次射擊間 40ms、擊殺仍停留 220ms。命中瞬間有短閃光，屍體在 140ms 內輕微落定；不晃動鏡頭。最後才開升級／結算。減少動態保留短提示與順序，取消位移。前一版的 280ms 統一飛行已由本節取代。
- 78 項自動測試通過（加上擁擠卡片硬性避讓、各槍種數量／抵達時序、SMG 殘彈或首發擊殺、敵方與減少動態設定）；相關最終微調重跑 16 項通過。瀏覽器／真機驗證仍交給使用者或 Claude，見根目錄紙條。存檔 v3、戰鬥與防禦規則不變。

## 3.5.0：浮卡精簡、技能預留與戰鬥演出順序

使用者已回報上一批手動驗證完成；依其三項新意見完成本批，未自行重做瀏覽器 QA。

- 目標卡縮至 136px，名稱、HP、命中率、距離、掩護與狀態逐行顯示，縮小行距／內距並移除額外字距。仍在地圖內定位、觸控穿透。
- 補回原防禦欄位，顯示停用的「技能」按鈕，開火恢復單欄。更新 HUD、讀檔、升級後仍不能啟用；實際角色技能未實作，等待的三項效果不變。
- `src/presentation.js`：WeakMap 觀察器在 `captureAction` 期間記錄 `presentStep` 前後快照；每顆玩家子彈、手榴彈、敵方行動、定時爆炸與環境傷害有獨立片段。`Game.action` 仍同步完成，沒有重跑 RNG 或延後規則。快照使用原型和 structuredClone，排除 RNG／effects，不進存檔。
- 時序為飛行 280ms → 套用受擊／屍體／掉落快照 → 停留 220ms → 下一次攻擊；最後才顯示升級或結算。手榴彈新增投擲軌跡，MISS 與傷害數字延至抵達；雙發逐發演出，敵方反擊在其後。連鎖爆炸在同一命中片段結算。
- controller 立即保存真正的完整回合；Renderer/HUD 在演出中讀快照。演出期間阻擋按鈕、鍵盤及地圖指標操作，禁止開選單；完成才回到真正 game 並叫出 pendingPerks/result。重新整理直接恢復已結算回合，點數／擊殺不會再發放。
- Renderer 與 Playback 共用可見畫面時間；隱藏分頁／手機橫向時暫停，回來接續，單幀最多推進 100ms，避免跳過屍體停留。減少動態效果時使用短提示，仍保留先後與停頓。新局／匯入會丟棄舊演出。
- 73 項自動測試通過；新增測試涵蓋抵達前後 HP、擊殺升級、雙發反擊、手榴彈連鎖、未命中、玩家／環境死亡、暫停時鐘、減少動態及與未錄製回合的 RNG／存檔一致性。真機動畫節奏、浮卡可讀性、快速操作仍待外部 QA。
- 可執行 `node qa/create-3.5-fixtures.mjs` 產生四個隔離場景，步驟見根目錄驗證紙條。新增 presentation.js 已納入 SW；存檔維持 v3。完成 main 發布及 Pages 狀態確認後停手。

## 3.4.0：目標浮動卡與等待整合防禦

使用者已決定：所有鎖定資訊直接浮在目標旁；中央等待包含命中、閃避與原防禦減傷，取代獨立防禦。下方 3.3.0 的待討論方案已被本節取代。

- 移除戰場下方 44px 掃描列。名稱、HP、命中率、距離／射程、牆角／箱體／無掩護與移動／蓄勢狀態，改為目標旁的青綠半透明卡，連線指向目標；無目標時隱藏。物件目標也有資料，超射程顯示無法射擊。
- `src/target-card.js` 提供資料與定位：靠邊換側，優先避開玩家／目標，再避開其他敵人及角落 HUD。極小視窗只能選遮擋最少的位置，不保證所有單位都無遮擋。卡片／連線 pointer-events:none，點擊穿透到地圖；不改變回合或鏡頭射程。
- Renderer 隨投影更新位置，靜止時重用布局；controller 更新卡片內容，字型載入後重新量測。兩者都不增加外部 HUD 高度。
- 等待：本次敵人階段直接傷害減半、被槍擊命中 −15，下次有效行動射擊命中 +15（最高 99%）。不疊加、無效動作與查看選單不消耗；下一個有效行動開始時結束減傷，自己的下一顆手榴彈不沿用減傷。毒液／高熱不受保護。
- 獨立防禦鍵已移除，開火鍵擴為雙欄；中央等待、句點與 F 都是同一行動。引擎保留 action('guard') → wait 相容別名，沒有第二套防禦效果。存檔版本仍 v3。
- 曝露致命性回歸測試改用原地反覆換槍，因為等待現在就是防禦；未調弱敵人或調整其命中規則。長期難度與手機浮卡手感交給使用者／Claude，勿把自動檢查當作真機驗證。
- 本批 67 項測試與 build 通過。新增模組已列入 SW 快取；未執行瀏覽器 QA，驗證步驟在根目錄「給驗證者的紙條.md」。

## 3.3.0：完整備份與縱向遊玩

- 使用者已手動驗證 3.2.0 的 iOS 長按放大鏡／選字選單以及飛航模式離線重開；這兩項不再列為 3.2.0 未測。Claude 的 3.2 報告在 `.claude/worktrees/codex-project-handoff-c04775/qa/results/2026-09-09-claude-3.2.md`，九項清單通過，真機／長期平衡限制依報告區分。
- 設定新增完整備份／還原，包含 profile 與目前任務，確認前顯示快照摘要，自動保留還原前備份並提供下載。支援失敗回退與啟動復原，詳見 [PROFILE_BACKUP.md](PROFILE_BACKUP.md)。既有單局匯出／匯入保留，正式／QA 完整備份分開。
- 使用者取消橫向玩法：移除雙欄橫向排版。手機橫向顯示轉回直向提示，不推進回合；桌機採最高 480px 寬的置中縱向欄。manifest 指定 portrait；不依賴瀏覽器強制鎖定旋轉。優先用實體螢幕方向判斷，避免直向軟鍵盤被誤認為橫向。
- `close()` 在 pendingPerks 時重新顯示升級選單，處理 Claude 合成操作發現的返回卡住問題。QA 的 browser/、node/ 臨時腳本加入忽略，報告可提交。
- 掃描／鎖定區與等待／防禦仍在本次討論中：已提出「地圖右上精簡目標卡、點開詳情」及「等待保留射擊 +15、去掉閃避；防禦保留直接傷害減半」，等待使用者選擇。本版尚未實作這兩項，不把它們標成完成。
- 瀏覽器／真機 QA 照舊交給 Claude 或使用者，見根目錄驗證紙條。新備份和方向提示尚未做瀏覽器實測。
- 本批 62 項自動測試與 build 通過；包含完整備份、替換還原、去重、錯誤格式拒絕、寫入失敗回退及方向判斷。

## 3.2.0 本批交付：路線、掉落與跨局貨幣接口

使用者已同意實作路線多樣化、敵人武器掉落、分類補給，並追加可於遊玩獲得的全局貨幣接口。本批完成後推 main、確認部署即停手；瀏覽器／真機 QA 交給 Claude 或使用者。下方 3.1.x 暫停說明為歷史紀錄，不代表本批未獲授權。

- 九房間採隨機生成樹與最多兩條額外連線，入口從四角隨機選，出口依房間圖距離決定；偏移門口、貨架／環形／錯列內牆。保留支路，三種補給房優先放在最短路線之外。必要走道不放阻路掩體或危險格，牆體編輯失敗會回退。
- 每層彈藥庫（實彈 20、電池 12、榴彈 3）、醫療室（1 包）、裝甲庫（20 護甲板），軍械與資料片段跟隨支路配置；探索地圖顯示已發現補給點。沒有加入新武器類型或詞條。
- 持槍敵人機率掉對應武器，封鎖官仍必掉電漿；彈藥依敵人類別掉落，另有少量醫療和重甲敵人的護甲板。背包顯示所有鄰近武器。
- 護甲板容量 30，吸收固定裝甲／防禦結算後傷害的至多一半並消耗，毒液／高熱不受影響；滿額時補給留原地。
- 協定點數：資料片段 +3、完成一層 +4、封鎖官 +8、核心守衛 +12、撤離額外 +16。每次存檔同步全局錢包，死亡保留。首頁／紀錄／結算顯示；尚未開放消費或角色選擇。
- 新 `src/progression.js` 與 [PROGRESSION.md](PROGRESSION.md) 說明穩定解鎖 ID、每局快照、發放去重、profile 遷移。存檔仍 v3，既有地圖不重生成，新房間從下次換層出現。
- 57 項測試及 build 通過（含新 10 項）；未執行本批瀏覽器或真機驗證。接手 [驗證紙條](../給驗證者的紙條.md)，可用 `node qa/create-3.2-fixtures.mjs` 建立隔離存檔，避免為測互動而反覆跑完整關卡。
- 未來候選仍是武器改裝／詞條、可解鎖新武器與角色、煙霧／EMP、支線任務。掃描區搬移與等待／防禦分工繼續待討論。

## Claude 驗證位於獨立 worktree（2026-09-09）

使用者刻意隔離外部 AI 的工作；往後也要查 `.claude/worktrees/*/docs/HANDOFF.md` 和 `.claude/worktrees/*/qa/results/`，不可只查主工作目錄或 origin/main 就判定沒有報告。不要修改對方 worktree。

已讀本機報告：`.claude/worktrees/codex-project-handoff-c04775/qa/results/2026-09-09-claude.md`。原報告及 Claude 修改的文件尚未提交／推送，保留在該 worktree。以下是報告摘要，不是 Codex 重新驗證：

- Claude 重跑 47 項既有測試、build 及另寫的 9 項引擎驗證，皆通過。Chromium 模擬下互動選單版面、手榴彈、防反白、各尺寸排版、屍體顏色及重新整理存檔等檢查通過。
- 無真機；實際觸控、飛航模式離線重開、拾取／下樓、多互動選單與陣亡後縮放仍有未測項目。第 1 層初始佈局無法觸發多互動選單是情境限制，不應直接視為功能缺陷。
- 背景分頁的動畫停擺與合成點擊缺乏使用者手勢造成工具假象，不要據此修正遊戲。手榴彈預設自身落點已由使用者判為誤報，維持現狀。
- 小項目：重新部署按鈕有簡體「开」；角落按鈕 32px 的真機手感待回饋。屍體的「30% 彩度」精確指原色保留權重，不是 HSV 飽和度比例。
- 地圖的起點與電梯位置固定，物品在固定槽位附近變動，可作為下一批地圖／探索多樣化的設計依據。目前僅討論功能，尚未授權開始新功能實作。

## 最新交接：先停手，驗證交給使用者或其他 AI

使用者 2026-09-09 最新指示：降低 Codex 操作成本，驗證工作以紙條交接；確認 GitHub 最新、更新文件後停止。**不要自行展開瀏覽器 QA、重跑遊玩或新增功能。** 驗證步驟與回報格式見根目錄 [給驗證者的紙條.md](../給驗證者的紙條.md)。後續有明確修正要求才恢復開發。

已確認 GitHub main 與本機遊戲程式提交同為 `9c095ae2823fae883d86d53c9ddacd1428ab739b`（3.1.2）。該提交的 Pages 工作流程已成功，上一輪線上確認 BUILD 3.1.2，連點沒有新增 console error。47 項測試及 build 是上一輪結果，本次僅文件交接，不宣稱重新實測手機。

3.1.2 也包含建置時的 JS／CSS／SW URL 內容版本號與離線快取重新驗證，避免新版頁面混用舊模組。正式遊玩可開 `https://darkbearlab.github.io/ash_protocol/?v=3.1.2`；驗證必須另加 `test=1`，保留正式存檔。

根目錄「給Codex的紙條.md」是使用者留下的歷史交接原件，未修改、未納入本次提交；目前版本與發布狀態以本檔及 GitHub 為準。

**永久交付要求：所有完成更新推到 https://github.com/darkbearlab/ash_protocol 並整合到 main，驗證 GitHub Pages 部署。見根目錄 AGENTS.md。**使用者希望持續擴充手機瀏覽器科幻 roguelike；上下左右必須對應螢幕方向，不要改回等角移動。

## 3.1.2 收尾

3.1.1 提交 aaec082 已補推 main 並確認 Pages 成功。線上 QA 找到 selectstart 的 target 可能為 Text 節點，3.1.2 修正 closest 例外。所有遊戲 QA 仍用 ?test=1，沒有改動正式進度。下一步等手機回饋，不擴充延期功能。

## 3.1.1 手機回饋修正（2026-09-09）

- 原下樓鍵統一負責拾取／終端／電梯／投擲確認；同時存在多種互動則開選單選擇。E 同步支援。沒有動態增加的互動提示列。
- 手榴彈按鈕切換至瞄準後成為「取消投擲」，右下互動鍵為「確認投擲」。移除自傷警告列，爆炸仍會自傷。
- 戰術訊息固定單行 28px，超長省略；鎖定區與武器列固定 44px，避免文字換行改變戰場尺寸。
- 使用者允許生命、等級、縮放與復位覆蓋地圖角落：現為半透明小 HUD；復位重設縮放為 1。正方形戰場與角色／目標自動取景保留。
- 停用選字與戰場長按選單；輸入欄仍可編輯／選字。
- 屍體用原生解析度 canvas 預先處理成 30% 彩度、80% 亮度，保留原色；攻擊特效與活人不變。不依賴手機 Canvas filter 支援。
- 320×568 Chromium 實測：手榴彈瞄準前後戰場均 262×262，控制區位置／高度不變，沒有橫向溢出，body user-select:none。
- 掃描資訊區搬移與防禦／等待重設計留待下次討論，見願望清單。完成發布後暫停。

## 3.1 手機優先批次（2026-09-09）

**使用者最新指示：圖集接入並推送 GitHub 後先暫停，保留額度供手機驗證與修正。不要自動開始其他擴充。** 後續以根目錄「使用者願望清單.txt」追蹤。

- 戰場永遠正方形，只有 canvas 位於 viewport；血量、縮放、提示、互動與投擲操作均在外側。窄直向以實測 HUD 高度配置剩餘高度；寬橫向操作區獨立捲動。極小視窗保留至少 128px 戰場，必要時頁面捲動。
- src/camera.js 保持角色置中，四向距離相等；鎖定敵人 / 物件與投擲落點會限制放大倍率，保留 20px 邊界。縮放不影響遊戲真正的視線或射程。
- 等待位於十字鍵中央：本次敵人階段的槍擊命中 −15；下一次有效行動射擊命中 +15，上限 99%。任何有效行動後消耗，不疊加；無效操作 / 開選單不消耗。focus / evasive 是存檔 v3 可選欄位，舊存檔預設 false。
- 開頁先顯示任務簡介，可繼續舊任務；遊戲中沒有標題、標語或側欄。樓層地圖改為獨立選單，只顯示已探索區域。
- 新 aftermath 圖集：10 種倒地外觀（gunner 共用 rifleman）、6 種像素特效；被擊殺的敵人保留在原位置。玩家死亡後「查看最後戰場」可看倒地角色。傷害字改顯示實際結算值。
- CSS 已整理：style.css 為共用 / 對話框，expansion.css 為全部作戰排版；不再疊加多代手機覆寫。
- 47 項測試與 build 通過。Chromium 390×844、320×568（含投擲）、667×375 橫向實測；尚需使用者 iOS/Android 真機回饋。
- 這批沒有擴充隨機武器掉落、詞條、地圖形狀或任務目標。既有每層軍械與封鎖官掉落仍保留。

## 目前可玩內容

- 俯視方格，鍵盤 / 十字方向鈕 / 點鄰格 / 單步滑動，沒有即時敵人移動。
- 六層程序生成設施；第 3 層封鎖官、第 6 層核心守衛、撤離與死亡結局。
- 六把武器、三類備彈、三格武器背包、拾取 / 裝備 / 拆解 / +3 改裝。
- 方向性掩體減傷、可破壞掩體、油桶連鎖爆炸、手榴彈範圍預覽及自傷。
- 十類敵人：新增低耐久、可持續射擊的斷訊槍兵 / 破口突擊兵；原八類保留。
- 牆角探身可見 / 可射對稱、移動和方向性掩護影響命中率；交叉火力增加探索壓力。
- 毒液 / 高熱地板、中毒、防禦、八種升級模組、單次補給終端。
- 每層資料片段、敵人圖鑑、任務歷史、種子重開、JSON 匯出 / 匯入存檔。
- GPT Image 原創圖集轉成真 32px / 4-bit / 16 色 / RGB5 精靈；原圖、提示、轉換程式與驗證全部保留。

## 啟動與測試

1. `npm start`，開啟 `http://localhost:5173`。純 Node、原生 ES modules，無第三方 npm 套件。
2. `npm test`：67 個測試。包含 600 張關卡的實際可行走連通性、戰鬥與物品規則、v1 遷移、跨局點數及六層合法行動模擬。
3. `npm run balance -- 24`：列出 24 個種子的回合、樓層、勝負、非法動作與卡住診斷。
4. **瀏覽器 QA 一律用 `http://localhost:5173/?test=1`**。此模式使用 `qa-` 儲存命名空間，不修改使用者任務。
5. 使用者已確認 2.0 可通關。不要假設正式存檔還停在舊紀錄位置；只用 QA 命名空間測試。
6. `npm run build` 產生 dist/，與 GitHub Pages `/ash_protocol/` 子路徑相容；push main 後自動部署。

## 主要檔案

| 檔案 | 責任 |
| --- | --- |
| src/data.js | 武器 / 敵人 / 樓層 / 升級 / 資料片段的內容與數值 |
| src/world.js | RNG、DDA 嚴格視線、含內牆 / 射擊位的地圖、連通性 |
| src/combat.js | 敵我共用的牆角探身、牆角掩護、命中率 |
| src/game.js | Game 類別，所有遊戲規則、行動 / AI / 爆炸 / 背包 / 存檔 |
| src/engine.js | 穩定匯出入口，供 UI 與測試共用 |
| src/renderer.js | 正交俯視 Canvas 美術、相機、命中座標、動畫、小地圖 |
| src/render.js | Renderer 穩定入口 |
| src/controller.js | DOM、觸控、鍵盤、背包 / 終端 / 圖鑑 / 設定對話框 |
| src/main.js | 瀏覽器入口 |
| src/storage.js | 本機存檔、QA 隔離、v1 備份、任務歷史 |
| src/progression.js | 全局點數、profile 遷移、跨局發放去重與未來解鎖接口 |
| src/audio.js | 程式合成音效 |
| style.css / expansion.css | 共用與對話框樣式 / 3.1 作戰排版 |
| tools/pixelize.py | 來源圖集轉 32px / 索引色精靈，詳見 PIXEL_ART.md |
| tools/build.mjs | 收集公開客戶端檔案至 dist/，並為 JS／CSS／SW URL 加內容版本號 |
| .github/workflows/pages.yml | main push → tests → build → Pages |
| tools/balance.mjs | 無畫面遊玩機器人；知道地圖輪廓，僅透過合法 action 操作 |
| server.mjs | 5173 靜態伺服器，只允許公開遊戲檔案 |
| sw.js | 列舉需要離線保存的檔案。新增 JS、CSS、SVG 後要更新 |

## 關鍵約定

- +x 右、+y 下。project / unproject 必須互為反函式；移動只接受 Manhattan 距離 1。
- `Game.action(type,arg)` 是回合入口。無效操作不得消耗回合、彈藥或物資。
- 正常有效行動：玩家 → reveal → enemyTurn → environmentTurn → reveal → 死亡判定。
- 樓層切換與成功撤離另有分支；換層補血、補彈並清毒，不讓新房敵人立刻攻擊。
- UI 的瞄準 / 背包 / 圖鑑不推進時間。手榴彈先選落點，再確認。
- `target` 可指向敵人或可破壞物，並非只有 enemies。UI 應透過 `game.targeted` 查找。
- 玩家彈匣為索引陣列，WEAPONS 索引持久化；**不可重排現有武器，只能附加**。
- RNG 狀態跟隨存檔保存，讀檔不得重新抽傷害 / 命中。
- 視線：combatSight 可探身，嚴格 lineOfSight 用於爆炸。不可讓爆炸沿探身路徑繞牆。
- 命中公式 97 + 瞄準15 − 目標移動22 − 箱體35或牆角42 − 等待閃避15，最低10、最高99；敵我共用。
- p.moved 表示目前玩家行動是否移動；enemy.moved 保留至下一次敵人階段。
- reveal 重建 visibleTiles，renderer 使用該集合避免每幀大量重算探身視線；序列化排除該衍生狀態。
- 爆炸會傷害玩家，無視掩體但被牆擋住。油桶先標記摧毀再遞迴，以防無窮連鎖。
- 環境毒 / 火不被一般裝甲或防禦吸收；hazmat 控制環境傷害。
- 有 pendingPerks 或已陣亡 / 勝利時，不能 action。

## 存檔注意

正式鍵 `ash-save`，版本 3，支援 v1/v2 遷移。v1 會保留當前地圖 / 角色 / 擊殺進度，補上新欄位，並將舊第三層 boss 轉為 warden。**舊樓層不強制重新生成**，因此新增危險地形與新武器箱從下一個新生成樓層起出現。

首次遷移前存 `ash-save-v1-backup` 或 `ash-save-v2-backup`；匯入前存 `ash-save-before-import`。本機歷史為 `ash-profile`，最多十次。QA 對所有鍵加 `qa-` 前綴。

不要在未告知使用者的情況下清除存檔或改變武器索引。改 schema 時加版本遷移及測試。

## 已驗證與實際限制

- 47 項自動測試全通過。100 種子 × 6 層，掩體不會擋住出口、敵人或物品。
- v3 最近 12 種子自動遊玩：3 勝 / 9 死 / 0 卡住 / 0 非法動作。**不是玩家勝率**：機器人主要使用步槍、不主動探索軍械，敵人預警反應也很粗略。
- 390×844 和 320×667 手機子路徑實測：點陣素材載入、移動後目標命中率 97%→40%、暴露 / 掩護提示、無橫向溢出、無 console error。
- 專用 100 種子壓力測試：暴露原地換槍 4 回合至少 90% 陣亡；有牆角掩護時至多 10% 陣亡。1500 組射線對稱測試通過。
- v1 真實任務已成功遷移。正式任務不使用 QA 行為測試。
- 沒有手把、雲端同步、音樂、全地圖無障礙導覽或裝備稀有度。精靈目前單一朝下姿勢，尚無四向動畫；場景仍有程式化 2D 元件，並非 3D 模型。

## 未來候選（先等手機回饋）

1. 用真人流程玩第 3–6 層，調整後期難度；考慮偵察 / 標準難度選擇。別拿簡單 bot 勝率直接改全遊戲數值。
2. 手機橫向 / Safari 真機 / 離線實機驗證；目前 primarily Chromium 響應式檢查。
3. 將 controller.js 對話框與輸入拆分；CSS 已於 3.1 整理。
4. 強化導引：入場教學、方向性掩體提示、不同敵人更鮮明的輪廓。
5. 擴展房間形狀、支線事件、稀有武器詞綴，再考慮永久成長。

使用者每輪要求在一次 Plus 五小時額度內完成，最後必須留下文件並推到 GitHub main。先處理可靠發布，再花額度增加次要內容。
