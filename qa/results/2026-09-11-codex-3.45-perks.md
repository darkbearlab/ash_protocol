# 3.45.0 通用升級交接

基底 main 3.44.1／1c1b544。範圍依使用者確認：通用十一項、永久三階、資源無限、60% 一名額已選優先；職業專屬與主選單 UPGRADES 未動。

程式：新增 src/perks.js 集中抽選／適用條件／效果／遷移；data.js 為內容表，game 只接線，controller 顯示階數。可選 characters／eligible，未來新技能需新增效果 handler 及必要保存驗證，不需改抽選。sw 已納入離線模組。新命中／迴避每階 +8；新武器增幅每完整攻擊 +6 分攤 burst，舊 bonus 保留。

保存：v28；profile4／backup1 不變。保存實際 draft ID 與 index，非從目前 level 固定輪替。先前選擇計數保守推回；超過新上限原樣保留，不重給獎勵。未知修改值不推斷。舊狀態、RNG、武器彈匣與資源保持。

驗證：新增12項專項；涵蓋2500種子抽選／加權樣本、連升、非法選項、全滿退路、實際命中、連發傷害、舊局與完整備份、v27 首讀原件。原兩項測試曾直接選未提供 ID，改為先指定合法 fixture 選項，仍保留資源／暫停保障。其他舊檔測試未放寬；遷移只信任已用升級次數容許的推算。

四份 fixture 已生成並通過 Game.restore：three-picks、rank-three、supply-fallback、legacy-v27。後兩者含人工強化資料，不代表自然平衡。

未跑瀏覽器／真手機／自然局；由 Claude／使用者依根目錄驗證紙條檢查卡片和實玩。沒有聲稱本輪通關。測試結果與發布 SHA 請見下方最終紀錄。

最終本機紀錄：npm test 478/478 通過；npm run build 通過；git diff --check 通過。四份 fixture 生成／restore 通過。發布提交以 `git log --grep="randomized capped run perks"` 查詢，Pages 由固定倉庫工具核對該 SHA；部署最終結果另由聊天回報。
