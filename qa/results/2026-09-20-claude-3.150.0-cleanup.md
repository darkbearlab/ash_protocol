# 3.150.0 待辦清理 — 驗證報告（使用者決定，Claude 實作，2026-09-20）

## 使用者決定（回覆 Claude 整理的待辦清單）

- 交給 Claude 判斷：
  - 文字版的研究員尖叫、鎖定補給箱：依 AI 代理的用法改；
  - 核心撤離的任務標記；
  - 紀錄頁的地圖生成世代；
  - 程式整理；
  - 平衡機器人。
- 「升級訊號就多閃一下」。
- 破片彈：「我同意應該等價」。
- 存檔穩定性（兩個分頁、舊備份）：「照你的辦法」。
- 其他決定都記在願望清單與 STORY.md 5.2：
  - 第 1 層敵人數、圖示、畫面觀察先不動；
  - 死靈法師變種移到 Roadmap；
  - About 頁與本機排行榜等核心與表現法確定後再做；
  - 音效慢慢接；
  - 故事方向：終端開頭放碎片、職業來歷併入碎片、演出只靠碎片；
  - 盲開槍的規則：命中和報位一樣 −40，暴露看視線。

## 改動

- **升級訊號**：`signal-glitch.js` 的 `transmission.again`，第一次閃之後約 0.7 秒再閃一次（強度 3、0.3 秒），訊息框也再閃一次。
- **破片手榴彈**（`throwables.js`）：12 廢料一顆（原本 12 廢料兩顆），抵價 4 → 7，和煙霧彈相同。
- **存檔穩定性**：
  - `storage.js` 的 `TAB_ID`、`claimTab`：每個分頁開啟時寫下自己的 ID；
  - controller 聽 `storage` 事件，被後開的分頁接手時停止存檔，跳出提示，按「在這個分頁繼續」會重新載入並接手；
  - `pruneSaveBackups`：寫新的舊版本備份時，只留版本最新的兩份，其他存檔、設定與個人紀錄不動。
- **紀錄頁**：每筆部署紀錄後面加「地圖 v…」（`history` 本來就有記）。
- **文字版**（`tools/text-play.mjs`）：
  - 平民進出視野不算「發現新敵人」，不打斷連續指令；
  - 同一句紀錄連續出現會合併，並註明次數與到第幾回合；
  - 鎖定一般補給箱或保險室鐵門時，直接說明打不壞；TEXT_PLAY.md 原本寫補給箱都能射，已更正；
  - 標頭的任務改用 `missionSummary`（第 1～5 層寫「目標位於第 6 層」），不再顯示 0/1。
- **平衡機器人**：升級排序表加上加掛板架 60、改裝精通 45、游擊 40、沉著 25、裝甲回收 30。
- **程式整理**：
  - 刪掉 44 個沒用到的匯入；
  - 整行都沒用到的兩個保留成只載入模組，因為模組互相引用，移除會改變初始化順序；
  - 第一次掃描把展開運算 `...名字` 誤判成沒用到，刪了 23 個其實有用的匯入，全套測試立刻抓到（模組載入失敗），已全部加回。

## 驗證

- **新增測試**：`tests/save-safety.test.mjs` 加 1 個，舊備份只留最新兩份、其他鍵不動。
- **修改的測試**：
  - `tests/ammunition.test.mjs`：破片彈現在一次一顆，差一顆就滿時不會溢出；
  - `tests/terminal-economy.test.mjs`：破片彈抵價 7。
- **`npm test`**：**1199 通過**。`npm run build` 通過。
- **identity**：
  - 地圖與任務相同；機器人 8 局改變，逐項複製進基準線；
  - 暫存副本只把升級排序改回舊的，就和舊基準線完全相同。
- **瀏覽器**（`?test=1`）：
  - 開兩個分頁：先開的分頁跳出「遊戲在另一個分頁開啟了……這個分頁已停止存檔」，按鈕重新載入後接手；
  - 用頁面腳本按移動、開火共六次，回合 6 → 10，主控台沒有遊戲錯誤；
  - 預覽窗格在背景，畫面迴圈依設計暫停，這次沒看到迴圈持續更新；
  - 文字版開新局，標頭顯示「任務：核心撤離 · 目標位於第 6 層」。

## 更正：3.149.0 的實機檢查

- 3.149.0 報告寫的「實際進遊戲走動、開火五回合，主控台沒有新的錯誤」，很可能跑在舊程式上：
  - 開發伺服器在推送前幾分鐘（16:19 UTC）被 app 停掉；
  - 之後頁面由 service worker 的快取提供，版本是 3.148.1，沒有新的干擾程式。
- 這次重新啟動伺服器、清掉 service worker 與快取，確認載入的是新程式（controller 含干擾與分頁鎖）後再檢查，結果如上：操作沒有錯誤，但窗格在背景，沒看到畫面迴圈。
- 3.149.0 的離屏畫面檢查不受影響：那時伺服器還在，動態載入的是新模組。

## 部署

- 使用者：「你先看有沒有問題，沒有就做，然後和 150 一起推上去」。推送 `bc85864..2bbf7e2`，3.150.0 與 3.151.0 一起推。
- GitHub Pages 工作流程 35465908308：completed／success，19:57:21 → 20:01:20（UTC），共 3 分 59 秒。
- 線上確認：`src/version.js` 回報 `3.151.0`。
