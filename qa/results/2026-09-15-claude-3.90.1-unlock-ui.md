# 3.90.1 解鎖介面與 3.90.0 複查（Claude，2026-09-15）

## 範圍

- 獨立驗證 Codex 的 3.90.0（Codex 無法提交，工作樹原樣帶進 Claude 工作樹）。
- 3.90.0 複查：子代理人逐項對照 docs/UNLOCKS.md 讀完整差異，之後修正。
- 3.90.1 介面：解鎖頁、鎖定樣式、屍體繪製與回收、故事結算、故事內容檔說明。

## 3.90.0 獨立驗證（Codex 工作樹，未修改）

- `npm test`：865/865。
- `node qa/enemy-data-identity.mjs`：identical to baseline（generation 780／missions 110／bots 24）。基準差異只有 20 份 endless 任務樣本，和 Codex 報告及 diff JSON 一致。
- 繪製紀錄：敵人 `1f415fe7fcda0dac`（2529 次）；12 層設施 `c9ea651162aadaa7`（115,347 次），和 3.89 相同。
- `npm run build`：通過。

## 複查結果

| 項目 | 嚴重度 | 處理 |
| --- | --- | --- |
| 已鎖回職業，卻沒有購買頁，屍體也看不到、碰不到 | 高（3.90.0 不能單獨上線） | 3.90.1 補上介面，兩版一起推送 |
| 仍開著的 3.89 分頁把玩家檔案寫回 v6，已購解鎖被重設 | 中 | 頂層 `unlockLedger` |
| v3–v6 退款沒有上限：資料不一致時餘額大於累計，備份失敗 | 低（需要資料不一致） | 恢復 `earned − balance` 上限 |
| storage 的 `grantUnlock` 接受 corpse／extraction 來源 | 低 | 公開入口只做購買 |
| 測試迴圈、stories 工具邊角、harness 註解、v44 無盡指定派系 | 低 | 記在 UNLOCKS 第 13 節，留給 Codex |

## 3.90.1 驗證

- `npm test`：869/869（新增 4 項；調整 2 項，理由見 CHANGELOG）。
- 行為一致性基準不變（780／110／24）；兩份繪製紀錄不變；`npm run build` 通過。
- **「走進出口」測試的隨機失敗**：200 個街機種子中有 9 個（4、18、25、29、76、157…）出口西側不能通過，測試原本就會失敗；單獨重跑 8 次都通過。確認是測試擺位，不是出口規則。
- **瀏覽器**（`?test=1`，桌面窗格）：
  - **UNLOCKS 頁**：職業 3/8、起始職業標示；未解鎖卡片灰階，按鈕顯示「還差 929 點」。故事頁 6 段只顯示標題與「戰役 · 任何設施 · 第 N 層」。
  - **購買**：改成 1500 點後按鈕可按 → 確認畫面「花費 1000 協定點數，剩餘 500」→ 解鎖忍者；餘額 500，`unlocks` 與 `unlockLedger` 一致，頁面提示已解鎖。
  - **職業屍體**：在頁面內用 Game 產生無盡第 14 層（忠誠者）的重裝兵屍體，玩家放在相鄰格，以 QA 完整備份還原。
    - 倒地圖與 ID 標籤畫在玩家上方格；右下互動顯示「回收識別資料」。
    - 回收後跳出「解鎖職業：重裝兵」；玩家檔案與 ledger 加入 bulwark，存檔的 `operatorCorpse.recovered` 為 true。
    - 關閉後屍體壓暗、ID 標籤消失，互動鈕停用。
  - **KILL HOUSE**：士兵、偵察兵、工程師、重裝兵可選；死靈法師、德魯伊、狂戰士、忍者停用並標示「未解鎖 · 主選單 UNLOCKS」。
  - **部署第 2 步**（NORMAL · 無盡深入）：同樣四個職業可選；四個未解鎖職業排在後面，灰階、× 記號、沒有頭像、不能選，附「無盡第 8 層起可能發現遺體，回收即解鎖。也可在主選單 UNLOCKS 用 1000 點購買。」
- **沒有在瀏覽器驗**：撤離結算的故事段（有單元測試）、試玩設定、手機實機、3.89 分頁降版的真實情境（有單元測試）。
