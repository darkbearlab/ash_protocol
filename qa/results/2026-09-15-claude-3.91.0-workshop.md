# 3.91.0 工程師工坊第 1 階段（Claude，2026-09-15）

## 範圍

- docs/ENGINEER.md 第 10 節第 1 階段：工坊技能、生產序列、部署上限、定點砲台與追隨無人機、內建機槍改手槍彈、舊存檔轉換。
- 修理、回收與拆解依使用者決定移到第 6 階段，這版沒有。
- Codex 暫停期間由 Claude 實作；規則層變更另請子代理人獨立複查（見下）。

## 驗證

- **npm test**：867/867。
- **行為一致性基準**：
  - 第一次執行有 113 項差異：110 份任務樣本全部改變，原因只是每位玩家多了 `productionLines: []` 欄位；另外 3 筆是工程師機器人。
  - 正規化加上「空的生產序列不列入雜湊」後，只剩 3 筆工程師機器人差異；780 份生成、110 份任務、21 次其他職業機器人完全相同。
  - 只把這 3 筆從候選輸出複製到基準（7 行），沒有用 `--write` 重錄。
- **繪製紀錄**：敵人 `1f415fe7fcda0dac`（2529 次）、12 層設施 `c9ea651162aadaa7`（115,347 次），不變。
- **npm run build**、故事檔檢查：通過。
- **瀏覽器**（`?test=1`，桌面窗格，在頁面內建立工程師新局並以 QA 完整備份還原）：
  - 技能鈕顯示「工坊 序列 1/1 · 部署 0/1」，按下打開工坊：部署中 0/1、廢料 100、序列 1 是追隨無人機。
  - 按「部署」進入選點，身邊 2 步內的格子標示出來，右下改成「確認部署」；確認後回合 1 → 2，追隨無人機在玩家旁邊、彈匣 12，手槍彈 72 → 60，序列空出來。
  - 再開工坊，空的序列按「生產」：藍圖清單列出兩張藍圖（30 廢料、1 回合、內建輕機槍、手槍彈部署時裝填）。
  - 生產定點砲台：回合 → 3，廢料 100 → 70，序列放進定點砲台。
  - 再開工坊：序列裡的定點砲台按鈕停用，寫「部署上限 1 台已滿。」

## 獨立複查（子代理人）

- **沒有發現**：當機、存檔遷移錯誤、非工程師行為改變。實際載入 v45 的收納（含 0 HP）、活動、別層活動、已毀、v26 收納追隨無人機、學會僚機的士兵、有封存樓層的往返存檔，都正常。
- **修正的小問題**：
  - 友軍名額已滿又沒有可清的殘骸或別層機體時，部署通過驗證卻在結算失敗，白白耗一回合。現在驗證時就拒絕。
  - 只給一個座標的部署指令會改用預設格。現在拒絕（介面一律給完整座標）。
  - 升級紀錄固定寫「收納中的機體放進生產序列」，現在依實際轉換內容寫。
  - `qa/drone-waves.mjs` 依賴已取消的回收，標記過時。
  - 存檔版本測試改回寫死 `46`，下次升版仍會提醒更新。
  - 補註解：每層存檔上限 8 台的前提是抵達時只放在玩家 6 步內的空格。
- **保留的說明**：
  - 舊存檔裡活動中的機體彈匣會清空，第一次行動用來換彈（依規格退回彈藥）。
  - 友軍名額已滿時清掉別層機體會寫戰鬥紀錄。
  - 內建輕機槍的武器 ID 仍是 `rifle`，只影響射擊特效與音效的查詢。

## 工程師機器人樣本（tools/balance.mjs，不使用技能）

| 樣本 | 之前 | 之後 |
| --- | --- | --- |
| engineer:1 | 第 1 層陣亡，19 回合，擊殺 5 | 第 1 層陣亡，19 回合，擊殺 6 |
| engineer:2 | 第 1 層陣亡，40 回合，擊殺 9 | 第 1 層陣亡，30 回合，擊殺 7 |
| engineer:3 | 第 1 層陣亡，117 回合，擊殺 20 | 第 1 層陣亡，50 回合，擊殺 12 |

- **原因**：開局不再有收納中的僚機（友軍編號與存檔形狀不同）、備彈從步槍彈移到手槍彈、升級池多了兩項工程師升級。
- **不能當平衡證據**：這個機器人本來就不部署機體，數字只證明變化只影響工程師。

## 沒有驗

- 手機實機操作。
- 自然局裡工坊的強弱與廢料節奏（需要試玩或會用技能的機器人）。
- 多台機體在窄路的換位與讓路壓測。
- 真實的 3.90 工程師存檔升級（單元測試與複查涵蓋收納、已毀、活動三種狀態）。

## 部署

- 3.91.0（`6840553`）推送到 main。
- GitHub Pages：run 34956277008，success。線上 `src/version.js` 為 3.91.0。
- Codex 工作目錄已快轉到 `6840553`。
