# 3.166.1 — 操作指南與設定頁精簡（使用者定稿）

2026-09-24，Claude。規格：docs/TEXT_INVENTORY.md 第十節。

## 需求

使用者在精簡審稿頁（<https://claude.ai/artifact/6Lm6ydzHycnPAVzsAKYiYU>，資料庫 `drafts` 集合）逐項選完 28 項，「已填寫完成 請確認」。

## 讀回的選擇

- 用草稿 17 項，保留原文 2 項，使用者自己改寫 8 項。
- 背包那項沒按選項，備註是「改寫為不限定鍵盤的操作方法」：照備註改成按鈕的說法（背包鈕、武器鈕、長按投擲物／道具／技能鈕），拿掉 B、I、R、H。
- 自動改鎖選用草稿，備註要求「改鎖」寫成「改鎖定」。

## 改動

- `src/controller.js` 的 `showHelp`：改過的各節整段換成語言表的句子；五節合成〈職業〉（職業名稱讀 `CHARACTERS[id].label`，每個職業一列）；刪掉新增戰術、兵種配給、生活區與補給站；新順序見 TEXT_INVENTORY 第十節。
- 設定頁 11 段換成語言表的句子，在訊號干擾之後加一行「減少動態效果」。
- 語言表加 35 句（`manual.*` 23 句、`settings.*` 12 句），共 311 句。句子裡的數字仍讀常數：搬運距離、背包上限、終端額度、繩索距離、鉤鎖距離、伏擊倍率。
- 刪掉的段落用到的 17 個常數從 controller.js 的 import 拿掉。

## 驗證（Claude 自己跑的）

- 完整 `npm test`：1256 通過、0 失敗。`tests/i18n.test.mjs` 的動態代號清單加上 `manual.class.*`。
- 變數衝突檢查：287 個 `t()` 呼叫，沒有衝突。
- `node qa/enemy-data-identity.mjs`：identical to baseline。
- `node qa/text-identity.mjs --compare`：95 局、278,723 條相同（操作指南與設定頁不在這個工具的範圍，改動是刻意的）。
- `npm run build`：通過。
- 瀏覽器（本機 `?test=1`）：操作指南共 25 列加開頭說明，順序與內容和定稿一致；職業各佔一列、狂戰士的鉤鎖距離顯示 5、伏擊倍率顯示 ×1.5；設定頁 18 段文字和定稿一致；主控台沒有錯誤。

## 未驗

- 手機上操作指南與設定頁的實際閱讀感受，交給使用者。
