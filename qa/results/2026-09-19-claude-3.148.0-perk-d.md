# 3.148.0 升級 D、抗失能與敏捷學習資料、紅外線護目鏡 — 驗證報告（使用者決定，Claude 實作，2026-09-19）

## 使用者決定

- 升級 D 的方向（使用者提出）：
  - 精準射擊改成加強等待的命中；
  - 戰術閃避改成加強移動的迴避；
  - 武器增幅改成讓詞條效果增強；
  - 複合裝甲改名，改成加護甲板上限；
  - 生存本能、爆破專家留著。
- Claude 提的數字與名字都同意：沉著每階 +8、游擊每階 +6、改裝精通每階 25%、加掛板架每階 +10。
- 改裝精通：
  - 看過 Claude 的統計後（壞處也放大時，霰彈槍、衝鋒槍打裝甲會掉很多），改成只放大好處；
  - 限定詞條不受影響，長管射程無條件捨去，追獵第 3 階幾乎無視移動目標也可以。
- 「抗失能和敏捷學習資料可以做」；抗毒、密封防護不做。
- 「紅外線目鏡可以加入」。
- 「那就一起做吧，做完直接推。」

## 改動

- **`src/data.js`**：
  - 四張新升級（`rules:[3]`）；四張舊升級標成 `rules:[1,2]`；
  - `PERK_D` 數字；
  - 小隊長 `dropsInfrared`；補給名稱「紅外線護目鏡」；SAVE 68。
- **`src/perks.js`**：
  - `eligiblePerks` 依任務的 `perkRules` 篩選；
  - 改裝精通只在身上有一般詞條的武器時出現（`hasRegularAffix`）；
  - 加掛板架立刻補 10 片，算進新的上限。
- **`src/combat.js`**：
  - 沉著：等待的命中加成 15 ＋ 8×階；
  - 游擊：你移動後，敵人的移動懲罰 22 ＋ 6×階（再加上敏捷等原有修正）。
- **`src/weapons.js`**：`masteredAffix`，只放大一般詞條的好處；命中、射程、追獵無條件捨去；詞條說明後面加上「改裝精通：好處 +N%」。
- **`src/game.js`**：
  - 新任務 `perkRules: 3`，讀檔接受 1～3；
  - `plateCapacity` 加上加掛板架；
  - 小隊長死亡時的紅外線護目鏡固定判定（`lines.js` 的 `infraredDrop`，25%，不動亂數）；
  - 撿起護目鏡。
- **其他**：
  - `src/prepared.js`：佩戴件「紅外線護目鏡」（給紅外線特質）；
  - `src/terminal.js`：不賣，可抵價 24；
  - `src/learning-data.js`：新增抗失能、敏捷，未識別貨櫃內容池 25 → 27；
  - `src/renderer.js`：地上圖示。
- **`tools/balance.mjs`**：平衡機器人連續三次被拒絕的行動會改成等待一回合。
  - 起因：新升級池讓死靈法師種子 3 走到不同的地方，卡在「隔著矮隔板和召喚物換位」，重試 1535 次到行動上限；
  - 舊基準線裡所有機器人的被拒次數都是 0，所以這個改動不影響舊結果（下面的證明）。

## 驗證

- **新增 `tests/perk-d.test.mjs`**，8 個：
  - 新任務只抽得到新的四張、舊任務只抽得到舊的四張；
  - 沉著只在等待後有效；
  - 游擊只在移動後有效，數字在命中上下限內；
  - 改裝精通：出現條件、各詞條第 3 階的數字、壞處不變、限定詞條不變；
  - 加掛板架：上限、立即補板、修復噴劑補到新上限；
  - 兩份學習資料在內容池、學得會；
  - 紅外線護目鏡：掉落、撿起、第二副留在原地、戴上有紅外線、存檔；
  - 存檔保留任務的規則與新升級。
- **修改的測試**：
  - SAVE 68 的釘選；
  - `tests/perks.test.mjs`、`tests/engine.test.mjs` 的舊升級測試明確用舊任務規則；
  - `tests/perk-growth.test.mjs`：新任務是 3，壞值改成 4；
  - 三個內容池數量 25 → 27。
- **`npm test`**：**1194 通過**。`npm run build` 通過。
- **identity**：
  - 差異：generation 371、missions 110、bots 10，逐項複製進基準線；
  - 複製後：`identical to baseline { generation: 780, missions: 110, bots: 24 }`。
- **identity 的證明**（暫存副本）：
  - 副本 X：新任務改回 `perkRules: 2`、內容池還原。結果和舊基準線完全相同，所以機器人的改動、護目鏡掉落、零階時的新規則都不影響舊結果；
  - 副本 Y：X 加回兩份學習資料。只有 generation 371、missions 88 不同，任務差異全部是未識別貨櫃的內容（`learningId`、`type`、`weapon`）；機器人不變；
  - Y 到新版：110 個任務的差異全部只有 `perkRules: 2 → 3`；機器人 10 局不同，來自新的升級池。
- **瀏覽器**：這次沒有另外檢查。改動是規則，加上既有升級卡與武器卡上的文字；畫面沿用原本的元件，內容由上面的測試涵蓋。
- **機器人 10 局的變化**（它們不會等待，也不懂新升級，只能當參考）：

| 局 | 舊 | 新 |
| --- | --- | --- |
| 德魯伊 1 | 第 2 層陣亡 | 第 3 層陣亡 |
| 死靈法師 2 | 第 2 層陣亡 | 第 2 層陣亡 |
| 死靈法師 3 | 第 3 層陣亡 | 通關 |
| 士兵 1 | 第 3 層陣亡 | 第 3 層陣亡（多撐 208 回合） |
| 士兵 3 | 第 3 層陣亡 | 第 5 層陣亡 |
| 重裝兵 1 | 第 3 層陣亡 | 第 3 層陣亡 |
| 重裝兵 2 | 通關 | 第 5 層陣亡 |
| 重裝兵 3 | 第 6 層陣亡 | 通關 |
| 偵察兵 1 | 第 6 層陣亡 | 第 5 層陣亡 |
| 偵察兵 3 | 第 2 層陣亡 | 第 3 層陣亡 |

## 暫定、待實玩

- 四張新升級的每階數字，以及紅外線護目鏡的掉落率 25%，都是 Claude 提、使用者同意的暫定值。

## 部署

- 使用者：「做完直接推」。推送 `0e6145b..47b155e`。
- GitHub Pages 工作流程 35448778038：completed／success，14:27:10 → 14:31:39（UTC），共 4 分 29 秒。
- 線上確認：`src/version.js` 回報 `3.148.0`。

## 3.148.1：地雷與誘餌的落點

- 使用者回報：「地雷和誘餌好像只能選自己腳下，但就會顯示已經有東西，所以沒辦法部署。」
- 原因（3.144.0 起就有）：
  - `controller.js` 的 `move()` 在照明彈、手榴彈模式下移動瞄準框，新的放置模式（`place`）沒有列進去，所以方向鍵與滑動變成讓角色走路；
  - 瞄準框從你腳下開始，而地雷不能埋在有人的格子上；
  - 直接點地圖可以選，所以 3.144.0 的瀏覽器檢查（用點的）沒發現。
- 修正：
  - `move()` 在放置模式下移動瞄準框；
  - 新增 `placeStart`（`field-gear.js`）：起點依序試鎖定的敵人、你面前那格、你旁邊的格子，挑第一個會被接受的；都不行才回到你腳下。
- 驗證：
  - `tests/field-gear.test.mjs` 加 1 個：起點的挑法，以及 `move()` 的原始碼；
  - `npm test` 1195 通過；
  - 瀏覽器（`?test=1`，手機直向）：預備地雷後按道具鍵，瞄準框出現在你面前一格；按方向鍵右，瞄準框右移、角色不動、回合數不變；確認埋設後紀錄是「地雷埋好了。」，回合 +1，地雷剩 2。
- 3.148.1 的部署：和 3.149.0 一起推（`3caa313..e8eef8c`），GitHub Pages 工作流程 35454956710 成功，線上回報 `3.149.0`。
