# 3.67.0 偵察兵延伸點射

使用者核准將遠距單發實驗帶入遊戲，並要求射程詞條順延區間。已完成 Recon 初始被動、舊存檔補齊、結算發數與彈耗、目標卡／背包說明、單發彈道。其餘武器、職業與等待不調整。

## 實作契約

- 標準 SMG：5 格內兩發，6～7 格一發。長管：7 格內兩發，8～9 格一發。超出最遠射程不接受新攻擊。
- 單發傷害維持原值，包括既有每輪強化除以兩發的算法；一發只耗一發彈藥。
- 快速敵人跨越 5／6 格界線，依結算位置變更發數。原目標失去射線仍扣彈，按最後位置開火，不重選可見敵人。
- 不增加存檔欄位，save 32／profile 5／backup 1 不變；舊偵察兵補一次 character:recon 來源。

## 本機實測

- `node --test tests/recon-extended-burst.test.mjs`：4/4 通過。涵蓋武器與職業限定、長管區間、實際行動彈耗與回合、每發傷害、單發演出、快速跨界、失去視線的承諾及不轉火、舊檔與完整備份往返。
- `npm test`：603/603 通過，0 失敗（約 96 秒）。
- `npm run build`：通過。
- `node qa/create-recon-burst-scenes.mjs`：產生三份隔離用單局檔案；詳見驗證紙條。

未執行手機／瀏覽器驗收，也未操作正式存檔。自然遊玩勝率、單發遠射是否足以改善新地圖偵察兵，留待使用者觀察。本批不把自動玩家的先前實驗當成人類勝率。
