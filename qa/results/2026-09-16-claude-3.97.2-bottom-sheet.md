# 3.97.2 選單底部抽屜（Claude，2026-09-16）

## 範圍

- 使用者需求：返回按鈕要釘在手機螢幕下緣，分頁也是，單手才好操作。
- 手機寬度下把選單改成底部抽屜；分頁列移進底部按鈕列；有分頁的選單填滿高度，上緣不動。
- 只改介面（controller 的 `pinFooter`／`modal`、expansion.css），規則與存檔不變。

## 驗證

- **npm test**：901/901。
- **行為一致性基準**：identical to baseline（780／110／24）。
- **繪製紀錄**：敵人 `1f415fe7fcda0dac`（2529 次）、12 層設施 `c9ea651162aadaa7`（115,347 次），不變。
- **瀏覽器**（`?test=1`，種子 4242 工程師開局並部署追隨無人機）：

### 375×812（主畫面捷徑的整個視窗）

| 選單 | 上緣 | 下緣 | 底部列 |
| --- | --- | --- | --- |
| 背包・武器 | 12 | 812 | 682–812 |
| 背包・手榴彈 | 12 | 812 | 682–812 |
| 背包・道具 | 12 | 812 | 682–812 |
| 背包・技能 | 12 | 812 | 682–812 |
| 資料與紀錄 | 12 | 812 | 690–812 |
| 任務簡介（無分頁） | 452 | 812 | 734–812 |

- 四個分頁的上下緣與底部列位置完全相同，切換分頁不再移動。
- 底部列內容依序是「武器・手榴彈・道具・技能」與「返回戰場 →」。
- `checkMenus()` 回報 ok：背包四個分頁溢出 0，其他選單主按鈕都在畫面內。

### 375×635（Safari 含工具列）

- 背包：上緣 12、下緣 635、底部列下緣 635。
- `checkMenus()` ok；戰鬥畫面仍鎖定。

### 過程中的修正

- 第一版只把選單設成填滿高度，底部列用 `position:sticky`。內容短的分頁（道具、技能）底部列停在畫面中間（例如下緣 353），不是螢幕下緣。
- 修正：填滿高度的抽屜改用 `#modal-content` 的 flex 欄加上 `.modal-footer{margin-top:auto}`，內容短也會把底部列推到下緣；重新量測四個分頁都是 682–812。

## 沒有驗

- 真 iPhone：底部列與 Home Indicator 的距離（模擬沒有安全區域），以及單手是否真的好按。
- 橫向強制模式與觸控筆電。

## 部署

- 3.97.2（`c6a4a6d`）推送到 main。
- GitHub Pages：run 35035161570，success。線上 `src/version.js` 為 3.97.2。
- Codex 工作目錄已快轉到部署紀錄提交。
