# 3.78.0 legacy 派系規則（獨立提交一）

基底 06e3f16（3.77.1）。依 FACTION_DATA 第 3–8、10 節；只有 legacy，未新增派系、數值或 UI。

## 驗證

- `node qa/enemy-data-identity.mjs`：identical to baseline，780 張地圖、110 份任務局面、24 次機器人重跑全同。未使用 --write，基準未改。
- 新增 factions.test.mjs 七項通過；完整 npm test 759/759 通過，npm run build 通過。
- 唯一修改的既有測試：real-mode.test.mjs 的隱藏喊話欄位清單新增 faction，由使用者明確批准；沒有其他期望值變更。
- 舊 generateLegacy 的原始輸出不加新欄位；正式 generate／Game.loadFloor 才補派系，保留 mapgen.test.mjs 的 v1 位元組雜湊期望值。此調整後 mapgen＋factions 共 19 項通過。
- save40、profile5、backup1、生成世代10。舊欄位遷移、當層與封存派系驗證、完整備份保留。友軍保留底型產生的 faction，但規則不使用它。

## 架構／介面

出生表與頭目唯一來源在 faction-catalog.js；舊 ENEMY_SPAWNS、FLOOR_INFO.boss、heavyExtra 都為推導檢視。factions.js 供名稱、覆寫與保存查詢。派系權重未填時均勻路徑完全相同，有填時使用同一抽取值，不新增 RNG。

可見與隱藏喊話增加 faction。依使用者分工，派系標籤、聲線、染色與顯示過濾留 Claude；沒有做瀏覽器／真手機 QA。API 詳見 FACTION_DATA 第 13 節。

## 發布

本提交必須在精英規則提交前通過原基準；兩提交依序整合 main，發布結果在第二批報告補記。

2026-09-14 發布受阻：git add 的自動核准審查兩次回覆「Selected model is at capacity」，命令未執行。第一階段已完成測試與 build，但尚未提交、推送或部署。第二階段尚未開始，避免混入第一階段獨立提交。請先按本報告範圍提交 3.78.0，再開始 ELITE_ENEMIES 第 2 節；不要加入既有未追蹤的自訂頭像、Recon 實驗或私人紙條。原身份基準仍未更動。既有測試唯一例外已獲使用者批准：隱藏喊話的精確欄位清單新增 faction。

