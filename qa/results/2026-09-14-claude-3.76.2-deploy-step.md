# 3.76.2 部署難度步驟與真實模式開關 QA（Claude，2026-09-14）

使用者指示：選完任務與行動員之後，多一步選難度，這一格預留給之後的難度旋鈕；另加一個勾選框，勾選後進遊戲就是真實模式。

## 自動測試

`npm test` 734/734 通過，其中新增 tests/deploy-ui.test.mjs 3 項；build 通過。新增測試涵蓋：

- 預設為標準難度、非真實模式；只有布林值 true 才會開啟真實模式。
- 未知的難度 id 會回到標準。
- 難度清單的偏移量都合法；說明文字讀取 AFFIX_TUNING 與 REAL_MODE_TUNING。
- 用這些選項建立的局，realMode 會鎖定，存檔再讀取後仍然保持。

## 瀏覽器檢查

環境：Chromium 內嵌瀏覽器，直向模擬，`?test=1`。

| 檢查 | 結果 |
| --- | --- |
| 部署步驟標號 | 任務為 1 OF 3，行動員為 2 OF 3，難度為 3 OF 3 |
| 行動員步驟的按鈕 | 「下一步：選擇難度 →」 |
| 難度步驟內容 | 摘要列為「NORMAL · 核心撤離 · 工程師 · 種子 隨機」。「標準」已選，註明「詞條自第 7 層」，下方有「難度旋鈕預留」說明。真實模式勾選框顯示為 [ ]，並附「協定點數 +10%」與規則說明 |
| 返回再回來 | 勾選後按返回，行動員仍是剛選的工程師；再進第三步，勾選仍在 |
| 開始任務 | 存檔 qa-ash-save 為 version 39、realMode true、difficultyOffset 0、character engineer |
| 結算（匯入基礎 29 點的真實模式局後放棄） | 標題列為「MISSION ABANDONED / RUN 11 / REAL」；點數列為「真實模式 · 本次協定點數 +29，真實加成 +2 · 累計持有 31」 |
| 最近任務紀錄 | 真實模式那局標有「· 真實模式」，標準局沒有 |

## 未驗

- **DAILY GAME 路徑**：它與一般任務共用同一個行動員步驟與難度步驟，但沒有在瀏覽器實際點過。
- **真手機**：勾選框的點擊範圍與文字換行。
- **勝利與死亡的結算畫面**：只看了放棄；三種結局共用同一段結算程式。
- **目標卡與地圖的數值隱藏**：尚未實作，所以目前勾選真實模式，只有戰鬥紀錄不顯示數字。
