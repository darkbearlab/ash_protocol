# 3.177.12 — 動力拳套

2026-09-25，Claude。規格：docs/GLOSSARY.md。

## 需求

- 術語表審稿時使用者的備註：「Fist 改成 Gauntlet 是不是比較安全？」Claude 建議改成「動力拳套／Power Gauntlet」：Power Fist 跟《戰鎚 40K》與《異塵餘生》的武器同名，Gauntlet 是一般用語，也符合這把武器「裝甲內建」的設定。
- 使用者：「動力拳套 Power Gauntlet 確定要改。」

## 做法

- 語言表：`weapons.powerfist.name` 改成「動力拳套／Power Gauntlet」；`characters.bulwark.text` 與 `manual.class.bulwark` 裡的「動力拳」／power fist 一起改。
- `node tools/glossary.mjs` 更新術語表。術語表審稿的決定到此全部處理完（開發期舊敵人照使用者擱置）。

## 驗證（Claude 自己跑的）

- 語言表裡已經沒有單獨的「動力拳」與 power fist。
- 完整檢查：`npm test` 1311 通過、0 失敗；`npm run build` 成功（`/ash_protocol/` 相容）；英文掃描乾淨；術語表 `--check` 通過。

## 部署

- 推送 `a57e52d..c210668`（`97ff1d1` 3.177.12，另有兩個文件提交）。GitHub Pages 工作流程 36107181363（CI 測試全數通過後部署）：completed／success。線上 `src/version.js` 回報 `3.177.12`。
