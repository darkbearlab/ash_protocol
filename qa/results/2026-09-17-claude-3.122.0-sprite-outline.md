# 3.122.0 角色精靈圖原色加外框（Codex 美術）— 發布與驗證報告（Claude，2026-09-17）

## 依據

- Codex 交接：`docs/SPRITE_OUTLINE_HANDOFF.md`。使用者確認採用「原色加外框」版本，並核准由 Claude 發布。
- 使用者：「等 Codex 調好後一起推」（與 3.121.0 熱鍵一起推送）。

## 核對 Codex 的採用快照

快照資料夾：`art/sprites/adopted/original-outline-2026-09-17/`。

- **雜湊**：`classes.png`、`actors.png`、`dead.png` 的 SHA-256 與 `checksums.json` 一致。
- **來源**：`source/` 三張與目前正式圖集（`assets/pixel/classes-v1/atlas.png`、`assets/pixel/atlas.png`、`assets/pixel/aftermath.png`）逐位元組相同，確認是從現行版本加工。
- **尺寸**：沒有改變（職業 128×128、角色與倒地 128×160）。
- **逐格比對來源與採用檔**（Pillow，逐像素）：

| 圖集 | 改變的格 | 原本不透明的像素被改 | 透明處新增的顏色 |
| --- | --- | --- | --- |
| 職業（classes） | 16 格＝8 職業站姿＋倒地 | 只有第 6 格（狂戰士站姿）222 像素，即核准的 125% 加寬 | 外框色 (12,14,18) 以外的新增像素只出現在狂戰士站姿（加寬後的原色） |
| 角色（actors） | 12 格：索引 0–9（玩家舊圖、槍兵、突擊兵、狙擊手、破壞者、無人機、封鎖官、頭目、獵犬、自爆體）、16 平民、17 噴吐蟲 | 0 | 只有外框色 |
| 倒地（dead） | 同上 12 個倒地格 | 0 | 只有外框色 |

- 物件格（掩體、油桶、醫療、彈藥、手榴彈、終端）與特效格（槍口、子彈、電漿、斬擊、爪擊、衝擊）完全沒動。合計 40 格，和交接文件一致。
- `generator.lua`（Aseprite 批次配方）已讀過。注意它讀的是 `assets/pixel/` 的正式圖集；正式圖換上外框後重跑會重複加框，交接文件與 `docs/PIXEL_ART.md` 都寫明要改指向 `source/`。

## 發布內容

- 三張採用檔複製到正式位置，雜湊與快照相同。路徑不變，`sw.js` 不用加檔；升版會換快取名稱。
- 提交快照資料夾（採用檔、`source/`、`generator.lua`、`checksums.json`）與交接文件（開頭加上發布註記）。
- **不提交**：Codex 本機的試作（`art/palette-preview/`、`assets/pixel/preview-*`、`qa/sprite-palette-preview.html`）與自訂頭像、舊 QA 未追蹤檔。
- `src/renderer.js` 的本機美術試看區塊只存在 Codex 的工作目錄，沒有進這個版本。

## 驗證

- `npm test`：**1022 通過**。職業與角色的單張 PNG 測試讀的是單張檔，沒有跟著換，仍通過。
- identity 基準線：`identical to baseline { generation: 780, missions: 110, bots: 24 }`。
- 精靈繪圖 `1f415fe7fcda0dac`（2529 次呼叫）、地圖繪圖 `2d2abce64a8d8537`（115,347 次呼叫）未變。這兩份紀錄只記繪圖呼叫，不記像素，所以換圖不會改變它們。
- `npm run build` 通過。
- **瀏覽器（375×812，`?test=1`）**：狂戰士新局站在第 1 層中央，戰場上的自己、敵人都帶外框，塗裝色照舊；主控台沒有錯誤。另外順便確認 3.121.0 的放大、縮小、鏡頭復位按鍵：連按兩次 = 時縮放已在上限 1.6，按 - 變 1.45，按 C 回到 1；按 F 等待，回合 1 → 2。

## 部署

- 3.121.0 與 3.122.0 依使用者「等 Codex 調好後一起推」一起推送：`33ad23c..e5395b2`。
- GitHub Pages 工作流程 35238565088：completed／success。
- 線上版本確認：`https://darkbearlab.github.io/ash_protocol/src/version.js` 回報 `3.122.0`；線上 `assets/pixel/atlas.png`、`aftermath.png`、`classes-v1/atlas.png` 與 `src/hotkeys.js` 的 SHA-256 與提交內容相同。
- Codex 的工作目錄依交接文件移除本機美術試看區塊（改動先備份在 Claude 的暫存資料夾）、把與發布內容重複的未追蹤快照暫移後快轉到 `e5395b2`，快轉後的快照與原檔逐一比對相同。
