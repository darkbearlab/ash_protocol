# 蟲族第二步：毒液、鉤舌、感染詞條與蟲潮（規格，2026-09-14）

- **狀態**：3.85.0 已實作蟲潮與疊層中毒；以第9節為準。第6–8節保留前版現況與歷史。
- **分工**：規則與精靈圖交 Codex；台詞、預告與演出、數值交 Claude，在 Codex 之後做。
- **前一步**：3.83.0（Claude）已經用資料做出蟲族派系，見 [FACTION_DATA.md](FACTION_DATA.md) 第 16 節。
- **設計來源**：[FACTIONS.md](FACTIONS.md) 第 4 節（使用者構想）與第 10 節第 13 項（決定）。

## 1. 使用者決定（2026-09-14）

1. **頭目**：先用超大型蟲當頭目（3.83.0 已做資料卡）。**鉤舌也寫給它們用。**
2. **掉落**：非蟲潮的蟲族單位照常掉落資源（現在的彈藥掉落規則本來就涵蓋）。蟲族不掉武器，武器只能靠箱子，先觀察。
3. **被感染者**：除了遊蕩者，也保留基本士兵；射擊發數增加、但命中低很多，因為失去了準頭與扳機紀律（3.83.0 已做資料卡）。
4. **遠程壓力**：被感染士兵連發會造成壓制，再加上毒液射擊與近戰，這樣就足夠。

## 2. 3.83.0 已有的東西（不要重做）

- **派系 `swarm`**：
  - 出生池：獵殺蟲（crawler）、被感染槍兵、被感染突擊兵、孢子自爆體、巨型蟲。
  - 頭目：第 3 層巢穴巨獸 `hive_beast`，第 6 層母巢巨獸 `hive_matriarch`。
  - 雜兵是被感染者（fodder），蟲巢固定用地洞樣式，產物是蟲群幼體（brood）。
- **兵種卡的 `combat` 欄位**：出生時複製成單位的 `combatModifiers`。被感染士兵是 `rangedAccuracy:-35`，連發 3、4 發。
- **`nestStyle(p, faction)`**：派系可以固定蟲巢樣式。
- **`PROTOCOL_REWARDS`**：已有兩個蟲族頭目的協定點數。

## 3. 規則（Codex）

### 3.1 毒液噴吐蟲（新兵種卡）

- **卡片**：
  - 代號暫定 `spitter`，名稱暫定「毒液噴吐蟲」。
  - 遠程、生物、沒有裝甲；生命與射程由 Codex 先給暫定值，Claude 之後調。
- **攻擊**：命中時**不造成傷害，改成讓目標中毒**，靠中毒殺傷。
  - 玩家中毒已經存在（`player.poison`），醫療包能解毒、防護升級能抗毒。這一條要讓它們重新有價值。
  - **中毒的疊加、每回合傷害、持續回合、上限**：由 Codex 定，並寫進報告。
  - **友軍與寵物**：會不會中毒由 Codex 決定。不中毒的話，毒液打在友軍身上要怎麼處理也要寫明。
- **出生**：
  - 加進 `swarm` 的出生池：第 1–2 層不出現，第 3 層起 1 份，第 7 層起再加 1 份。
  - 數量之後由 Claude 調。
- **精靈圖**：站姿與倒地各一張，沿用現有敵人精靈的製程；原有圖格的順序與內容不變。
- **彈道外觀**：規則層只要讓攻擊風格可以辨認為毒液（例如新的 `attackStyle` 或 projectile 代號）；彈道畫法由 Claude 接。

### 3.2 鉤舌（給兩個蟲族頭目）

- **效果**：沿用狂戰士鉤鎖把人拉近的邏輯，把玩家拉到頭目身邊。
- **預告**：
  - 必須先蓄勢一回合，並標出拉扯路線，讓玩家有機會反應。
  - 預告的畫法由 Claude 接；規則層提供事件或狀態欄位即可。
- **打斷**：
  - 蓄勢期間，頭目失能、被壓制到無法行動、或死亡時，鉤舌取消。
  - 要不要加上「受到一定傷害就中斷」由 Codex 提案。
- **限制**：
  - 射程暫定 5 格，需要視線，不能穿牆。
  - 路線上有障礙或單位擋住時失敗或不發動，由 Codex 決定並寫明。
  - 冷卻暫定 4 回合。
- **對象**：第一版只拉玩家。能不能拉友軍由 Codex 決定並寫明。
- **資料化**：
  - 用兵種卡欄位或行為分支宣告「有鉤舌」，不要寫死頭目代號。
  - 巨型蟲要不要也有，試玩後由使用者決定，屆時只需改資料。

### 3.3 被感染者的詞條

- **死亡裂出小蟲**：被感染者（士兵與遊蕩者）死亡時，在身邊放出蟲群幼體。
  - 數量暫定 1–2 隻，受 `RUNTIME_TUNING.expendableLimit` 限制，空間不夠就少放。
  - 裂出的幼蟲不掉落、不給經驗，和現在的蟲巢產物一樣。
- **攻擊帶毒**：命中時除了原本的傷害，再讓目標中毒（沿用 3.1 節的中毒規則）。
- **做法**：
  - 建議做成隱藏詞條，沿用現有的詞條系統（名稱、「？」與顯現）。
  - 只適用於帶「感染」標籤的卡片，建議新增 `infected` 標籤。
  - 由 `swarm` 的 `affixWeights` 或卡片資料決定出現機率。
  - 具體做法由 Codex 提案。
- **小菁英**：蟲族的小菁英抽詞條時，能不能抽到感染詞條，由 Codex 決定並寫明。

### 3.4 蟲潮（研究並提案，可以分批）

- **使用者構想**：真的一大堆小蟲淹沒整個通道。霰彈槍的鄰格濺射會因此變得有用。
- **需求**：
  - 陸續出現，不是一次全部生成；沿用增援預告（「+N」倒數格），確保公平。
  - 不佔每層的威脅額度（參考 [CIVILIANS.md](CIVILIANS.md) 第 4.5 節的做法）。
  - 需要一個蟲潮專用的數量上限，不能吃掉現有的 `liveLimit`。
- **Claude 的前置工作**：
  - 現在每隻敵人移動都是一段獨立演出，30 隻小蟲會依序播 30 段。
  - 蟲潮上線前，Claude 會先把「只有移動」的連續步驟合併成同時播。這一項不動規則。
- **報告要寫明**：觸發條件、每波數量、間隔、上限、與蟲巢的關係。

## 4. 不變條件與驗收

- **基準不變**：`legacy`、`loyalist`、`rebel` 的出生與行為不變，`node qa/enemy-data-identity.mjs` 與基準一致，不重錄。
- **測試**：
  - 毒液命中不造成傷害而是中毒；醫療包能解毒。
  - 鉤舌要先蓄勢、會被打斷，拉扯遵守射程、視線與路線限制。
  - 感染詞條：死亡裂出小蟲受上限限制；攻擊帶毒。
  - 蟲潮的上限與不佔威脅額度。
  - 存讀檔保留所有新增狀態。
- **既有測試**：期望值原則上不改；非改不可時，在報告寫明理由。
- **報告**：寫進 `qa/results/`，並在本檔補「實作現況」一節。

## 5. 不用做（Claude 之後接）

- 毒液噴吐蟲、頭目的叫聲與台詞（蟲類沿用生物的叫聲分類）。
- 鉤舌預告線、毒液彈道、中毒狀態圖示等演出與介面。
- 所有數值調整：生命、傷害、射程、冷卻、中毒強度、蟲潮數量。

## 6. 實作現況（3.84.0，Codex，2026-09-14）

### 6.1 毒液與感染

- `src/swarm-tuning.js` 集中暫定數值：毒液命中增加3回合中毒，上限6；每個付費回合末沿用 `max(0,4-hazmat)` 傷害。持續時間相加，傷害不疊倍數、不隨樓層成長。既有環境地板優先於中毒結算、酸液重設3回合的規則維持，免費行動不結算。醫療包、終端治療與密封防護升級的解毒維持。
- `spitter`：HP34、射程6、裝甲0、1發、直接傷害0、XP1，非雜兵，照常掉資源、不掉武器。蟲族第3層起一份，第7層起多一份。命中按既有遠程命中／掩體／暗區／轉角規則，不打傷掩體；落空不施毒。
- **第一版只有玩家使用中毒狀態**；友軍、寵物、機體被毒液命中時不受傷且不中毒。感染者打中友軍仍有原本的直接傷害。理由是沿用已具醫療包／防護反制的玩家毒素系統，不在本批額外設計寵物與召喚物的治療經濟。這是刻意的攔截優勢，待實戰觀察。
- 新增 `infected` 標籤，給兩種感染士兵及共用的 fodder 卡；**效果還要求派系宣告**。`FACTIONS.swarm.infectedAffixes=['venomous','brood_host']` 固定給兩條隱藏詞條，從第一層起生效。未宣告的 legacy fodder 不取得詞條或改變行為；不更改原五條詞條的抽取池或亂數。其他派系日後可用同一個宣告與標籤。
- 帶毒 `venomous`：每發命中在原傷害外加中毒，施毒時顯現；多發感染士兵最多把時間加到6回合。育蟲 `brood_host`：死亡時顯現並裂出1–2隻幼蟲；包含被感染遊蕩者。使用獨立 `birthRandom(seed,floor,parentId,'infection-burst-v1')`，不抽戰鬥亂數。位置依上右下左，只選可通行、可跨越且沒有單位／物件／物品／危害的相鄰格；不夠則少放或不放。
- 幼蟲與蟲巢共用 `liveLimit=64`、`expendableLimit=6`，`expendable:true`、`reinforcement:true`；沒有經驗、廢料或物資，不帶感染詞條、不遞迴裂蟲。它們仍依既有雜兵規則給玩家追擊代幣。非雜兵感染者本身保持原來的獎勵與彈藥掉落。
- 感染士兵可以成為小菁英，保留兩條感染詞條且計入最低詞條數；非感染卡不能抽感染詞條。感染詞條出生時固定，不依深度另抽機率；之後可由派系資料調整是否提供。

### 6.2 鉤舌與反制

- 卡片 `tongue:true` 決定資格；兩個蟲族頭目已有，巨型蟲未加，之後改資料即可使用。只拉玩家；相鄰時使用原近戰，不用鉤舌。
- 射程5、曼哈頓距離，要求當下視線和可射擊的轉角射線。與狂戰士共用 `pullLanding(g,mover,anchor)`：四向鄰格為落點、依離被拉者距離與原順序取最近，掃過的直線不可穿牆、關門、隔板、實體物件或其他單位，斜線規則仍由既有 lineOfSight(move) 判定。沒有合法落點不蓄勢，繼續原AI。
- 第一次行動保存起點／玩家當時格／落點並預告；下一次行動才拉。玩家只要換格便使本次失敗，不轉向追蹤新格，也不改選其他落點。途中障礙、頭目位移、視線或射程失效均取消；即使玩家還在五格內，也可用換位閃避。
- 頭目死亡、失能、壓制到3層即取消，後者在施加壓制時就打斷，避免回合末衰減後遺漏。普通受傷不打斷；此例外不改狙擊／擲彈預告的原有壓制規則。
- 成功、失敗或打斷後冷卻4個付費回合，免費攻擊不扣冷卻。成功只拉、不立即攻擊；被拉玩家的等待加成、連續射擊與轉角暴露清除，按強制位移處理，不消耗額外玩家行動。玩家自身壓制／下錨不阻止外力拉扯，沒有額外贈送下錨解除。

### 6.3 保存與相容

- save **43**，profile5／backup1不變。新增的敵人欄位均可選：`tongueIntent:{origin:{x,y},target:{x,y},point:{x,y}}`、`tongueCooldown`、`broodReleased:true`、幼蟲的 `broodParent:parentId`。
- 舊檔缺欄位表示未蓄勢、冷卻0、未裂蟲；不重新抽詞條、不補發感染效果、不重生既有樓層或改亂數。新樓層／新出生照新版資料。既有 player.poison 直接沿用，驗證為0–6整數。`validSwarm` 驗證本層與封存樓層的起點／落點／父代／冷卻；普通存讀檔、完整備份與樓層enemies封存自然帶走欄位。回程冷卻依付費機會計算，不因離開樓層流逝而偷扣。
- 遊戲生成世代不新增，這批是蟲族資料與執行期能力；legacy／loyalist／rebel的生成和既有亂數保持不變。正式 enemy-data 基準未重錄。

### 6.4 Claude 介面交接

- 毒液事件：`{type:'enemyShot',attackerType:'spitter',style:'venom',from,to,damage:0,miss?:true}`。`projectile:'venom'`、WEAPON_VISUALS.venom只是識別與單發描述，專用毒液畫法尚待接。玩家剩餘中毒回合讀 `player.poison`；真實模式按既有政策隱藏數字。
- 準備事件：`{type:'tongueTelegraph',sourceId,from:origin,to:target,landing,damage:0}`；持續狀態由 `tongueTelegraphs(g)` 查 `{kind:'tongue',sourceId,origin,target,landing,interruptible:true}`。UI 必須再按玩家可視性過濾；這個純規則查詢不自行過濾。取消會移除intent，線條需隨每一步快照更新。
- 成功事件：`{type:'tonguePull',sourceId,from:玩家原格,to:落點,origin:頭目格,damage:0}`。已在敵人那一步保存前後狀態，沒有直接操作介面。專用線條與拉扯播放交Claude。
- 名稱照舊用 `enemyDisplayName`／`revealedAffixes`，片段「帶毒」「育蟲」，未顯現只顯示問號；新增詞條台詞未接，沒有提前寫入隱藏效果。裂蟲沿用 `nestSpawn/burrow` 演出，畫面不顯示尚未發動的育蟲能力。
- `node qa/create-swarm-scenes.mjs` 產生四份 `qa/fixtures/swarm/*.json`：鉤舌蓄勢、毒液／中毒、感染士兵、自然蟲族第3層。請用 `?test=1` 匯入；別用正式存檔驗死亡。手機體感、台詞、毒液彈道／圖示、鉤舌線未由Codex驗收。
- 新素材在 `assets/pixel/swarm-v1/`；圖集第18格，前17格逐像素未動。完整prompt和製程在 `art/swarm-v1/README.md`。

## 7. 蟲潮原提案（3.84.0歷史；3.85.0已實作，見第9節）

- 建議第3層起，每層一個獨立的入侵點，戰鬥配置完成後以獨立雜湊挑非起點房的可達位置，不吃 allocateThreat 或改戰鬥敵人位置；玩家首次進入6格才啟動。
- 共4波、每波4隻、間隔3個付費回合；每波先以現有增援預告方式標格、倒數2回合再生出。總量16，存活專用上限12；滿額或格子受阻延後，不直接在玩家身上生成。
- 與蟲巢是兩套有限來源：巢穴保留原6隻額度與共用雜兵上限；蟲潮使用 `horde:true` 與獨立上限12，不佔 liveLimit／expendableLimit。實作時需統一修正這兩個容量查詢，避免其他增援因蟲潮被擠掉，並測試兩套來源共存。蟲潮幼體無經驗、廢料、物資且不可再裂蟲。
- 每層狀態存入新的可選 floor-owned `swarmWaves`，須加入 FLOOR_FIELDS／驗證：active、remaining、cooldown、serial、pending預告格／due。是否允許玩家破壞入侵點，留待下一批定案。
- 本批**沒有生成蟲潮，也沒有宣稱通過尚未實作的蟲潮上限測試**；現有感染裂蟲／蟲巢共用容量的測試已做。等Claude合併純移動演出再上線，避免目前逐隻播放讓回合過長。

## 8. Claude 驗證與演出（3.84.1）

### 8.1 驗證 Codex 3.84.0

- **自動檢查**：
  - npm test 804/804。
  - `node qa/enemy-data-identity.mjs` 與基準一致。
  - GitHub Pages run 34863405868 成功，線上版本 3.84.0。
- **畫面**：
  - 原有 12 張卡的繪製呼叫雜湊 `1f415fe7fcda0dac`，沒有變。
  - 兩張圖集前 17 格逐像素相同，第 18 格是噴吐蟲。
  - 噴吐蟲的色調依名稱判斷，是角色色調。
- **程式檢查**：
  - 毒液只作用在玩家身上。
  - 感染詞條必須同時有標籤與派系宣告。
  - 鉤舌只在警戒後判定，會追蹤預告的格子，取消條件與冷卻一致。
- **沒有改的小問題**：多發的被感染士兵每一發命中都寫一行中毒紀錄，同一回合可能連寫好幾行。

### 8.2 演出

- **毒液彈道**：
  - `fx.style==='venom'` 畫成弧線飛行的綠色毒液塊；命中時濺出液滴，落空不濺。
  - 顏色在 `VENOM_VISUAL`（`src/enemy-visuals.js`）。
- **鉤舌預告**：
  - renderer 每一步都讀 `tongueTelegraphs(g)`，只畫 `g.visibleEnemies` 裡的頭目。
  - 從頭目到被鎖定的格子畫粉紅虛線，被鎖定的格子加底色與外框，落點另畫外框。
  - 預告取消時規則層會刪掉 intent，線條跟著消失。
- **預告瞬間**：`tongueTelegraph` 效果讓被鎖定的格子閃一下。
- **拉扯**：`tonguePull` 效果畫從頭目伸出的舌頭，舌尖在 220 毫秒內從玩家原位移到落點。
- **分支順序**：鉤舌與毒液的分支都排在一般彈道分支之前。修正前，這三種效果都會被畫成飛行的子彈。
- **目標卡**：頭目蓄勢時，狀態行顯示「鉤舌蓄勢」（`TONGUE_VISUAL.label`）；真實模式隱藏。
- **叫聲**：噴吐蟲與兩個頭目的卡片聲線是 `creature`，沿用生物叫聲，沒有新增台詞。

### 8.3 尚未處理

- **蟲潮**：Codex 的提案（第 7 節）要等使用者確認，以及 Claude 先把純移動的演出合併成同時播放。
- **數值**：毒液強度、噴吐蟲生命、鉤舌射程與冷卻，都等試玩後再調。

### 8.4 移動演出合併（3.84.2）

- `mergeMoveSteps(steps)`（`src/presentation.js`）把連續、只有移動（可以附帶喊話）的步驟合併成一段，由 `planPresentation` 自動套用。
- **結束合併的情況**：有射擊或其他效果、有單位倒下、玩家移動、換樓層，或同一個單位在這一段裡要走第二步。
- **只動演出**：規則、亂數與存檔都不變；減少動態模式照舊沒有移動動畫。
- **蟲潮可以開始實作**：前置工作已完成。

## 9. 蟲潮與疊層中毒實作（3.85.0，Codex，2026-09-15）

本節取代第6節的「剩餘回合」中毒與第7節的待定內容。使用者確認入侵點不能被破壞。

### 9.1 蟲潮時序與容量

- `FACTIONS.swarm.hordeType='brood'` 宣告來源；`addSwarmWaves` 在所有戰鬥配置、貨櫃、非戰鬥人口處理後追加狀態。第3層起一個入侵點，獨立雜湊 `birthRandom(seed,floor,'invasion','horde-origin-v1')` 選非起點房的可達格；避開敵人、物件、物資、危害、槽位、連接口與出入口附近。沒有動原敵人與物資、不吃威脅預算或地圖／戰鬥亂數。極端自訂圖若沒有合法空格，略過入侵點，不擠掉既有配置或讓生成失敗。
- 入侵點存成 `swarmWaves.origin`，不是props、沒有HP，不擋路、不可被瞄準、爆破、互動或摧毀。這批顯示增援格；入侵點本身的專用地面圖留給後續美術。
- 玩家在付費回合末首次進入曼哈頓6格內啟動，不要求視線。4波×4隻、總量16。每波完整取得4個合法預告格才安排；選原點4格內的可達格，按距離、y、x固定排序，不取亂數。
- 排定當回合為t，預告due=t+2；t+1尚未抵達，t+2敵我佇列與環境結算後出生，新幼蟲從下一個付費回合才行動。兩波的**預告開始**至少隔3個付費回合；上一波有待抵達的單位時不安排下一波。
- 格子被玩家、友軍、敵人、實體物件／物品／危害擋住，或活體達12，原格延後1回合；不換格、不擠開單位、不跳過預告。四隻可部分抵達，剩下的保留同一波。
- 存活專用上限12；`enemyRoom`／`expendableRoom` 都排除horde。蟲巢、感染裂蟲、撤退和頭目增援保持原live64／expendable6限制。測試已驗12隻蟲潮與6隻蟲巢共存。
- 幼蟲 `horde:true,expendable:true,reinforcement:true`，沒有經驗、廢料、物品掉落或感染詞條，不會再裂蟲；沿用一般雜兵的擊殺計數／追擊代幣。出生時警戒、lastKnown為玩家當下位置，沿用蟲巢幼體的行為。沒有新增行動順序或移動演出，3.84.2的純移動合併照常使用。
- `SWARM_TUNING` 數值：hordeMinFloor3、hordeTriggerRadius6、hordeWaves4、hordeWaveSize4、hordeInterval3、hordeWarningTurns2、hordeLiveLimit12、hordeSpawnRadius4。

### 9.2 疊層中毒

- `player.poison` 是層數，0–4。每層每個付費回合末1點，總傷害 `max(0,poison*poisonDamage-hazmat)`；**先扣當前層數傷害，再處理退層計時**。每2個付費回合退1層。4層無再中毒時，傷害依序4、4、3、3、2、2、1、1，總20點。
- `poisonClock` 是已經走過的退層間隔：省略代表0，第一個結算後為1，第二個結算刪除欄位並退1層。再次施毒只加層，不重設計時；免費預備、免費裝填、追擊等不推進。
- 噴吐蟲每次命中+1。感染者「帶毒」在**同一次攻擊**首次有效命中時+1，此後命中仍造成原傷害與壓制，但不再施毒／重複記錄；全部落空不施毒。兩名感染者各攻擊一次則各能加一層。
- 酸液保留原 `max(0,8-hazmat)` 地板傷害；當hazmat<8，每次地板結算另加1層，再進行當回合毒傷與退層。火焰／酸液不再暫停中毒。無防護首次踩酸液總扣9（8+1），下一回合離開再扣1並退清。這是本輪全域中毒改版的刻意規則變更，包含非蟲族關卡。
- 醫療包、終端治療、密封防護、原本換新樓層的清毒，都統一用 `clearPoison`，清除全部層數與計時；返回封存樓層仍保持原本不補資源、不自動清毒的規則。友軍／寵物仍不使用玩家中毒系統。
- 暫定數值：poisonCap4、poisonHitStacks1、poisonDamage1、poisonDecayTurns2、acidStacks1，集中在SWARM_TUNING。
- **平衡建議，未套用**：現有密封防護每階hazmat+5，因此第一階就完全抵銷最多4點毒傷。照使用者公式保留，視為明確反制；若實測太強，建議日後單獨把毒傷抗性改為每階減1（例如floor(hazmat/5)），保留地板防護原值。本輪不自行修改升級數值或公式。

### 9.3 save44、遷移與樓層所有權

- save **44**，profile5／backup1不變。舊save1–43的合法剩餘中毒回合0–6，換算為 `min(4,ceil(oldPoison/2))`，計時從完整2回合開始。新檔不再換算；非法舊值仍拒絕。不更改舊敵人詞條、亂數或既有樓層，不憑空在舊樓層補蟲潮；新生成樓層才加入。
- 可選樓層欄位 `swarmWaves={origin:{x,y},active,remaining,serial,wave,cooldown,pending:[{x,y,id,due}]}`。remaining是尚未出生總數（含預告），serial是已出生數，wave是已排定波數；固定守恆 `serial+remaining=16`、`serial+pending.length=wave*4`。幼體ID `horde-樓層-序號`，已死亡的仍保留在enemies供保存驗證。
- 已加入FLOOR_FIELDS；新樓層與返回樓層都先清掉前一層的可選值，避免混入。封存時保留pending與冷卻，返回時只平移due，離開樓層的時間不推進蟲潮；完整備份照原單局payload保存。
- 驗證包含：層數／派系、原點、計數守恆、ID唯一、預告格／due、horde來源／無獎勵旗標與活體上限。封存樓層同樣驗證。Poison層數與poisonClock獨立驗證，零層不得殘留計時。
- 沒有改動正式enemy-data基準：780生成、110任務、24機器人仍一致。新增樓層欄位僅蟲族生成，無中毒時不新增poisonClock，因此其他任務序列化形狀不變。

### 9.4 Claude 接手與驗收

- `reinforcementTelegraphs(g)` 合併撤退增援與蟲潮pending；renderer已接既有「+倒數」標記，仍只畫可視格。兩者保存清單分開，不挪用撤退任務欄位／限制。蟲潮抵達沿用nestSpawn/burrow；入侵點不是可破壞道具。
- 狀態列請顯示「中毒 N 層」，不要再顯示剩餘N回合；讀 `player.poison`。計時需要時讀 `player.poisonClock??0`，下次退層還需 `poisonDecayTurns-clock` 個付費回合。真實模式的數值隱藏仍照既有介面政策；本輪未改狀態列文字或毒液圖示。
- `node qa/create-wave-scenes.mjs` 產生 `qa/fixtures/swarm-waves/` 四份：未啟動、兩回合預告、12隻滿額、4層中毒且已走過一個退層間隔。舊 `create-swarm-scenes` 的毒液場景改為合法4層。只用 `?test=1` 匯入。
- 新測試12項，完整npm test **824/824**，build通過。正式enemy-data基準不變、未重錄；96張其他派系地圖的額外比較也通過。手機蟲潮壓力、標記可讀性、同時移動體感、防護第一階的免疫價值由使用者與Claude驗收。

## 10. Claude 驗證與介面（3.85.1）

### 10.1 驗證 Codex 3.85.0

- **自動檢查**：
  - npm test 824/824。
  - `node qa/enemy-data-identity.mjs` 與基準一致。
  - 繪製紀錄雜湊 `1f415fe7fcda0dac` 沒有變。
  - GitHub Pages run 34871967014 成功，線上版本 3.85.0。
- **合併**：Codex 發布前合併了 3.84.3，說話者倒下的泡泡淡出仍在。
- **程式檢查**：
  - 疊層中毒先扣傷害再退層；加層不重設計時；解毒統一走 `clearPoison`。
  - 感染者一次攻擊最多加一層。
  - 蟲潮不佔 `enemyRoom`、`expendableRoom`；入侵點不是道具；預告格併入 `reinforcementTelegraphs`。

### 10.2 介面

- **狀態列**：顯示「中毒 N 層」。
- **入侵點**：
  - 蟲潮啟動後，在入侵點那一格畫地洞（`drawNestSprite`，`burrow`）。
  - 還有蟲沒出來時是「活躍」，出完後是「坍塌」。
  - 畫在地面層、角色之下，不擋路、沒有生命條；還沒啟動時不畫。
- **驗收**：見 [3.85.1 QA](../qa/results/2026-09-15-claude-3.85.1-wave-status.md)。

### 10.3 待使用者決定

- **密封防護與中毒**：第一階 hazmat +5，就完全抵銷最多 4 層的中毒傷害。Codex 建議改成每階只抵銷 1 點毒傷（`floor(hazmat/5)`），地板傷害的防護維持原值。

## 11. 使用者決定（2026-09-15）

- **密封防護與中毒**：採 Codex 的建議，每一階（hazmat 5）只抵銷 1 點中毒傷害（`SWARM_TUNING.hazmatPerPoisonPoint`）；地板傷害仍扣完整的 hazmat（3.85.2）。
- **蟲潮預告**：地圖不再顯示蟲潮的「+N」倒數格，排波次時也不寫預告紀錄，蟲潮憑空出現。規則上仍是排定後兩回合才出生；撤退增援的預告保留（3.85.2）。
- **出生格被佔住**：採方案 A（3.85.2），見第 11.1 節。

### 11.1 出生格被佔住（3.85.2 已改）

- **現況**：
  - 到期的出生格被玩家、友軍、敵人、物品、道具或危害佔住時，那一隻會原格延後一回合，而且會一直等下去。
  - 只要還有一隻在等，就不會排下一波。
- **模擬**（每回合把出生的幼蟲清掉，避免擁擠干擾結果）：
  - 沒有阻擋：第 12 回合前 16 隻全部出生。
  - 玩家一直站在一個出生格上：只出生 3 隻，剩下 13 隻永遠不會來。
  - 出生格上掉了一個彈藥：同上，直到彈藥被撿走。
  - 玩家只在到期那回合站上去、之後離開：延後一回合，之後正常。
- **影響**：預告隱藏後，玩家可能在不知情的情況下卡住整個蟲潮。
- **決定與實作（3.85.2）**：
  - 使用者採方案 A：出生格被佔住時，改到入侵點 4 格內、離原格最近的空格出現，不會出現在任何單位身上；只有完全沒有空格時才等一回合。
  - 掉落物不算佔住，幼蟲可以出現在物品上。
  - 排新波次的空格判斷用同一套規則。卡住只剩「周圍全滿」或「活體已達 12 隻」兩種情況，這兩種情況本來就排不出新波次，所以不必另外放寬波次檢查，存檔驗證也不變。
  - 同樣的模擬改完後：四種情況 30 回合內都出生 16/16。
  - 既有測試「被擋住時原格延後」依使用者決定改寫為兩項：被擋住時換格，而且不拖住後續波次；掉落物不擋，只有完全沒有空格才等待。
