# 3.72.0 — 德魯伊餵養規則交付（Codex，2026-09-13）

## 實作範圍

四條三階成長、獨立燃料、相鄰付費餵食、醫療包純治療、砲台射擊、定時投擲物產出、死亡自動重生、無距離限制同行。飽食／飢餓沿用原強化 ID，保留共生對雜兵的回血。save v35；舊 active／packed／down／destroyed／留在別層的寵物遷移。羈絆不封存到個別樓層，完整備份保留。

API、初版數值與時序、Claude 接線欄位詳見 `docs/DRUID_FEEDING.md` 第11節。`src/pet-growth.js` 是規則入口；暫定數值集中 PET_FEEDING_TUNING。

## Codex 實測

- 最終完整 `npm test`：**653/653 通過**，約121秒。
- 新增 `tests/pet-feeding.test.mjs` 19 項：覆蓋規格15條不變條件，另含燃料精度／容量、免費與付費時間、快速敵人使餵食失效、兩發與擊殺後停火、射線失效仍扣燃料、存檔拒絕非法狀態、無落點重生等待、致命保護與減傷。
- 更新 `tests/allies.test.mjs` 四項已被取代的收納測試；保留機體、召喚、倖存友軍的回歸。舊低血玩家取消餵食案例改成敵人靠近玩家，避免改版後存活寵物搶走目標。
- `tests/endless.test.mjs` 的深層寵物備份改用合法 arriving 狀態，保留原備份深度／紀錄斷言。
- `npm run build` 通過；新增模組已納入 SW。
- `node qa/create-pet-feeding-scenes.mjs`：六份場景均通過引擎 restore，輸出於已忽略的 `qa/fixtures/`。
- `git diff --check` 通過。

## 未驗證與交接

- 本輪不做餵食 UI。controller 技能頁還不識別 reforming／arriving，舊說明還有收納文字；Claude 接介面時一併更新。角色／技能資料與強化名稱已改。
- 未用真手機或瀏覽器驗證新演出，未跑自然平衡模擬；本次 Node 場景不能證明遊玩體感。使用者與 Claude 接手 `?test=1`。
- 舊 `PET_REGEN/PET_MEDKIT_FRACTION` 匯出只為避免舊 controller 匯入失敗；不再驅動任何寵物規則。Claude 移除舊文案後可一併清除。
- 素材、其他待辦與使用者未追蹤頭像未納入本次提交。

發布目標：main／GitHub Pages，3.72.0。最終 SHA 與部署結果由交付訊息提供。
