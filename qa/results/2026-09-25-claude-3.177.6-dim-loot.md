# 3.177.6 — 撿不了的掉落物畫成半透明；中文目標列

2026-09-25，Claude。規格：docs/LOOT_ICONS_HANDOFF.md（3.177.6）、docs/PURGE_REVIEW.md 第 3 節。

## 需求

Claude 提了一份打磨清單（不加新功能），使用者：

- 「1、2 我都驗證過了（其實以後推上去後我沒特別說就是驗證過了）」：驗證紙條的 42 項結案，結尾節奏維持。
- 「3 同意」：中文「目標狀態」改成「目標」。
- 「4 撿不了的半透明，當時被插隊忘記說」。
- 「先改這些，剩下的下一輪，改完直接推」。

## 做法

- **`src/game.js`**：新增 `Game.canTake(item)`，只讀不改，照 `pickup()` 判斷現在走上去能不能拿到一點。
  - 拿不到：武器欄已滿；彈藥、投擲物（共用上限）、醫療包與道具已經封頂；護甲板已滿；外骨骼已經有或是大型職業；夜視鏡、熱像鏡已經有。
  - 其他（廢料、資料、繩索、鑰匙卡、學習資料）一律拿得到。
- **`src/renderer.js`**：地上掉落物改經 `groundItem`。`canTake` 是 false 時，用 `LOOT_ICON.dim`（45%，`src/loot-icons.js`）畫，畫完還原。記得位置但看不到的物品也一樣。
- **語言表**：`purge-review-ui.objectiveStatus` 中文改成「目標」（英文 3.177.0 已是 Objective）。
- **驗證紙條**：
  - 42 項原文封存到 docs/archive/verification-notes-3.147-to-3.177.md；
  - 紙條改成「驗證方式」加「最近推送的檢查重點」；
  - docs/RELEASE.md 第 4 步、docs/HANDOFF.md 跟著改；
  - 記憶加一條：推送後沒特別說即視為已驗。

## 驗證（Claude 自己跑的）

- **`tests/loot-dim.test.mjs`（新）**：
  - 每一種掉落物（5 種彈藥、4 種投擲物、醫療包、6 種道具、護甲板、外骨骼、兩種鏡、廢料、武器）從開局一路撿到滿，每一步都比對 `canTake` 和 `pickup()` 實際有沒有從地上拿走東西，全部一致；除了廢料，每種最後都會滿。
  - `groundItem` 對拿不到的用 `LOOT_ICON.dim` 畫、拿得到的照原樣，畫完畫布的透明度還原。
- **`tests/purge-review.test.mjs`**：舊存檔的結算列名改成「單位／目標」。
- **完整檢查**：`npm test` 1305 通過、0 失敗；`npm run build` 成功（`/ash_protocol/` 相容）；英文掃描乾淨；敵人資料身分檢查與基準一致（生成 780、任務 110、機器人 24）。

## 未驗

- 瀏覽器實際畫面：瀏覽器窗格這時沒有在繪製（視窗被隱藏），截不到圖，只靠上面的測試。實機上 45% 夠不夠看得出、會不會太淡，交給使用者看。

## 部署

- 見下方。
